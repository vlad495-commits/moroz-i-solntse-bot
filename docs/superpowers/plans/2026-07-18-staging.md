# Phase 3 Staging Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Развернуть на текущем VPS изолированный staging-контур Reliable Telegram Pipeline и доказать через отдельный Telegram test bot один live inbox, один LLM-вызов, один outbound, отсутствие дубля и безопасное восстановление.

**Architecture:** Base Compose переиспользуется через минимальный staging override и явный project name `moroz-staging`. Публичен только Caddy route `/telegram/webhook`; PostgreSQL остаётся источником критического состояния, Redis и RabbitMQ изолированы project-scoped volumes, а один profile-only CLI безопасно управляет webhook и агрегатным evidence.

**Tech Stack:** Python 3.12 stdlib, уже установленные aiogram 3.27.0 и asyncpg 0.31.0, FastAPI/uvicorn, PostgreSQL 16, Redis 7, RabbitMQ 4.3.2, Docker Compose, Caddy 2.11.4, pytest 8.4.1.

## Global Constraints

- Источники правды: `AGENTS.md`, `План реализации.md`, `Дорожная карта.md`, `ТЗ и архитектура.md`, `docs/superpowers/specs/2026-07-18-staging-design.md` и актуальные фазовые планы.
- Учебная лестница и `project/.step` не используются.
- Проект и все проверки запускаются только через Docker Compose; прямой Python на host запрещён.
- Все local/fake проверки используют task-specific Compose project и одноразовые credentials; общий и production контуры не запускаются, не останавливаются и не очищаются.
- Staging на VPS всегда использует project name `moroz-staging`, отдельный checkout, отдельный ignored `.env`, отдельные credentials, сеть и volumes.
- Отдельный Telegram test token обязателен; identity gate сравнивает `getMe().id` с `STAGING_TELEGRAM_BOT_ID` до `setWebhook`.
- `TELEGRAM_WEBHOOK_SECRET` имеет 32–64 символа из `[A-Za-z0-9_-]`; bot и `setWebhook` получают одно и то же значение.
- Публичен только точный `/telegram/webhook`; stores, queue, admin, OpenAPI и health endpoints наружу не публикуются.
- `allowed_updates=["message", "callback_query"]`, `max_connections=5`, `drop_pending_updates=false` по умолчанию.
- Bot/worker/migrate images работают non-root; Caddy запускается UID/GID `10001:10001` на внутренних 8080/8443 с host mappings 80/443.
- Existing `stop_grace_period: 30s` сохраняется; worker shutdown должен завершаться не более чем за 30 секунд.
- Raw payload, chat/user IDs, токены, passwords, DSN, secret headers, prompt/user text и provider responses не выводятся в evidence или сообщения.
- `alembic downgrade`, destructive DB rollback и удаление volumes запрещены; rollback меняет только app images.
- YCLIENTS, guardrails, новые scheduler-функции, production admin, monitoring/alerts, backup/restore и production release не реализуются.
- Caddy закреплён как `caddy:2.11.4`; новые Python/runtime dependencies не добавляются.
- Каждая implementation task идёт red → green → затронутый Docker gate → отдельный commit → specification review → quality review.
- Phase 3 закрывается только после `Local/fake staging ready` и `Live staging passed`; fake evidence не заменяет live gate.

## Плановая структура файлов

- Create `project/docker-compose.staging.yml` — staging image tags, loopback bot port, Caddy и два least-privilege one-shot services.
- Create `project/ops/staging/Caddyfile` — автоматический HTTPS и единственный webhook route.
- Create `project/ops/staging.py` — webhook identity/config, aggregate snapshots, duplicate/recovery update injection и safe log scan.
- Create `project/tests/unit/test_staging.py` — единый focused unit/contract module для override, Caddy и CLI.
- Create `project/ops/staging-runbook.md` — exact preflight, launch, check, recovery, stop и rollback commands.
- Modify `project/tests/unit/test_documented_compose_commands.py` — runbook входит в canonical command contract.
- Modify `Дорожная карта.md`, `План реализации.md`, `changelog.md` — local/live evidence и фазовый статус.

## Task-specific Docker credential preamble

Before every Task 1–4 Docker command, run this PowerShell function once in the current shell. It overrides repository `.env` values without printing or persisting credentials:

```powershell
function Set-StagingTaskEnvironment {
    $suffix = [Guid]::NewGuid().ToString('N')
    $env:POSTGRES_USER = "staging_$suffix"
    $env:POSTGRES_PASSWORD = [Guid]::NewGuid().ToString('N')
    $env:POSTGRES_DB = "staging_$suffix"
    $env:DATABASE_URL = ""
    $env:REDIS_PASSWORD = [Guid]::NewGuid().ToString('N')
    $env:REDIS_URL = "redis://:$($env:REDIS_PASSWORD)@redis:6379/0"
    $env:RABBITMQ_USER = "staging_$suffix"
    $env:RABBITMQ_PASSWORD = [Guid]::NewGuid().ToString('N')
    $env:RABBITMQ_URL = "amqp://$($env:RABBITMQ_USER):$($env:RABBITMQ_PASSWORD)@rabbitmq:5672/"
    $env:TELEGRAM_BOT_TOKEN = "123456:$([Guid]::NewGuid().ToString('N'))abc"
    $env:STAGING_TELEGRAM_BOT_ID = "123456"
    $env:TELEGRAM_WEBHOOK_SECRET = [Guid]::NewGuid().ToString('N')
    $env:LLM_API_KEY = "local-fake-$suffix"
    $env:OPENAI_API_KEY = ""
    $env:STAGING_IMAGE_TAG = "task-$suffix"
    $env:STAGING_DOMAIN = "staging.example.test"
    $env:STAGING_PUBLIC_URL = "https://staging.example.test"
}

Set-StagingTaskEnvironment
```

---

### Task 1: Изолированный Compose override и HTTPS ingress

**Files:**
- Create: `project/docker-compose.staging.yml`
- Create: `project/ops/staging/Caddyfile`
- Create: `project/tests/unit/test_staging.py`

**Interfaces:**
- Consumes: существующие services `bot`, `worker`, `migrate`, `cutover`, `postgres`, `redis`, `rabbitmq` из `project/docker-compose.yml`.
- Produces: Compose services `caddy`, image tags `${STAGING_IMAGE_TAG}`, loopback bind `${STAGING_BOT_PORT:-18081}`, volumes `staging_caddy_data`/`staging_caddy_config`.

- [x] **Step 1: Написать failing staging contract tests**

Создать `project/tests/unit/test_staging.py`:

```python
from pathlib import Path

import yaml


ROOT = Path("/workspace")
STAGING = ROOT / "docker-compose.staging.yml"
CADDYFILE = ROOT / "ops/staging/Caddyfile"


def load_staging():
    return yaml.safe_load(STAGING.read_text(encoding="utf-8"))


def test_staging_override_tags_apps_and_never_publishes_stores():
    services = load_staging()["services"]
    assert services["bot"]["image"] == "moroz-staging-bot:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    assert services["worker"]["image"] == "moroz-staging-worker:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    assert services["migrate"]["image"] == "moroz-staging-migrate:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    assert services["cutover"]["image"] == services["migrate"]["image"]
    assert services["bot"]["ports"] == [
        "127.0.0.1:${STAGING_BOT_PORT:-18081}:8081"
    ]
    for name in ("postgres", "redis", "rabbitmq"):
        assert "ports" not in services.get(name, {})


def test_caddy_is_pinned_non_root_and_routes_only_webhook():
    compose = load_staging()
    caddy = compose["services"]["caddy"]
    assert caddy["image"] == "caddy:2.11.4"
    assert caddy["profiles"] == ["staging-ingress"]
    assert caddy["user"] == "10001:10001"
    assert caddy["read_only"] is True
    assert caddy["cap_drop"] == ["ALL"]
    assert caddy["security_opt"] == ["no-new-privileges:true"]
    assert caddy["ports"] == ["80:8080", "443:8443", "443:8443/udp"]
    assert set(compose["volumes"]) == {
        "staging_caddy_data",
        "staging_caddy_config",
    }

    text = CADDYFILE.read_text(encoding="utf-8")
    assert "http_port 8080" in text
    assert "https_port 8443" in text
    assert "path /telegram/webhook" in text
    assert "reverse_proxy bot:8081" in text
    assert "respond 404" in text
    assert "/admin" not in text
    assert "/openapi.json" not in text


def test_staging_bot_healthcheck_probes_http_listener():
    health = " ".join(load_staging()["services"]["bot"]["healthcheck"]["test"])
    assert "http://127.0.0.1:8081/openapi.json" in health
    assert "/proc/1/cmdline" not in health
```

- [x] **Step 2: Запустить RED через task-specific Docker Compose**

Run from `project/` with shell-only one-time credentials:

```powershell
docker compose --env-file ../.env -p moroz-staging-task1-red --profile test build test
docker compose --env-file ../.env -p moroz-staging-task1-red --profile test run --rm test pytest tests/unit/test_staging.py -q
```

Expected: FAIL because `docker-compose.staging.yml` and `ops/staging/Caddyfile` are absent. If Docker daemon is unavailable, record infrastructure blocker and do not call the test red/green.

- [x] **Step 3: Добавить минимальный staging override**

Create `project/docker-compose.staging.yml`:

```yaml
services:
  bot:
    image: "moroz-staging-bot:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    ports:
      - "127.0.0.1:${STAGING_BOT_PORT:-18081}:8081"
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          python -c "import urllib.request;
          raise SystemExit(0 if urllib.request.urlopen(
          'http://127.0.0.1:8081/openapi.json', timeout=3).status == 200 else 1)"
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s

  worker:
    image: "moroz-staging-worker:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"

  migrate:
    image: "moroz-staging-migrate:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"

  cutover:
    image: "moroz-staging-migrate:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"

  caddy:
    profiles: ["staging-ingress"]
    image: caddy:2.11.4
    user: "10001:10001"
    restart: unless-stopped
    read_only: true
    cap_drop: ["ALL"]
    security_opt: ["no-new-privileges:true"]
    environment:
      STAGING_DOMAIN: ${STAGING_DOMAIN:?set STAGING_DOMAIN}
    ports:
      - "80:8080"
      - "443:8443"
      - "443:8443/udp"
    volumes:
      - ./ops/staging/Caddyfile:/etc/caddy/Caddyfile:ro
      - staging_caddy_data:/data
      - staging_caddy_config:/config
    tmpfs:
      - /tmp
    depends_on:
      bot:
        condition: service_healthy
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: 5m
        max-file: "3"
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  staging_caddy_data:
  staging_caddy_config:
```

Create `project/ops/staging/Caddyfile`:

```caddy
{
	http_port 8080
	https_port 8443
	admin off
}

{$STAGING_DOMAIN} {
	@telegram_webhook path /telegram/webhook

	handle @telegram_webhook {
		reverse_proxy bot:8081
	}

	handle {
		respond 404
	}
}
```

- [x] **Step 4: Запустить GREEN и Compose/Caddy render checks**

Set nonsecret render values first:

```powershell
$env:STAGING_IMAGE_TAG = "task1"
$env:STAGING_DOMAIN = "staging.example.test"
```

Run:

```powershell
docker compose --env-file ../.env -p moroz-staging-task1-green --profile test build test
docker compose --env-file ../.env -p moroz-staging-task1-green --profile test run --rm test pytest tests/unit/test_staging.py -q
docker compose --env-file ../.env -p moroz-staging-task1-green -f docker-compose.yml -f docker-compose.staging.yml config --quiet
docker compose --env-file ../.env -p moroz-staging-task1-green -f docker-compose.yml -f docker-compose.staging.yml --profile staging-ingress run --rm --no-deps caddy caddy validate --config /etc/caddy/Caddyfile
```

Expected: focused pytest PASS, Compose config exit 0, Caddy validation exit 0. Do not start bot or bind 80/443 in this task.

- [x] **Step 5: Cleanup и commit**

Run cleanup and verify `0/0/0/0`, then:

```bash
git add project/docker-compose.staging.yml project/ops/staging/Caddyfile project/tests/unit/test_staging.py changelog.md
git commit -m "ops: добавлен изолированный staging ingress"
```

---

### Task 2: Безопасное управление Telegram webhook

**Files:**
- Create: `project/ops/staging.py`
- Modify: `project/docker-compose.staging.yml`
- Modify: `project/tests/unit/test_staging.py`

**Interfaces:**
- Produces: `validated_public_url(value: str) -> str` and `validated_secret(value: str) -> str` for both webhook and smoke trust boundaries.
- Produces: `WebhookConfig.from_env(env: Mapping[str, str]) -> WebhookConfig`.
- Produces: `manage_webhook(action: str, config: WebhookConfig, *, drop_pending_updates: bool = False, bot_factory=Bot) -> dict[str, object]`.
- CLI: `python /app/ops/staging.py webhook {set|status|delete} [--drop-pending-updates]`.

- [x] **Step 1: Написать RED tests для validation, identity gate и exact setWebhook**

Append to `project/tests/unit/test_staging.py`:

```python
import importlib.util
from types import SimpleNamespace

import pytest


def load_staging_module():
    spec = importlib.util.spec_from_file_location(
        "staging_ops", ROOT / "ops/staging.py"
    )
    module = importlib.util.module_from_spec(spec)
    import sys
    sys.modules[spec.name] = module
    spec.loader.exec_module(module)
    return module


def webhook_env(**overrides):
    values = {
        "TELEGRAM_BOT_TOKEN": "123456:abcdefghijklmnopqrstuvwxyzABCDE",
        "STAGING_TELEGRAM_BOT_ID": "123456",
        "STAGING_PUBLIC_URL": "https://staging.example.test",
        "TELEGRAM_WEBHOOK_SECRET": "A" * 32,
    }
    values.update(overrides)
    return values


def test_webhook_config_rejects_http_and_invalid_secret():
    staging = load_staging_module()
    with pytest.raises(ValueError, match="staging_public_url_invalid"):
        staging.WebhookConfig.from_env(
            webhook_env(STAGING_PUBLIC_URL="http://staging.example.test")
        )
    with pytest.raises(ValueError, match="webhook_secret_invalid"):
        staging.WebhookConfig.from_env(
            webhook_env(TELEGRAM_WEBHOOK_SECRET="secret with spaces")
        )


@pytest.mark.asyncio
async def test_identity_mismatch_stops_before_set_webhook():
    staging = load_staging_module()

    class FakeBot:
        def __init__(self, token):
            self.set_calls = []
            self.session = SimpleNamespace(close=self.close)

        async def close(self):
            return None

        async def get_me(self):
            return SimpleNamespace(id=999999)

        async def set_webhook(self, **kwargs):
            self.set_calls.append(kwargs)

    created = []
    with pytest.raises(RuntimeError, match="staging_bot_identity_mismatch"):
        await staging.manage_webhook(
            "set",
            staging.WebhookConfig.from_env(webhook_env()),
            bot_factory=lambda token: created.append(FakeBot(token)) or created[-1],
        )
    assert created[0].set_calls == []


@pytest.mark.asyncio
async def test_set_webhook_uses_exact_safe_contract():
    staging = load_staging_module()

    class FakeBot:
        def __init__(self, token):
            self.set_calls = []
            self.session = SimpleNamespace(close=self.close)

        async def close(self):
            return None

        async def get_me(self):
            return SimpleNamespace(id=123456)

        async def set_webhook(self, **kwargs):
            self.set_calls.append(kwargs)
            return True

    bot = FakeBot("unused")
    result = await staging.manage_webhook(
        "set",
        staging.WebhookConfig.from_env(webhook_env()),
        bot_factory=lambda _token: bot,
    )
    assert result == {"ok": True, "action": "set"}
    assert bot.set_calls == [{
        "url": "https://staging.example.test/telegram/webhook",
        "secret_token": "A" * 32,
        "allowed_updates": ["message", "callback_query"],
        "max_connections": 5,
        "drop_pending_updates": False,
    }]
```

- [x] **Step 2: Запустить RED**

Run:

```powershell
docker compose --env-file ../.env -p moroz-staging-task2-red --profile test run --rm test pytest tests/unit/test_staging.py -q
```

Expected: FAIL because `ops/staging.py` and webhook interfaces are absent.

- [x] **Step 3: Реализовать минимальный webhook CLI без raw exception output**

Create `project/ops/staging.py` with these exact core interfaces:

```python
from __future__ import annotations

import argparse
import asyncio
from dataclasses import dataclass
import json
import os
import re
from typing import Mapping
from urllib.parse import urlsplit

from aiogram import Bot


WEBHOOK_SECRET = re.compile(r"[A-Za-z0-9_-]{32,64}\Z")


def validated_public_url(value: str) -> str:
    public_url = value.rstrip("/")
    parsed = urlsplit(public_url)
    if (
        parsed.scheme != "https"
        or not parsed.hostname
        or parsed.username is not None
        or parsed.password is not None
        or parsed.path not in {"", "/"}
        or parsed.query
        or parsed.fragment
    ):
        raise ValueError("staging_public_url_invalid")
    return public_url


def validated_secret(value: str) -> str:
    if WEBHOOK_SECRET.fullmatch(value) is None:
        raise ValueError("webhook_secret_invalid")
    return value


@dataclass(frozen=True, slots=True)
class WebhookConfig:
    token: str
    expected_bot_id: int
    webhook_url: str
    secret: str

    @classmethod
    def from_env(cls, env: Mapping[str, str]) -> "WebhookConfig":
        public_url = validated_public_url(env.get("STAGING_PUBLIC_URL", ""))
        secret = validated_secret(env.get("TELEGRAM_WEBHOOK_SECRET", ""))
        expected = env.get("STAGING_TELEGRAM_BOT_ID", "")
        if not expected.isdecimal():
            raise ValueError("staging_bot_id_invalid")
        token = env.get("TELEGRAM_BOT_TOKEN", "")
        if not token:
            raise ValueError("telegram_token_missing")
        return cls(
            token=token,
            expected_bot_id=int(expected),
            webhook_url=f"{public_url}/telegram/webhook",
            secret=secret,
        )


async def manage_webhook(
    action: str,
    config: WebhookConfig,
    *,
    drop_pending_updates: bool = False,
    bot_factory=Bot,
) -> dict[str, object]:
    bot = bot_factory(config.token)
    try:
        identity = await bot.get_me()
        if identity.id != config.expected_bot_id:
            raise RuntimeError("staging_bot_identity_mismatch")
        if action == "set":
            accepted = await bot.set_webhook(
                url=config.webhook_url,
                secret_token=config.secret,
                allowed_updates=["message", "callback_query"],
                max_connections=5,
                drop_pending_updates=drop_pending_updates,
            )
            if not accepted:
                raise RuntimeError("telegram_set_webhook_rejected")
            return {"ok": True, "action": "set"}
        if action == "status":
            info = await bot.get_webhook_info()
            allowed = sorted(info.allowed_updates or [])
            return {
                "ok": (
                    info.url == config.webhook_url
                    and allowed == ["callback_query", "message"]
                    and info.max_connections == 5
                ),
                "action": "status",
                "pending_update_count": info.pending_update_count,
                "has_last_error": info.last_error_date is not None,
            }
        if action == "delete":
            deleted = await bot.delete_webhook(drop_pending_updates=False)
            if not deleted:
                raise RuntimeError("telegram_delete_webhook_rejected")
            return {"ok": True, "action": "delete"}
        raise ValueError("webhook_action_invalid")
    finally:
        await bot.session.close()


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser()
    groups = parser.add_subparsers(dest="group", required=True)
    webhook = groups.add_parser("webhook")
    webhook.add_argument("action", choices=("set", "status", "delete"))
    webhook.add_argument("--drop-pending-updates", action="store_true")
    return parser


async def run(args: argparse.Namespace) -> dict[str, object]:
    if args.group == "webhook":
        return await manage_webhook(
            args.action,
            WebhookConfig.from_env(os.environ),
            drop_pending_updates=args.drop_pending_updates,
        )
    raise ValueError("command_group_invalid")


def main() -> int:
    try:
        result = asyncio.run(run(build_parser().parse_args()))
        print(json.dumps(result, sort_keys=True))
        return 0 if result.get("ok") else 1
    except Exception as error:
        print(json.dumps({"ok": False, "error_type": type(error).__name__}))
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

Add to `project/docker-compose.staging.yml`:

```yaml
  staging-webhook:
    profiles: ["staging-tools"]
    image: "moroz-staging-bot:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    entrypoint: ["python", "/app/ops/staging.py", "webhook"]
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?set TELEGRAM_BOT_TOKEN}
      STAGING_TELEGRAM_BOT_ID: ${STAGING_TELEGRAM_BOT_ID:?set STAGING_TELEGRAM_BOT_ID}
      STAGING_PUBLIC_URL: ${STAGING_PUBLIC_URL:?set STAGING_PUBLIC_URL}
      TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET:?set TELEGRAM_WEBHOOK_SECRET}
    volumes:
      - ./ops:/app/ops:ro
```

Extend the contract test to assert `staging-webhook` has no `env_file` and exactly the four environment keys above.

- [x] **Step 4: Запустить GREEN и safe-output tests**

Add:

```python
def test_cli_failure_prints_only_error_type(monkeypatch, capsys):
    staging = load_staging_module()
    sentinel = "https://user:password@provider.test private-user-text"

    async def fail(_args):
        raise RuntimeError(sentinel)

    monkeypatch.setattr(staging, "run", fail)
    monkeypatch.setattr(
        staging,
        "build_parser",
        lambda: SimpleNamespace(parse_args=lambda: SimpleNamespace()),
    )
    assert staging.main() == 1
    output = capsys.readouterr().out
    assert json.loads(output) == {"ok": False, "error_type": "RuntimeError"}
    assert sentinel not in output
```

Add `import json` to the test module. Then run:

```powershell
docker compose --env-file ../.env -p moroz-staging-task2-green --profile test run --rm test pytest tests/unit/test_staging.py -q
```

Expected: PASS; token, secret and exception message sentinels absent from captured output.

- [x] **Step 5: Commit**

```bash
git add project/ops/staging.py project/docker-compose.staging.yml project/tests/unit/test_staging.py changelog.md
git commit -m "ops: добавлено безопасное управление staging webhook"
```

---

### Task 3: Aggregate smoke, duplicate и recovery evidence

**Files:**
- Modify: `project/ops/staging.py`
- Modify: `project/docker-compose.staging.yml`
- Modify: `project/tests/unit/test_staging.py`

**Interfaces:**
- Produces CLI `smoke snapshot --label live`, `smoke verify --label {live|worker-restart|redis-loss}`, `smoke replay-live`, `smoke inject --label {worker-restart|redis-loss}`, `smoke scan-logs`.
- State files: `/state/staging-live.json`, `/state/staging-worker-restart.json`, `/state/staging-redis-loss.json`; contain only timestamp and aggregate counts.
- Synthetic update IDs: `worker-restart=-1000000001`, `redis-loss=-1000000002`.

- [x] **Step 1: Написать RED tests для безопасного state, replay и sanitizer**

Append focused tests:

```python
def test_evidence_state_contains_only_safe_aggregates(tmp_path):
    staging = load_staging_module()
    path = staging.write_snapshot(
        "live",
        staging.Counts(inbox=4, llm=3, sent=2),
        started_at="2026-07-18T09:00:00+00:00",
        state_dir=tmp_path,
    )
    assert path.read_text(encoding="utf-8") == (
        '{"counts":{"inbox":4,"llm":3,"sent":2},'
        '"label":"live","started_at":"2026-07-18T09:00:00+00:00",'
        '"version":1}'
    )


def test_build_replay_update_uses_persisted_fields_without_logging_them():
    staging = load_staging_module()
    update = staging.build_update({
        "update_id": "902",
        "message_id": "17",
        "chat_id": "42",
        "user_id": "7",
        "text": staging.CANARY_TEXT,
        "received_at": "2026-07-18T09:00:00+00:00",
    })
    assert update["update_id"] == 902
    assert update["message"]["chat"] == {"id": 42, "type": "private"}
    assert update["message"]["from"]["id"] == 7
    assert update["message"]["text"] == staging.CANARY_TEXT


def test_safe_log_scan_returns_counts_not_matching_lines():
    staging = load_staging_module()
    private = "bot123456:secret-token private-user-text"
    result = staging.scan_log_lines([private, "Traceback (most recent call last)"])
    assert result == {
        "ok": False,
        "secret_shaped_lines": 1,
        "traceback_lines": 1,
    }
    assert private not in repr(result)


def test_evidence_delta_requires_exactly_one_each():
    staging = load_staging_module()
    before = staging.Counts(inbox=4, llm=3, sent=2)
    assert staging.evidence_delta(before, staging.Counts(5, 4, 3)) == {
        "ok": True,
        "inbox_delta": 1,
        "llm_delta": 1,
        "sent_delta": 1,
    }
    assert staging.evidence_delta(before, staging.Counts(5, 5, 3))["ok"] is False
```

- [x] **Step 2: Запустить RED**

```powershell
docker compose --env-file ../.env -p moroz-staging-task3-red --profile test run --rm test pytest tests/unit/test_staging.py -q
```

Expected: FAIL because evidence interfaces are absent.

- [x] **Step 3: Добавить evidence primitives и exact queries**

Extend `project/ops/staging.py` with:

```python
from dataclasses import asdict
from datetime import UTC, datetime
from pathlib import Path
import sys
from urllib import request

import asyncpg

from moroz.common.config import database_url_from_env


CANARY_TEXT = "staging canary: проверка ответа"
SYNTHETIC = {
    "worker-restart": (-1000000001, "staging synthetic: worker restart"),
    "redis-loss": (-1000000002, "staging synthetic: redis loss"),
}
LABELS = {"live", *SYNTHETIC}


@dataclass(frozen=True, slots=True)
class Counts:
    inbox: int
    llm: int
    sent: int


@dataclass(frozen=True, slots=True)
class Snapshot:
    counts: Counts
    started_at: datetime


def write_snapshot(
    label: str,
    counts: Counts,
    *,
    started_at: str | None = None,
    state_dir=Path("/state"),
) -> Path:
    if label not in LABELS:
        raise ValueError("evidence_label_invalid")
    path = state_dir / f"staging-{label}.json"
    started = started_at or datetime.now(UTC).isoformat()
    path.write_text(
        json.dumps(
            {
                "counts": asdict(counts),
                "label": label,
                "started_at": started,
                "version": 1,
            },
            ensure_ascii=False,
            sort_keys=True,
            separators=(",", ":"),
        ),
        encoding="utf-8",
    )
    return path


def read_snapshot(label: str, *, state_dir=Path("/state")) -> Snapshot:
    if label not in LABELS:
        raise ValueError("evidence_label_invalid")
    data = json.loads((state_dir / f"staging-{label}.json").read_text("utf-8"))
    if data.get("version") != 1 or data.get("label") != label:
        raise ValueError("evidence_state_invalid")
    return Snapshot(
        counts=Counts(**data["counts"]),
        started_at=datetime.fromisoformat(data["started_at"]),
    )


def evidence_delta(before: Counts, after: Counts) -> dict[str, object]:
    result = {
        "inbox_delta": after.inbox - before.inbox,
        "llm_delta": after.llm - before.llm,
        "sent_delta": after.sent - before.sent,
    }
    return {"ok": set(result.values()) == {1}, **result}


async def collect_counts(connection) -> Counts:
    row = await connection.fetchrow(
        """
        SELECT
          (SELECT count(*) FROM message_inbox) AS inbox,
          (SELECT count(*) FROM token_usage) AS llm,
          (SELECT count(*) FROM outbound_messages WHERE status = 'sent') AS sent
        """
    )
    return Counts(row["inbox"], row["llm"], row["sent"])


def build_update(payload: dict[str, str], *, update_id=None, text=None) -> dict:
    received = datetime.fromisoformat(payload["received_at"])
    return {
        "update_id": int(payload["update_id"] if update_id is None else update_id),
        "message": {
            "message_id": int(payload["message_id"]),
            "date": int(received.timestamp()),
            "chat": {"id": int(payload["chat_id"]), "type": "private"},
            "from": {
                "id": int(payload["user_id"]),
                "is_bot": False,
                "first_name": "Staging",
            },
            "text": payload["text"] if text is None else text,
        },
    }


def scan_log_lines(lines) -> dict[str, object]:
    secret = re.compile(
        r"bot\d+:[A-Za-z0-9_-]{20,}|"
        r"(?:postgresql|redis|amqp)s?://[^\s@]+:[^\s@]+@|"
        r"(?:Authorization|X-Telegram-Bot-Api-Secret-Token)\s*[:=]"
    )
    secret_count = 0
    traceback_count = 0
    for line in lines:
        secret_count += bool(secret.search(line))
        traceback_count += "Traceback (most recent call last)" in line
    return {
        "ok": secret_count == 0 and traceback_count == 0,
        "secret_shaped_lines": secret_count,
        "traceback_lines": traceback_count,
    }


async def post_update(public_url: str, secret: str, update: dict) -> None:
    body = json.dumps(update, ensure_ascii=False).encode("utf-8")
    req = request.Request(
        f"{public_url.rstrip('/')}/telegram/webhook",
        data=body,
        method="POST",
        headers={
            "Content-Type": "application/json",
            "X-Telegram-Bot-Api-Secret-Token": secret,
        },
    )
    def send() -> int:
        with request.urlopen(req, timeout=10) as response:
            return response.status

    if await asyncio.to_thread(send) != 200:
        raise RuntimeError("staging_webhook_rejected")
```

Add exact DB selectors inside smoke commands:

```sql
SELECT payload
FROM message_inbox
WHERE payload->>'text' = $1
  AND created_at >= $2
ORDER BY created_at DESC
LIMIT 1
```

Pass `Snapshot.started_at` as `$2`. For synthetic verification use `external_message_id = $1`. Never select or print raw outbound text. Validate `STAGING_PUBLIC_URL` with `validated_public_url` and `TELEGRAM_WEBHOOK_SECRET` with `validated_secret` before network I/O. Connect with `asyncpg.connect(database_url_from_env(os.environ))`, close in `finally`, and catch only at `main()` so output remains `error_type` only.

- [x] **Step 4: Добавить CLI wiring и least-privilege Compose service**

Add the following smoke command functions. They save only UTC timestamp/counts, poll at most 120 seconds, never return payload or identifiers, and reuse the already consented live identity only inside memory:

```python
def smoke_config(env: Mapping[str, str]) -> tuple[str, str, str]:
    return (
        database_url_from_env(env),
        validated_public_url(env.get("STAGING_PUBLIC_URL", "")),
        validated_secret(env.get("TELEGRAM_WEBHOOK_SECRET", "")),
    )


async def find_payload(connection, label: str, snapshot: Snapshot) -> dict | None:
    if label == "live":
        row = await connection.fetchrow(
            """
            SELECT payload
            FROM message_inbox
            WHERE payload->>'text' = $1
              AND created_at >= $2
            ORDER BY created_at DESC
            LIMIT 1
            """,
            CANARY_TEXT,
            snapshot.started_at,
        )
    else:
        row = await connection.fetchrow(
            """
            SELECT payload
            FROM message_inbox
            WHERE channel = 'telegram' AND external_message_id = $1
            """,
            str(SYNTHETIC[label][0]),
        )
    if row is None:
        return None
    payload = row["payload"]
    if isinstance(payload, str):
        payload = json.loads(payload)
    required = {"update_id", "message_id", "chat_id", "user_id", "text", "received_at"}
    if not isinstance(payload, dict) or not required <= payload.keys():
        raise ValueError("staging_evidence_payload_invalid")
    return payload


async def snapshot_command(label: str, database_url: str) -> dict[str, object]:
    connection = await asyncpg.connect(database_url)
    try:
        write_snapshot(label, await collect_counts(connection))
    finally:
        await connection.close()
    return {"ok": True, "action": "snapshot", "label": label}


async def verify_command(label: str, database_url: str) -> dict[str, object]:
    snapshot = read_snapshot(label)
    connection = await asyncpg.connect(database_url)
    try:
        deadline = asyncio.get_running_loop().time() + 120
        while True:
            result = evidence_delta(snapshot.counts, await collect_counts(connection))
            target = await find_payload(connection, label, snapshot)
            if result["ok"] and target is not None:
                return {"action": "verify", "label": label, **result}
            if asyncio.get_running_loop().time() >= deadline:
                return {"action": "verify", "label": label, **result, "ok": False}
            await asyncio.sleep(1)
    finally:
        await connection.close()


async def replay_live(database_url: str, public_url: str, secret: str) -> dict[str, object]:
    snapshot = read_snapshot("live")
    connection = await asyncpg.connect(database_url)
    try:
        payload = await find_payload(connection, "live", snapshot)
    finally:
        await connection.close()
    if payload is None:
        raise RuntimeError("staging_evidence_update_missing")
    await post_update(public_url, secret, build_update(payload))
    return await verify_command("live", database_url)


async def inject_synthetic(
    label: str,
    database_url: str,
    public_url: str,
    secret: str,
) -> dict[str, object]:
    if label not in SYNTHETIC:
        raise ValueError("synthetic_label_invalid")
    await snapshot_command(label, database_url)
    live_snapshot = read_snapshot("live")
    connection = await asyncpg.connect(database_url)
    try:
        payload = await find_payload(connection, "live", live_snapshot)
    finally:
        await connection.close()
    if payload is None:
        raise RuntimeError("staging_evidence_update_missing")
    payload = {
        **payload,
        "received_at": datetime.now(UTC).isoformat(),
    }
    update_id, text = SYNTHETIC[label]
    await post_update(
        public_url,
        secret,
        build_update(payload, update_id=update_id, text=text),
    )
    return {"ok": True, "action": "inject", "label": label}


async def run_smoke(args: argparse.Namespace) -> dict[str, object]:
    if args.action == "scan-logs":
        return scan_log_lines(sys.stdin)
    database_url, public_url, secret = smoke_config(os.environ)
    if args.action == "snapshot":
        return await snapshot_command(args.label, database_url)
    if args.action == "verify":
        return await verify_command(args.label, database_url)
    if args.action == "replay-live":
        return await replay_live(database_url, public_url, secret)
    if args.action == "inject":
        return await inject_synthetic(args.label, database_url, public_url, secret)
    raise ValueError("smoke_action_invalid")
```

Extend `build_parser()` and `run()` exactly:

```python
    smoke = groups.add_parser("smoke")
    smoke_actions = smoke.add_subparsers(dest="action", required=True)
    snapshot = smoke_actions.add_parser("snapshot")
    snapshot.add_argument("--label", choices=("live",), required=True)
    verify = smoke_actions.add_parser("verify")
    verify.add_argument(
        "--label",
        choices=("live", "worker-restart", "redis-loss"),
        required=True,
    )
    smoke_actions.add_parser("replay-live")
    inject = smoke_actions.add_parser("inject")
    inject.add_argument(
        "--label", choices=("worker-restart", "redis-loss"), required=True
    )
    smoke_actions.add_parser("scan-logs")
```

```python
async def run(args: argparse.Namespace) -> dict[str, object]:
    if args.group == "webhook":
        if args.drop_pending_updates and args.action != "set":
            raise ValueError("drop_pending_only_valid_for_set")
        return await manage_webhook(
            args.action,
            WebhookConfig.from_env(os.environ),
            drop_pending_updates=args.drop_pending_updates,
        )
    if args.group == "smoke":
        return await run_smoke(args)
    raise ValueError("command_group_invalid")
```

Add to `project/docker-compose.staging.yml`:

```yaml
  staging-smoke:
    profiles: ["staging-tools"]
    image: "moroz-staging-bot:${STAGING_IMAGE_TAG:?set STAGING_IMAGE_TAG}"
    entrypoint: ["python", "/app/ops/staging.py", "smoke"]
    environment:
      DATABASE_URL: ${DATABASE_URL:-}
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB}
      STAGING_PUBLIC_URL: ${STAGING_PUBLIC_URL:?set STAGING_PUBLIC_URL}
      TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET:?set TELEGRAM_WEBHOOK_SECRET}
    volumes:
      - ./ops:/app/ops:ro
      - ../tmp:/state:rw
```

Extend contract tests: no `env_file`; exact six environment keys; no Telegram token, LLM key, Redis URL or Rabbit URL.

- [x] **Step 5: Run GREEN plus existing duplicate/Redis recovery regression**

```powershell
docker compose --env-file ../.env -p moroz-staging-task3-green --profile test run --rm test pytest tests/unit/test_staging.py tests/e2e/test_privacy_gate.py tests/e2e/test_message_delivery.py -q
```

Expected: PASS; existing duplicate pipeline remains one inbox/LLM/send, Redis fallback remains durable, new output tests reveal no sentinel.

- [x] **Step 6: Commit**

```bash
git add project/ops/staging.py project/docker-compose.staging.yml project/tests/unit/test_staging.py changelog.md
git commit -m "test: добавлены staging smoke и recovery evidence"
```

---

### Task 4: Exact staging runbook и documented-command contract

**Files:**
- Create: `project/ops/staging-runbook.md`
- Modify: `project/tests/unit/test_documented_compose_commands.py`
- Modify: `project/tests/unit/test_staging.py`

**Interfaces:**
- Runbook Compose prefix: `docker compose --env-file ../.env -p moroz-staging -f docker-compose.yml -f docker-compose.staging.yml`.
- Server checkout: `/opt/moroz-staging`; protected environment: `/opt/moroz-staging/.env`; root temporary state: `/opt/moroz-staging/tmp/`.

- [x] **Step 1: Написать RED documentation contract**

Append:

```python
def test_staging_runbook_uses_exact_project_and_never_destroys_data():
    text = (ROOT / "ops/staging-runbook.md").read_text(encoding="utf-8")
    prefix = (
        "docker compose --env-file ../.env -p moroz-staging "
        "-f docker-compose.yml -f docker-compose.staging.yml"
    )
    assert prefix in text
    assert "setWebhook" in text
    assert "getWebhookInfo" in text
    assert "deleteWebhook" in text
    assert "alembic upgrade head" in text
    assert "worker-restart" in text
    assert "redis-loss" in text
    assert "STAGING_PREVIOUS_IMAGE_TAG" in text
    for forbidden in (
        "alembic downgrade",
        "down --volumes",
        "docker volume rm",
        "drop_pending_updates=true",
    ):
        assert forbidden not in text
```

Modify `test_documented_compose_commands.py` so `DOCUMENTS` also includes `/repo/project/ops/staging-runbook.md`. Keep the canonical `--env-file ../.env` rule unchanged.

- [x] **Step 2: Run RED**

```powershell
docker compose --env-file ../.env -p moroz-staging-task4-red --profile test run --rm test pytest tests/unit/test_staging.py tests/unit/test_documented_compose_commands.py -q
```

Expected: FAIL because runbook is absent.

- [x] **Step 3: Write exact runbook**

Create `project/ops/staging-runbook.md` with these sections and commands:

1. Fail-closed prerequisites: dedicated bot ID/token, `STAGING_DOMAIN`, DNS, server access, staging LLM credential, Docker daemon, 2 GiB free, `/opt/moroz-staging` ownership.
2. Read-only inventory: `docker compose --env-file ../.env ls`, `docker ps`, `ss -ltn`, `getent ahostsv4`, resolved absolute staging path; never print environment.
3. Secret generation through server-side `openssl rand` redirected directly into protected `.env`; `chmod 600`; never `echo` values.
4. Config/build: set `STAGING_IMAGE_TAG` from `git rev-parse --short=12 HEAD`; run `config --quiet`, build bot/worker/migrate, inspect `.Config.User`, record image digests.
5. Start stores explicitly; migrate with profile `migration`; verify Alembic head without downgrade.
6. Ingress branch: own Caddy profile only when 80/443 are free; otherwise validate existing Caddy and add the candidate route below without stopping it. If the current ingress cannot consume this validated Caddy contract safely, stop with an ingress blocker.
7. Start `bot worker`; wait for health; test TLS, unrelated-path `404`, wrong-secret `403` using a nonsecret sentinel header.
8. Run `staging-webhook status`, `set`, `status`; default set command has no drop flag.
9. Consent in the dedicated test bot, then `staging-smoke snapshot --label live`; send exact canary; `verify --label live`; `replay-live`.
10. Worker recovery: stop only `moroz-staging-worker-1`, run `inject --label worker-restart`, start worker, verify.
11. Redis recovery: stop only `moroz-staging-redis-1`, inject `redis-loss`, start Redis, verify.
12. Safe logs: pipe `docker compose --env-file ../.env -p moroz-staging -f docker-compose.yml -f docker-compose.staging.yml logs --no-color --since=10m bot worker caddy` to `staging-smoke scan-logs`; do not display raw stream.
13. Rollback: export `STAGING_PREVIOUS_IMAGE_TAG`, run `up -d --no-build bot worker`, preserve stores, verify health/webhook; no migration command.
14. Safe stop: status pending count, webhook delete without drop, `stop --timeout 30 caddy bot worker rabbitmq redis postgres`; no `down --volumes`.
15. Evidence table containing commit/digests/head/health/safe counts/durations/blockers only.

Existing Caddy candidate route:

```caddy
{$STAGING_DOMAIN} {
	@telegram_webhook path /telegram/webhook

	handle @telegram_webhook {
		reverse_proxy 127.0.0.1:18081
	}

	handle {
		respond 404
	}
}
```

Every Compose command in the runbook must use the exact prefix and explicit service list. Commands that could reveal resolved environment (`config` without `--quiet`, `env`, `inspect` of container environment) are forbidden.

- [x] **Step 4: Run GREEN**

```powershell
docker compose --env-file ../.env -p moroz-staging-task4-green --profile test run --rm test pytest tests/unit/test_staging.py tests/unit/test_documented_compose_commands.py -q
```

Expected: PASS; canonical command scan finds no unsafe Compose invocation.

- [x] **Step 5: Commit**

```bash
git add project/ops/staging-runbook.md project/tests/unit/test_documented_compose_commands.py project/tests/unit/test_staging.py changelog.md
git commit -m "docs: добавлен staging runbook"
```

---

### Task 5: Local/fake staging completion gate

**Files:**
- Modify: `Дорожная карта.md`
- Modify: `План реализации.md`
- Modify: `changelog.md`

**Interfaces:**
- Produces evidence status `Local/fake staging ready` or exact blocker.
- Does not change `Live staging passed`.

- [x] **Step 1: Generate shell-only one-time credentials without output**

Run from `project/`; assignments do not print or persist values:

```powershell
$composeProject = "moroz-staging-local-$((Get-Date).ToUniversalTime().ToString('yyyyMMddHHmmss'))"
$suffix = [Guid]::NewGuid().ToString('N')
$env:POSTGRES_USER = "staging_$suffix"
$env:POSTGRES_PASSWORD = [Guid]::NewGuid().ToString('N')
$env:POSTGRES_DB = "staging_$suffix"
$env:DATABASE_URL = ""
$env:REDIS_PASSWORD = [Guid]::NewGuid().ToString('N')
$env:REDIS_URL = "redis://:$($env:REDIS_PASSWORD)@redis:6379/0"
$env:RABBITMQ_USER = "staging_$suffix"
$env:RABBITMQ_PASSWORD = [Guid]::NewGuid().ToString('N')
$env:RABBITMQ_URL = "amqp://$($env:RABBITMQ_USER):$($env:RABBITMQ_PASSWORD)@rabbitmq:5672/"
$env:TELEGRAM_BOT_TOKEN = "123456:$([Guid]::NewGuid().ToString('N'))abc"
$env:STAGING_TELEGRAM_BOT_ID = "123456"
$env:TELEGRAM_WEBHOOK_SECRET = [Guid]::NewGuid().ToString('N')
$env:LLM_API_KEY = "local-fake-$suffix"
$env:OPENAI_API_KEY = ""
$env:STAGING_IMAGE_TAG = "local-$suffix"
$env:STAGING_DOMAIN = "staging.example.test"
$env:STAGING_PUBLIC_URL = "https://staging.example.test"
```

Do not start bot during pytest and never save these values to Git or command output.

- [x] **Step 2: Run full Docker suite from a fresh test image**

```powershell
docker compose --env-file ../.env -p $composeProject --profile test build --no-cache test
docker compose --env-file ../.env -p $composeProject --profile test run --rm test pytest -q
```

Expected: all tests PASS. If Docker daemon is unavailable, record `Local/fake staging blocked: Docker daemon unavailable`; do not proceed to an implementation success claim.

- [x] **Step 3: Render staging Compose and validate Caddy**

```powershell
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml config --quiet
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml --profile staging-ingress run --rm --no-deps caddy caddy validate --config /etc/caddy/Caddyfile
```

Expected: both exit 0 without binding public ports.

- [x] **Step 4: Build immutable app images and inspect non-root users safely**

```powershell
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml build --no-cache bot worker migrate
docker image inspect "moroz-staging-bot:$env:STAGING_IMAGE_TAG" --format '{{.Config.User}}'
docker image inspect "moroz-staging-worker:$env:STAGING_IMAGE_TAG" --format '{{.Config.User}}'
docker image inspect "moroz-staging-migrate:$env:STAGING_IMAGE_TAG" --format '{{.Config.User}}'
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml --profile staging-ingress run --rm --no-deps --entrypoint id caddy -u
```

Expected: three app images print `appuser`, Caddy prints `10001`; commands inspect image config/runtime user only, never container environment.

- [x] **Step 5: Run fresh migration and runtime degradation checks**

Run:

```powershell
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml up -d postgres redis rabbitmq
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml --profile migration run --rm migrate
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml --profile migration run --rm migrate alembic -c /app/alembic.ini current
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml up -d worker
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml stop redis
docker inspect "$composeProject-worker-1" --format '{{.State.Health.Status}}'
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml start redis
$timer = [Diagnostics.Stopwatch]::StartNew()
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml stop --timeout 30 worker
$timer.Stop()
if ($timer.Elapsed.TotalSeconds -gt 30) { throw "Worker exceeded 30-second shutdown bound" }
docker compose --env-file ../.env -p $composeProject --profile test run --rm test pytest tests/e2e/test_privacy_gate.py tests/e2e/test_message_delivery.py -q
```

Expected: migration reaches head, worker remains `healthy` while Redis is stopped, Redis restarts, worker stops ≤30 seconds, duplicate/Redis-loss E2E tests PASS.

- [x] **Step 6: Safe scans and cleanup**

Run:

```powershell
git diff --check
$added = git diff --unified=0 --no-color | Where-Object { $_ -match '^\+(?!\+\+)' }
$secretCount = @($added | Where-Object {
    $_ -match 'bot\d+:[A-Za-z0-9_-]{20,}' -or
    $_ -match '(postgresql|redis|amqp)s?://[^\s@]+:[^\s@]+@'
}).Count
if ($secretCount -ne 0) { throw "Secret-shaped added lines: $secretCount" }
$safeLogResult = docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml logs --no-color --since=10m worker postgres redis rabbitmq |
    docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml --profile staging-tools run --rm -T --no-deps staging-smoke scan-logs
if (($safeLogResult | ConvertFrom-Json).ok -ne $true) { throw "Safe log scan failed" }
docker compose --env-file ../.env -p $composeProject -f docker-compose.yml -f docker-compose.staging.yml down --volumes --remove-orphans
docker image rm "$composeProject-test" "moroz-staging-bot:$env:STAGING_IMAGE_TAG" "moroz-staging-worker:$env:STAGING_IMAGE_TAG" "moroz-staging-migrate:$env:STAGING_IMAGE_TAG"
$containers = @(docker ps -aq --filter "label=com.docker.compose.project=$composeProject").Count
$volumes = @(docker volume ls -q --filter "label=com.docker.compose.project=$composeProject").Count
$networks = @(docker network ls -q --filter "label=com.docker.compose.project=$composeProject").Count
$images = @(docker image ls -q --filter "label=com.docker.compose.project=$composeProject").Count
if ("$containers/$volumes/$networks/$images" -ne "0/0/0/0") {
    throw "Task-specific cleanup incomplete"
}
```

No matching line, log body, credential or DSN is printed; only aggregate failure counts/status are allowed.

- [x] **Step 7: Record evidence and commit**

If every gate passed, mark only `Local/fake staging ready`; keep Phase 3 and live blockers open. If any gate is blocked, record exact blocker instead.

```bash
git add Дорожная\ карта.md План\ реализации.md changelog.md
git commit -m "test: подтвержден local staging gate"
```

---

### Task 6: Current VPS deployment и live staging gate

**Files:**
- Modify: `Дорожная карта.md`
- Modify: `План реализации.md`
- Modify: `changelog.md`

**Interfaces:**
- Consumes: completed Tasks 1–5, approved server access, staging DNS, dedicated test bot and live LLM credential.
- Produces: `Live staging passed` evidence or exact external blocker.

- [ ] **Step 1: Apply the deploy skill and perform read-only VPS preflight**

Before any server mutation read and follow the installed `deploy` skill. Resolve `/opt/moroz-staging`, inventory Compose projects/ports/ingress/DNS/disk, verify no target belongs to common or production. If server access or safe ingress path is missing, stop server mutation and record the exact blocker.

- [ ] **Step 2: Make the reviewed commit available on the VPS without unauthorized push**

Use an already authorized Git source or an explicitly approved artifact handoff. Do not push, merge or create a remote repository without separate user permission. Verify server checkout commit equals the reviewed local commit before build; otherwise stop with `server source commit unavailable`.

- [ ] **Step 3: Provision protected staging environment and isolated stores**

Follow the runbook: generate credentials directly on VPS, mode 600, render config quietly, build/tag images, start only `moroz-staging` stores, migrate to head, verify health. Never read or reuse common/production `.env`.

- [ ] **Step 4: Configure HTTPS ingress without stopping shared containers**

If 80/443 are free, start staging Caddy. If existing Caddy owns them, validate its current config, add only the staging-domain route to loopback 18081, validate candidate, atomic reload, and verify existing routes remain healthy. Unknown ingress or unsafe reload is a blocker.

- [ ] **Step 5: Set and verify Telegram webhook**

Run identity-gated `staging-webhook status`, then `set`, then `status`. Expect exact URL, pending count available, no current delivery error. Never print token or secret. Do not use the drop flag.

- [ ] **Step 6: Perform consented live smoke and duplicate replay**

Grant consent in the dedicated test bot, snapshot baseline, send `staging canary: проверка ответа`, verify `1 inbox / 1 token_usage / 1 sent outbound`, receive one Telegram answer, replay the same update internally and verify deltas remain `1/1/1`.

- [ ] **Step 7: Perform worker restart and Redis-loss drills**

Use the runbook's negative synthetic updates. Stop/restart only `moroz-staging` worker and Redis, verify health transitions, durable processing, exactly one additional LLM/outbound per scenario and no duplicate. Record worker shutdown duration ≤30 seconds.

- [ ] **Step 8: Rehearse app-only rollback**

Switch bot/worker to `STAGING_PREVIOUS_IMAGE_TAG` with `--no-build`, preserve DB/Redis/Rabbit volumes, do not run downgrade, verify webhook and health, then return to candidate images and verify again. If the previous app is schema-incompatible, record rollback blocked and use forward fix; never force destructive rollback.

- [ ] **Step 9: Run safe log/evidence checks**

Pipe logs through `staging-smoke scan-logs`; require zero secret-shaped and traceback lines. Record commit, image digests, Alembic head, health, webhook safe status, aggregate deltas, recovery durations and blockers only.

- [ ] **Step 10: Request whole-branch review and run verification-before-completion**

Use `requesting-code-review` against the Phase 3 merge base. Fix all Critical/Important findings via TDD and repeat review. Then use `verification-before-completion`: full Docker suite, Compose/Caddy validation, safe scans, live status and task-specific cleanup evidence.

- [ ] **Step 11: Close Phase 3 only with both evidence gates**

If Local/fake and Live gates both pass, mark Phase 3 complete and next checkpoint YCLIENTS. Otherwise keep Phase 3 open and list exact blockers.

```bash
git add Дорожная\ карта.md План\ реализации.md changelog.md
git commit -m "docs: зафиксирован live staging gate"
```

Never push or merge without a separate explicit user request.
