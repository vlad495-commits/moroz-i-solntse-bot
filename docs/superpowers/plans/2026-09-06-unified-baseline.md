# Единая исходная точка main / GitHub / staging

> **For agentic workers:** использовать Superpowers для интеграции кода; выполнять последовательно, с независимой проверкой совместимости веток. Не начинать заново выполненный аудит и пакеты A/B.

**Goal:** зафиксировать последнюю согласованную итерацию проекта в main, origin/main и staging, не потеряв историю и незавершённую работу.

**Architecture:** основа — reference-simplification d2699ca и согласованная архитектура 2026-09-06: свободный диалог, ручной промпт, технический YCLIENTS, без постоянного меню и редактора промпта. Staging 558b380 / UX-ветка 87405a3 содержат более ранний альтернативный интерфейс; переносить только совместимые исправления, не восстанавливать отменённые механизмы автоматически.

**Tech Stack:** Git, Python 3.12, Docker Compose, PostgreSQL, Redis, Telegram, YCLIENTS.

## Global Constraints

- Не терять пользовательские изменения, не применять reset --hard, force push и массовое удаление worktree.
- main изменять только после проверки интеграционного кандидата. Публикация GitHub и staging явно разрешена владельцем в текущем запросе.
- Не запускать живые Telegram/YCLIENTS-сценарии и платные evals; техническая проверка сборки/схемы/health обязательна.
- Секреты staging остаются только на сервере; не заменять server-only .env локальным.
- Сохранить историю обеих завершённых веток и явно описать заменённые более поздним решением механизмы.
- Не включать автоматически сторонние незавершённые инициативы, в частности ЮKassa.

## Задачи

- [x] Обновить remote refs через git fetch origin; сверить main, reference-simplification, telegram-ux-second, сервер и чистоту worktree. main 9e3c5d4; origin/main 74a1154; source d2699ca; UX 87405a3; staging 558b380.
- [x] Создать интеграционную ветку codex/unified-baseline-2026-09-06 в уже изолированном worktree, сохранив исходные refs. В трёх посторонних worktree найдены изменения: не трогать их.
- [ ] Сопоставить UX-изменения с новой архитектурой по клиентским сценариям. Зафиксировать переносы, уже покрытые случаи и осознанно заменённые механизмы.
- [ ] Собрать кандидат без отменённого меню/grounding; каждое разрешение code-конфликта проверить по контрактам новой архитектуры. Обновить roadmap и changelog.
- [ ] Проверить итоговый код и миграцию 0025 → 0026 в Docker, провести независимый review. Не выдавать предыдущие тесты за проверку нового кандидата.
- [ ] Сохранить исходные точки Git; fast-forward main до проверенного кандидата и обычным push опубликовать origin/main. Проверить exact SHA через git ls-remote.
- [ ] По staging-runbook сохранить rollback (код, env, образы, БД), собрать immutable образы, проверить совместимость миграции, обновить staging. Проверить exact image IDs, schema, health и HTTPS без живых диалогов.
- [ ] Подтвердить единую исходную точку и документировать оставшиеся архивные/незавершённые ветки. Чистить только доказанно объединённые ненужные refs, не удаляя активные worktree и уникальную работу.
