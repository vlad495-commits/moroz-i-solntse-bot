# Единая исходная точка main / GitHub / staging

> **For agentic workers:** использовать Superpowers для интеграции кода; выполнять последовательно, с независимой проверкой совместимости веток. Не начинать заново выполненный аудит и пакеты A/B.

**Goal:** зафиксировать последнюю согласованную итерацию проекта в main, origin/main и staging, не потеряв историю и незавершённую работу.

**Architecture:** основа — reference-simplification d2699ca и согласованная архитектура 2026-09-06: свободный диалог, ручной промпт, технический YCLIENTS, без постоянного меню и редактора промпта. Staging 558b380 / UX-ветка 87405a3 содержат более ранний альтернативный интерфейс; переносить только совместимые исправления, не восстанавливать отменённые механизмы автоматически.

**Tech Stack:** Git, Python 3.12, Docker Compose, PostgreSQL, Redis, Telegram, YCLIENTS.

## Global Constraints

- Не терять пользовательские изменения, не применять reset --hard, force push и массовое удаление worktree.
- main изменять только после проверки интеграционного кандидата. Публикация GitHub и staging явно разрешена владельцем в текущем запросе.
- Не запускать живые Telegram/YCLIENTS-сценарии и платные evals; техническая проверка сборки/схемы/health обязательна.
- Секреты staging остаются только на сервере; не заменять server-only .env локальным.
- Сохранить историю обеих завершённых веток и явно описать заменённые более поздним решением механизмы.
- Не включать автоматически сторонние незавершённые инициативы, в частности ЮKassa.

## Задачи

- [x] Обновить remote refs через git fetch origin; сверить main, reference-simplification, telegram-ux-second, сервер и чистоту worktree. main 9e3c5d4; origin/main 74a1154; source d2699ca; UX 87405a3; staging 558b380.
- [x] Создать интеграционную ветку codex/unified-baseline-2026-09-06 в уже изолированном worktree, сохранив исходные refs. В трёх посторонних worktree найдены изменения: не трогать их.
- [x] Сопоставить UX-изменения с новой архитектурой по клиентским сценариям. Независимая read-only сверка завершена; решения ниже. Оценка кода не является live-приёмкой.
- [x] Собрать кандидат без отменённого меню/grounding: совместимый UX ad1bd21 + review-fix 7608087, review PASS/Approved без замечаний. История UX присоединена явным merge strategy ours после выборочного переноса; runtime tree не изменился при merge. Обе исходные ветки являются предками кандидата, старые механизмы не восстановлены.
- [x] Проверить итоговый код в Docker и провести независимый review. Full c536c7e: 2392 passed / 9 устаревших ожиданий mixed FAQ; test-only fix 13bae0e: 91 passed (270.20s), включая все девять. Runtime неизменен; task/full/test-fix review без замечаний. Это full + post-fix evidence, не второй полный зелёный suite.
- [x] Проверить миграцию 0025 → 0026 и старый runtime на новой схеме в изолированном Docker compatibility-контуре перед переключением staging. Старые bot/worker healthy; расходный контур удалён, staging stores сохранены.
- [x] Сохранить исходные точки Git; fast-forward main до проверенного кандидата и обычным push опубликовать origin/main. Exact SHA cb8dfdb4e187a061ea9fd165593a6a2460daeead подтверждён ls-remote.
- [x] По staging-runbook сохранить rollback (код, env, образы, БД), собрать immutable образы, проверить совместимость миграции, обновить staging. Exact image IDs, schema0026, 8/8 healthy, HTTPS200/admin200/webhook403, webhook status0pending/noerror и safe logs подтверждены. Живые диалоги не запускались.
- [x] Подтвердить единую исходную точку и документировать оставшиеся архивные/незавершённые ветки. Удалены 15 полностью объединённых локальных refs без worktree, все коммиты сохранены; активные/грязные worktree и уникальная работа не удалялись. Итоговый документный снимок обозначается тегом baseline/2026-09-06; runtime tree неизменен, finalization сверяет точные SHA и image IDs.

## Результат сверки UX

| Механизм 87405a3 | Решение для последней архитектуры |
|---|---|
| Меню 2×3, категории, подбор по фиксированным целям, consultation grounding | Заменены явно согласованным свободным диалогом и ручным prompt; не возвращать |
| Ограничение времени, выбор единственного специалиста, walk-in, STOP, защита повторных/чужих действий | Клиентский результат уже реализован новым координатором; не копировать старый wizard |
| Назад по истории wizard и view revision | Не переносить старую модель; сохранить новый независимый draft и B2 pagination |
| Одна редактируемая карточка | Совместимая потребность, но нужен узкий перенос через privacy fence с обработкой contact keyboard и mixed FAQ; автоматический merge опасен |
| Названия кнопок и формат ссылок / отключение preview | Совместимые отдельные улучшения; переносить display без изменения service ID/matching |
| Кнопки сертификата | Условия и ссылка сохранены в prompt; отдельное необязательное UI-добавление |

Предварительный Git merge-tree выявил 17 конфликтных путей. Поэтому совместимый UX перенесён выборочно, а история старой ветки затем присоединена явным merge strategy ours (`5c87558`), без изменения runtime tree. Старые индексы callback actions не копировались: они различаются между ветками. Сохранена полная Git-история в ignored корневом `tmp/before-unified-baseline-2026-09-06.bundle`, bundle verify успешен.

## Граница продолжения

Владелец подтвердил последнюю версию со свободным диалогом и автоматические проверки: выполнять интеграцию, проверки и синхронизацию до результата без промежуточных согласований. Live Telegram/YCLIENTS и платные evals не запускать. Исходные refs и посторонние worktree сохраняются.
