# Telegram UX: второй пакет — design

Основание: согласованный запрос владельца и дополнение UX-аудита 2026-09-05 15:28. Реализация в codex/telegram-ux-second, отдельный существующий worktree.

Выбран минимальный путь: расширить существующий TelegramBookingCoordinator и display helpers. Альтернатива с редактированием сообщений требует durable Telegram message_id и изменения доставки; альтернативный LLM-only путь не гарантирует стабильный первый экран. Оба отложены.

Контракты:
- Одна услуга категории открывает карточку сразу; несколько услуг — заголовок и компактные кнопки, без повторного текстового прайса. До 10 категорий помещаются на одной странице, короткие подписи по две в ряд.
- Карточка: клиентское название, краткая суть из утверждённого system.md, актуальные цена и длительность. Записываемые: «Выбрать время», «Подробнее о процедуре». Walk-in: «Подробнее о процедуре», «Как нас найти», «← Длительности» на предыдущий экран вариантов.
- Водородотерапия: кнопки строго «30 минут», «60 минут». Нормализация регистра, пробелов, кавычек и Fresh применяется на отображении; YCLIENTS IDs и исходные данные сохраняются.
- Короткие вопросы продолжают выбранную услугу через существующее состояние каталога. Вопрос о времени каждой части комплекса не получает общий duration direct reply; неизвестная разбивка обозначается явно.
- «Подобрать процедуру»: детерминированный первый экран «Отдых», «Загар», «Уход», «Массаж». После цели — консультация по существующему LLM-контуру.
- Запись не предлагает walk-in. Сертификат получает URL-кнопки «Оформить сертификат» и «Уточнить у администратора» из утвержденных контактов.
- Повтор одного callback после перехода не создаёт ещё одну карточку; чужие/устаревшие callback не выполняют действие. Recovery остаётся fail-closed. Новых миграций и зависимостей нет.

Проверка: Docker RED→GREEN, независимый review, свежий объединённый gate, exact staging RC с code/env/database/image rollback, Telegram Web targeted acceptance, финальный server audit. Production, GitHub push и YCLIENTS mutations запрещены.
