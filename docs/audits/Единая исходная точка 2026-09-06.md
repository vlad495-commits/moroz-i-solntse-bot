# Единая исходная точка — evidence интеграции 2026-09-06

Текущий статус ведётся в `Дорожная карта.md`; этот файл фиксирует основания и доказательства синхронизации. План: [unified baseline](../superpowers/plans/2026-09-06-unified-baseline.md).

## Исходные точки

| Контур | До интеграции |
|---|---|
| Локальный main | `9e3c5d457e07df02bbc5b4dc320fe8e8aa45c50b` |
| GitHub main (проверено fetch/ls-remote) | `74a1154de5cb2428880284730062c555af4f2db3` |
| Последняя архитектура, reference-simplification | `d2699ca` |
| Старый Telegram UX (включая отчёт) | `87405a3` |
| Фактический staging runtime/checkout | `558b38025bb699923712ca89cf36eda95bc555cf` |
| Схема staging | `0025_telegram_booking_flow` |

Полная история всех refs до интеграции сохранена локально в ignored `tmp/before-unified-baseline-2026-09-06.bundle`, `git bundle verify` успешен. Незакоммиченные материалы не входят в bundle и не удаляются.

## Разрешение расхождения архитектур

Владелец явно подтвердил: последняя итерация — **свободный диалог**, а не меню 2×3 старого staging. Поэтому объединение истории не означает восстановление всех старых runtime-файлов.

- Меню, каталог консультаций, подбор по фиксированным целям и wizard history заменены новым согласованным контрактом.
- Ограничения времени, STOP, walk-in, ownership и безопасное повторное подтверждение уже покрыты новым координатором.
- Сохраняются совместимые UX-возможности: обновление одной inline-карточки внутри сценария, нормализация отображаемых названий, читаемые ссылки без preview.
- Contact keyboard, обычный текст, смешанная консультация и финальный результат операции не должны затираться редактированием карточки.
- Старые callback codes не копируются. Содержательная база — ручной system.md, без второго прайса и старых hardcoded описаний.

## Правила Git после синхронизации

- Единственная точка начала новой работы — актуальный `main`, синхронизированный с `origin/main`.
- Новая работа — отдельная `codex/*` ветка от актуального main; не продолжать старые альтернативные ветки как будто это текущая версия.
- Staging обновляется только зафиксированным проверенным кандидатом с immutable `rc-<full SHA>`; Git checkout и image IDs сверяются отдельно.
- Не переписывать историю опубликованных коммитов и не применять force push.
- Старые ветки без уникальной работы можно удалить через `git branch -d` после проверки ancestry и отсутствия привязанного worktree. Работающие worktree и ветки с уникальными коммитами не удаляются автоматически.

## Сохранённая сторонняя работа

В трёх посторонних worktree обнаружены локальные изменения: `1668` (changelog), `38be` (папка исходных обучающих документов), `85c6` (исходный документ принципов бота). Они не изменялись и не включаются в релиз автоматически. Ветка ЮKassa содержит отдельную предварительную инициативу и не подмешивается в эту синхронизацию.

## Проверки и выпуск

Автоматические проверки выполняются на изолированных Docker stores, без реальных LLM/YCLIENTS. Live Telegram, платные evals и реальные операции записи в этой задаче не запускаются. Fresh результаты интеграционного кандидата будут добавлены после завершения соответствующих запусков; прежние результаты не считаются проверкой нового кода.

- Совместимый UX: `ad1bd21`, baseline 58 passed; ожидаемый RED 9 failed / 5 passed; focused GREEN 87 passed (264.91s) + 23 passed (57.98s).
- Review-fix: `7608087`; воспроизведено стирание confirmation старым callback (RED 1 failed, 9.69s); после исправления 35 passed (95.41s). Recovery теперь отправляется отдельно, полная сводка сохраняется. Исправлен импорт test fixture без подавления Ruff-правил.
- Весь проект проходит Ruff E9/F с исключениями только F401/F841; syntax compileall и Compose config прошли.
- Полный свежий rebuilt Docker suite `c536c7e`: **2392 passed / 9 failed, 2279.90s**; JUnit `tmp/unified-full.xml`: 2401 tests, 9 failures, 0 errors, 0 skipped. Все девять падений — один параметрический контракт `test_catalog_message_flow.py:146`: кнопки совпадают, но тест ожидал `booking_card` у mixed FAQ. Снятие этой метки намеренно защищает консультацию от затирания. Отдельный изолированный RED подтвердил причину (1 failed, 13.93s).
- Runtime после полного прогона не меняется; исправляются только ожидания теста. Адресный post-fix gate и его результаты фиксируются отдельно, не выдаются за второй полный зелёный прогон.
- Test-only fix `13bae0e`: сохранено полное сравнение reply_markup, явно запрещены booking_card/edit_booking_card/edit_message_id у mixed outbound. Четыре связанных файла — **91 passed, 270.20s**; JUnit `tmp/unified-postfix.xml`: 91/0/0/0, все девять прежних failures покрыты. Ruff и diff-check зелёные. Task/full и отдельный test-contract reviews — PASS без открытых замечаний. Runtime после полного suite идентичен; остальные тесты уже прошли на этом runtime.
