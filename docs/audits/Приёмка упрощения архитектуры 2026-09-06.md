# Итог локального упрощения архитектуры

Дата: 2026-09-06. Ветка codex/reference-simplification, worktree .worktrees/reference-simplification. Проверенный код и тесты — c5ac62e; последующая коррекция только документная. Main 9e3c5d4 и сервер не менялись. Это локальная техническая приёмка, не rollout и не live-приёмка.

## Что завершено

- Пакет A: ручной промпт и основная LLM вместо консультационного каталога, без admin editor/hot reload; проверка минут и сохранение booking reply при answer outage.
- B0 ad2a4be: continue следует текущей операции, перенос сохраняет исходную запись и повторное подтверждение.
- B1 924e14d: убраны legacy service/actions; единый services, безопасный cancel_draft для нескольких услуг. Публичная schema/topics и immutable datasets сохранены.
- B2 c11cd83: неразрешённое предпочтение специалиста не исчезает после ввода даты/времени.
- B2 eae41c3: доступны четвёртая и последующие собственные записи через короткие страницы; глобальные indices, ownership, stale и confirmation guards сохранены.
- Задача 8: FAQ сохраняет уже показанную кнопку; постоянное меню не возвращается. Sync остаётся часовой, TTL 24 часа; причины и границы приведены в §4.4 исходного аудита.
- Сценарное обоснование оставшихся отличий от Lucky Hair — §4.5 [исходного аудита](<Аудит упрощения архитектуры по Lucky Hair 2026-09-06.md>). Topics пока остаётся диагностическим контрактом; замена boolean не объявляется необходимой для этого выпуска.

## Проверки

| Gate | Результат |
|---|---|
| B1 Router/security/Evaluation/E2E | 656 passed, 70.16s |
| B2 staff + booking | 118 passed, 99.69s |
| B2 pages + booking, включая 10 000/10 001 | 125 passed, 123.19s |
| FAQ → та же inline-кнопка | 2 passed, 10.38s |
| Полный rebuilt suite c5ac62e | **2379 passed / 1 failed**, 2236.23s |
| Весь unit suite после документной коррекции | **1315 passed**, 30.80s |

Количество разных запусков не суммируется. Единственное полное падение — test_official_compose_commands_use_approved_env_file: B2 план дублировал изолированную команду, разрешённую только в каноническом плане. Дубликат заменён ссылкой на каноническую команду; проверка безопасности не ослаблялась. Runtime и тесты после c5ac62e не менялись. Новый полный повтор после документной коррекции не запускался: основание приёмки — **full + post-fix unit**, а не заявление о зелёном полном повторе.

Команды из project/:

```powershell
docker compose --env-file ../tmp/audit-test.env -p moroz-reference-test -f docker-compose.yml -f docker-compose.audit-test.yml run --rm --build -v ../tmp:/reports -w /workspace test pytest --rootdir=/workspace -q -p no:cacheprovider /workspace/tests --tb=short --show-capture=no --junitxml=/reports/reference-final-full.xml
docker compose --env-file ../tmp/audit-test.env -p moroz-reference-test -f docker-compose.yml -f docker-compose.audit-test.yml run --rm -v ../tmp:/reports -w /workspace test pytest --rootdir=/workspace -q -p no:cacheprovider /workspace/tests/unit --tb=short --show-capture=no --junitxml=/reports/reference-final-unit.xml
```

JUnit в ignored root tmp/: reference-final-full.xml и reference-final-unit.xml. Full XML: 2380 tests, 1 failure, 0 errors/skips. Post-fix unit: 1315 tests, 0 failures/errors/skips. Test image пересобран с актуальными /app/src и /app/tests; запускаемые исходники/fixtures — /workspace текущего worktree. Docker инфраструктура moroz-reference-test изолирована от рабочих PostgreSQL/Redis/RabbitMQ. Реальные Telegram/YCLIENTS mutations и paid LLM не выполнялись.

Независимые reviews B1, staff, pages и whole-branch main..c5ac62e существенных actionable замечаний не выявили. Проверены реальные coordinator/repository/service с fake provider, а не только отдельный Router verdict. Это не оценка точности живой LLM и не подтверждение всех бизнес-фактов.

## Что осталось вне локального пакета

1. По отдельному разрешению — обновление staging с сохранением текущего промпта/rollback и ручная Telegram-приёмка точного кандидата. До этого сервер продолжает работать на прежнем runtime.
2. Проверка живого Router/LLM, FAQ+запись, перенос/отмена, страниц, специалиста и отказов. Внешние операции записи/платные вызовы требуют явного допуска.
3. Ответы Светы: актуальность, составы и условия программ/цен. Неподтверждённые факты не обещать; изменения вносить отдельными content-коммитами.
4. Merge/push/production rollout не выполнялись. Ветка и worktree сохранены; перед объединением повторно проверить main/fast-forward, не переписывать историю.
