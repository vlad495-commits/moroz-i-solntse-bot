# Production Telegram V1 Master Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Выпустить production-ready Telegram AI-оператора центра «Мороз и Солнце» с YCLIENTS, устойчивой обработкой сообщений, сервисными уведомлениями, защищённым LLM-контуром и операционной админкой.

**Architecture:** Модульный монолит в одном репозитории с общим Python-пакетом и четырьмя Docker-процессами: `bot`, `admin`, `worker`, `scheduler`. PostgreSQL хранит критическое состояние, Redis — временное, RabbitMQ — доставляет фоновые задачи, YCLIENTS остаётся источником правды для записи.

**Tech Stack:** Python 3.12, aiogram 3.x, FastAPI, Jinja2, asyncpg, Alembic, Redis 7, RabbitMQ, aio-pika, PostgreSQL 16, Docker Compose, pytest.

## Global Constraints

- Проект запускается и проверяется только через Docker Compose; прямой `python bot.py` запрещён.
- Первый клиентский канал — только Telegram.
- Голос, ЮKassa, массовые рассылки, реактивация, новые каналы и медицинские анкеты не реализуются в V1.
- До согласия текст пользователя не сохраняется и не передаётся внешней LLM.
- PostgreSQL — единственное хранилище критического состояния; Redis и RabbitMQ не заменяют его.
- YCLIENTS — источник актуальных слотов, записей и статусов.
- Изменяющие действия требуют явного подтверждения и повторной проверки внешнего состояния.
- Один диалог обрабатывается последовательно; разные диалоги могут идти параллельно.
- Ответ LLM исправляется не более одного раза; затем fallback или эскалация.
- Напоминания: сразу, за 24 часа, в 09:00, за 1 час, no-show в момент начала.
- Post-visit оценка и ссылка Яндекс Карт запрашиваются один раз за всю историю клиента.
- Production блокируется без YCLIENTS API, юридических текстов, ротации секретов, алертов и проверенного восстановления бэкапа.
- Runtime использует текущие aiogram/FastAPI/Jinja2/asyncpg; отдельный SPA и новый ORM не добавляются.
- Все миграции выполняются Alembic отдельным шагом; runtime не создаёт таблицы.
- Каждая задача плана идёт через red → green → полный Docker-check → отдельный коммит.

## Дисциплина реализации

- План исполняется через Subagent-Driven Development: свежий implementer на задачу, затем отдельное ревью соответствия ТЗ и качества кода.
- Каждый implementer сначала читает task brief, `AGENTS.md`, относящиеся к задаче разделы `ТЗ и архитектура.md` и фазового плана; решения не должны расходиться с этими документами.
- Superpowers задаёт обязательный процесс: worktree, TDD, проверка перед завершением и review checkpoints.
- Ponytail применяется в режиме `full`: переиспользовать текущий код и стандартные средства, не добавлять спекулятивные абстракции и зависимости, но не упрощать безопасность, обработку ошибок и проверяемость.
- Context7 обязателен перед использованием или изменением API сторонних библиотек и инфраструктурных инструментов; выбираются актуальные официальные контракты для закреплённых версий.
- Проект и тесты запускаются только через Docker Compose; локальный `.env` worktree не коммитится и не передаётся субагентам в отчётах.

## Плановая структура

```text
project/
├── src/moroz/
│   ├── common/          # config, db pool, logging, ids, clock
│   ├── messaging/       # incoming, inbox/outbox, buffer, Telegram pipeline
│   ├── booking/         # state machine и YCLIENTS adapter
│   ├── security/        # consent, PII, guardrails, validator
│   ├── notifications/   # scheduler jobs, reminders, feedback
│   └── escalation/      # human mode и служебные задачи
├── llm/                 # bot entrypoint и LLM provider adapters
├── admin/               # FastAPI/Jinja2 UI
├── worker/              # RabbitMQ consumer entrypoint
├── scheduler/           # due-job publisher entrypoint
├── migrations/          # Alembic revisions
├── tests/unit/
├── tests/integration/
├── tests/contract/
├── tests/e2e/
└── ops/                 # Caddy, backup, restore, runbooks, smoke/load
```

## Общие контракты

```python
@dataclass(frozen=True, slots=True)
class IncomingMessage:
    message_id: str
    channel: str
    chat_id: str
    user_id: str
    text: str
    received_at: datetime
    correlation_id: UUID

@dataclass(frozen=True, slots=True)
class ScenarioResult:
    status: Literal["ok", "needs_input", "escalated", "failed"]
    message: str
    next_action: str | None
    events: tuple[DomainEvent, ...]
    error_code: str | None = None

class BookingPort(Protocol):
    async def list_slots(self, query: SlotQuery) -> list[Slot]: ...
    async def create_booking(self, command: CreateBooking) -> ExternalBooking: ...
    async def reschedule_booking(self, command: RescheduleBooking) -> ExternalBooking: ...
    async def cancel_booking(self, command: CancelBooking) -> None: ...
    async def get_booking(self, external_id: str) -> ExternalBooking: ...

class QueuePort(Protocol):
    async def publish(self, task: QueueTask) -> None: ...

class LLMPort(Protocol):
    async def complete(self, request: LLMRequest) -> LLMResponse: ...
```

Имена и типы этих контрактов сохраняются во всех фазовых планах.

## Порядок исполнения

| № | План | Проверяемый результат | Зависит от |
|---|---|---|---|
| 1 | [Foundation](docs/superpowers/plans/2026-07-14-production-v1-foundation.md) | Общий пакет, Docker test service, Alembic, RabbitMQ, worker/scheduler skeleton | — |
| 2 | [Reliable Telegram Pipeline](docs/superpowers/plans/2026-07-14-production-v1-telegram-pipeline.md) | Webhook → privacy gate → buffer → inbox/outbox → queue → delivery | 1 |
| 3 | [Staging](docs/superpowers/plans/2026-07-18-staging.md) | Отдельный test token, HTTPS ingress, `setWebhook`, изолированный smoke и recovery-check | 1–2 |
| 4 | [YCLIENTS Booking](docs/superpowers/plans/2026-07-14-production-v1-yclients-booking.md) | Создание, перенос, отмена, конфликт и fallback через mock/real adapter | 1–3 |
| 5 | [LLM Security](docs/superpowers/plans/2026-07-14-production-v1-llm-security.md) | Маскирование, guardrails, primary/reserve, router, validator, evals | 1–3 |
| 6 | [Scheduler & Notifications](docs/superpowers/plans/2026-07-14-production-v1-notifications.md) | Напоминания, no-show, feedback once, эскалации | 1–5 |
| 7 | [Production Admin](docs/superpowers/plans/2026-07-14-production-v1-admin.md) | Роли, TOTP, диалоги, записи, знания, health и аудит | 1–6 |
| 8 | [Operations & Release](docs/superpowers/plans/2026-07-14-production-v1-operations.md) | TLS, алерты, backup/restore, load/failure tests, launch checklist | 1–7 |

### Статус фаз

- [x] Фаза 1 — Foundation: Tasks 1–6 завершены; независимый whole-branch review одобрил ветку без замечаний, свежий completion gate прошёл 122/122 Docker-теста. Следующая фаза — Reliable Telegram Pipeline.
- [x] Фаза 2 — Reliable Telegram Pipeline: Tasks 1–5 завершены; whole-branch fix-loop закрыл webhook secret, per-chat delivery order, Redis-loss startup/recovery, Telegram error classification, singleton ownership и bounded shutdown. Повторный review — `0 Critical / 0 Important / 0 Minor`, свежий controller no-cache gate — `195 passed`. Live Telegram E2E вынесен в следующий staging checkpoint.
- [x] Фаза 3 — Staging: Tasks 1–5 и все live smoke/recovery/safe-log/rollback gates подтверждены. Первый отличающийся YCLIENTS app release сохранил immutable previous, развернул distinct candidate и прошёл официальный app-only `candidate → previous → candidate` без DB downgrade; финально staging работает на candidate.
  - Exact plan: `docs/superpowers/plans/2026-07-23-yclients-staging-release.md`. Previous/candidate archives inspectable; verified bundle доставлен без push, allowlisted config `7/7`. Forward-only schema `0006`; bot/worker distinct true/true; previous и restored candidate дали smoke `1/1/1` и log scan `0/0/0/0`; stores/Caddy IDs/StartedAt неизменны. GET-only YCLIENTS readiness `1/1/312`, exact field match `1`, provider mutations не запускались.
  - Fresh completion verification: no-cache полный suite `472 passed in 332.93s`, skips `0`; task-prefixed standalone migration — `0006_yclients_booking_key (head)`; corrected compile gate прошёл; exact cleanup `0/0/0/0`. Финальный remote read-only check подтвердил exact candidate IDs, app `2/2`, staging `6/6`, schema `0006`, HTTPS `404/403/403`, safe log scan `0/0/0/0` и оба immutable архива. Self-review и independent review/fix-loop завершены без оставшихся findings.
- [x] Фаза 4 — YCLIENTS Booking: local/fake Tasks 1–5, PostgreSQL-owned redesign и real sandbox lifecycle завершены. Additive `0006_yclients_booking_key (head)`, полный Docker suite `472 passed` без skips и независимый review `0/0/0`; exact `custom_fields.moroz_booking_key`, protected CRUD, rate limit и no-blind-retry semantics подтверждены. Test-филиал настроен минимальными правами, pre-design active запись удалена отдельным exact cleanup. Финальный consented no-cache smoke с первой попытки прошёл services/staff/slots `1/1/312` → create/get/reschedule/get/cancel, reconciliation `matches=1`, `active_matches=0`, `success=true`, `manual_review_required=false`; task resources `0/0/0/0`.
- Ownership Task 4 завершил local/fake и live smoke: один generated UUID проходит create/get/reschedule/get/cancel, финальная read-only reconciliation считает только exact `custom_fields.moroz_booking_key` и выдаёт safe stage/count/boolean evidence. Live YCLIENTS evidence подтверждено без реальных ПД и без blind mutation retry.
- Review fix Task 4: присваивание `external_id` происходит только после trusted create-result validation; reconciliation ignores missing key field only, while malformed present `custom_fields` and exact-key missing/non-boolean `deleted` fail closed. Fresh Docker smoke + safe logging: `38 passed`.
  - Real adapter design зафиксирован в `docs/superpowers/specs/2026-07-22-yclients-real-adapter-design.md`; local/fake и sandbox evidence gates закрыты, `yclients-smoke` profile подтверждён real lifecycle.
- Ownership Task 5: финальный broad review fix-loop закрыл post-cancel manual-review gap, сузил empty-env exception до exact ownership plan и удалил ownership assertion из generic HTTP serializer. No-cache isolated Docker подтверждает `0006_yclients_booking_key (head)`, полный suite `472 passed` без skips и cleanup `0/0/0/0`. Сетевое field/config и отдельно согласованный pre-design cleanup подтверждены; финальный explicit lifecycle smoke прошёл `success=true`, `matches=1`, `active_matches=0`, без unknown/manual-review и без mutation retry.
- [ ] Фаза 5 — LLM Security: in progress; design checkpoint актуализирован под фактический worker/eval path без изменения согласованного продуктового scope.
  - Whole-review follow-up закрыл webhook `IngressDecision.action`; platform grammar дополнительно сужена до explicit field word или обязательного `:`/`—` shorthand. Последний RED `4/15`, focused GREEN `20 passed`, refreshed aggregate `386 passed`, static/migration gates зелёные, повторный независимый re-review `0/0/0`, Ready yes. Фаза остаётся in progress до родительского completion gate.
- [ ] Фазы 6–8 — ожидают своих зависимостей.

## Review checkpoints

- После плана 1: старый функционал запускается, schema создаётся только миграциями, тесты работают в Docker.
- После плана 2: один текстовый Telegram-запрос проходит устойчивый pipeline без YCLIENTS.
- После плана 3: live update отдельного test-бота проходит через HTTPS webhook, recovery и безопасную доставку в staging.
- После плана 4: mock YCLIENTS проходит E2E; реальный adapter включается только при доступе.
- После плана 5: критические security/eval кейсы проходят, fallback проверен.
- После плана 6: виртуальные часы подтверждают все напоминания и единственный feedback.
- После плана 7: роли и TOTP закрывают ПД, операционные экраны работают.
- После плана 8: backup восстановлен, нагрузка пройдена, launch gates подписаны.

## Общая проверка после каждого плана

```powershell
Set-Location project
docker compose --env-file ../.env --profile test build test
docker compose --env-file ../.env --profile test run --rm test pytest -q
docker compose --env-file ../.env config --quiet
docker compose --env-file ../.env build bot
docker compose --env-file ../.env run --rm --no-deps --entrypoint python bot -m compileall -q /app
docker compose --env-file ../.env run --rm --no-deps --entrypoint python bot -c "import cache, config, db, handlers, llm"
docker compose --env-file ../.env up -d --build postgres redis rabbitmq admin worker scheduler
docker compose --env-file ../.env ps
docker compose --env-file ../.env logs --since=5m admin worker scheduler
```

Ожидается: pytest завершился без FAIL, Compose-конфигурация валидна, обязательные контейнеры healthy/running, в свежих логах нет traceback.
