# Дорожная карта — Moroz i Solntse Bot

## Текущая ступень: 3 — Эвалы

### Правило ведения проекта
- [x] После каждого завершённого шага обновлять `Дорожная карта.md`.
- [x] После каждого значимого изменения добавлять запись в `changelog.md`.

### Опорная рамка целевой версии
- [x] Главный целевой документ с требованиями и встроенной ASCII-схемой: `ТЗ и архитектура.md`.
- [x] Актуальная схема встроена в раздел 16 файла `ТЗ и архитектура.md`; исходный Excalidraw перенесён в `tmp/` как временный материал.
- [x] Все дальнейшие продуктовые и архитектурные решения сверять с целевым ТЗ.
- [x] Бот и админка сейчас размещены на сервере как тестовый контур, не как финальный production.
- [x] Целевая система проектируется как AI-оператор на 4 канала: Telegram, WhatsApp, Instagram, ВКонтакте.
- [x] Первый production-релиз ограничен Telegram; остальные каналы подключаются после стабилизации первого контура.
- [x] Целевая архитектура включает YCLIENTS, ЮKassa, буфер сообщений, роутер намерений, сценарии, guardrails, маскирование ПД, scheduler, worker/очереди, follow-up, реактивацию, рассылки, мониторинг, эвалы и полноценную админку владельца.
- [x] Проведён отдельный раунд production-вопросов; подтверждённые решения внесены в `ТЗ и архитектура.md`, нерешённые пункты сохранены в разделах 17 и 18.

### Сделано
- [ ] Довести YCLIENTS до сквозного Telegram booking flow: гибридный structured LLM-router, durable кнопочный `BookingWorkflow`, каталог услуг/мастеров по allowlist, многосервисная запись, подтверждение, create/reschedule/cancel, human-mode и полный mock/read-only/sandbox test gate. Дизайн и TDD-план одобрены; реализация начата через subagent-driven development, исходный Docker gate подтверждён `932 passed, 1 deselected`.
  - [x] Task 1: добавлены контракты allowlisted catalog, строгий numeric/unique/non-empty parser, детерминированный mock с обязательной фильтрацией всех service/staff IDs по configured allowlists и worker-конфигурация безопасного booking rollout; внешний YCLIENTS/Telegram вызов не выполнялся.
  - [x] Task 2: добавлен read-only YCLIENTS catalog adapter для услуг и мастеров с GET-only fake transport contract, строгими envelope/type/error gates и полной фильтрацией service/staff IDs по allowlists; protected `ExternalBooking` snapshot теперь хранит exact `service_ids` и actual `staff_id` через real/mock/persistence/notifications cascade. Docker gate: `242 passed`; внешние запросы и booking mutations не выполнялись.
    - [x] Review fix: services envelope приведён к подтверждённому `data.services`; staff endpoint передаёт repeated `service_ids[]`, до I/O проверяет selected IDs/allowlist и строго разбирает `id/name/bookable`, присваивая мастеру exact selected tuple. TDD RED `8 failed / 14 passed`, catalog GREEN `22 passed`, полный affected gate `247 passed`.
  - [x] Task 3: booking callback и собственный Telegram contact нормализованы в существующий durable `message_inbox`/`process_message` path с `kind/data`, private-chat и processing-consent gates, update-id dedupe и безопасным display text; non-text interactions не попадают в Redis text buffer. Docker RED `5 failed / 21 passed`, GREEN ingress/privacy/messaging gate `44 passed`; внешние Telegram/YCLIENTS вызовы не выполнялись.
    - [x] Review fix: добавлены best-effort callback acknowledgment, server-receipt UTC clock и временный fail-closed worker guard до Task 7; focused gates `4 + 3 passed`, полный affected Docker gate `116 passed`. Telegram ack failure логирует только error type и не отменяет durable outcome; non-text payload не достигает LLM/history/outbound/status mutations.
    - [x] Review fix 2: bot rollout закрыт строгим `BOOKING_INTERACTIONS_ENABLED=false` до готовности Task 7 dispatcher; disabled callback получает unavailable reply и единый best-effort ack без consent/inbox/process_message/LLM, disabled contact не сохраняет PII, paused callback также подтверждается. Enabled test seam сохраняет Task 3 durable ingress и worker guard. Docker RED `10 failed / 27 passed / 11 errors`, focused GREEN `34 passed`, fresh affected gate `142 passed`; включать gate разрешено только одновременно с dispatcher в Task 7.
  - [x] Task 4: добавлена additive migration `0010_telegram_booking_flow` и durable `BookingWorkflowRepository`: legacy Telegram identity backfill, DB partial unique active scenario, optimistic revision locks, короткие opaque actions с owner/revision/expiry binding, atomic checkpoint+consumption+saved result, owned booking/human-mode reads и escalation resolution fields. TDD RED: missing module и migration `6 failed / 19 passed`; focused GREEN `33 passed`, self-review RED→GREEN `2 failed → 2 passed`, расширенный booking gate `279 passed`. Worker/webhook/workflow/YCLIENTS/Telegram не менялись, rollout gate остаётся false до Task 7.
  - [x] Task 5: добавлен строгий advisory LLM-router с exact enum JSON, duplicate/schema/type/range/NaN gates, порогом `0.80`, единым PII-маскированием current/bounded safe context и детерминированным safety precedence. `llm.route_intent` использует тот же `PrimaryReserveGateway`, что security pipeline, но dispatcher/workflow/Telegram/YCLIENTS не подключены; `BOOKING_INTERACTIONS_ENABLED` остаётся false до Task 7. TDD: initial missing-module RED, shared-gateway RED `2 failed / 34 passed`, focused GREEN `42 passed`, fresh affected gate `192 passed`, compile/config/diff exits `0/0/0`.
  - [x] Task 6: добавлены JSON-safe presenter и durable create-booking workflow с exact Telegram owner, explicit processing-consent contact, opaque actions, unlimited multi-service/page navigation, aware 14-day slots, factual staff, masked 30-minute confirmation и вызовом только существующего `BookingService`. Atomic `complete_action` сохраняет latest terminal state, action result/event и invalidating revision; duplicate/concurrent confirmation даёт один provider effect и durable replay. Docker RED подтверждён, fresh affected gate `348 passed`; rollout, worker/webhook/Compose и Task 7 не затрагивались.
    - [x] Review-fix: закрыты TTL-after-accept/crash replay, expired summary restart, slot-unavailable recovery и concurrent collecting TOCTOU через 5 faithful PostgreSQL + BookingService regressions. Focused gate `67 passed`, fresh affected booking gate `353 passed`; runtime/live/rollout не затрагивались.
    - [x] Re-review fix: concurrent same-confirm + slot-unavailable serialized race закрыт faithful PostgreSQL + real BookingService regression с одинаковым durable recovery replay. Exact GREEN `1 passed`, focused `70 passed`, fresh affected booking gate `356 passed`; обычный collecting и unrelated ValueError остаются fail-closed.
  - [x] Task 7: подключены deterministic `MessageDispatcher`, strict persisted interaction normalization, atomic history/outbox/inbox completion и общий fail-closed `BOOKING_INTERACTIONS_ENABLED=false` для bot+worker. Legacy text/consultant usage сохранены; structured payload безопасно завершается и очищается даже при выключенном флаге. Mock использует exact service selection и injected aware clock; real catalog/mutation adapters разделяют один HTTP boundary без вызова сети. TDD RED подтверждён, combined gate `84 passed`, Telegram/security/messaging regression `193 passed`, fresh affected booking gate `380 passed`; re-review `0 Critical / 0 Important / 0 Minor`, оба verdict `Ready`.
    - [x] Root-review fix: startup bot атомарно связан с `worker: service_healthy`, поэтому при общем включённом booking gate неготовый или fail-closed worker не позволяет bot принимать structured ingress. Compose-цикла нет; controlled true proof оставил bot `created`, legacy false proof поднял bot/worker `running+healthy` через прежнюю отдельную команду bot, live-вызовов не было. TDD RED `1 failed`, exact GREEN `1 passed`, focused `85 passed`, broader Task 7 `118 passed`; независимый re-review реализации `0 Critical / 0 Important`, оба verdict `Ready`.
  - [x] Task 8: реализованы protected owned list, whole-record reschedule/cancel, exact pre-display и pre-mutation GET, opaque index actions, full service preservation, separate old/new starts, factual staff, 3-hour boundary, partial-service escalation, replay/slot-race/outcome-unknown и exact dispatcher routes. Docker TDD: RED `11 + 12 + 8 + 1 + 1 failed`, fresh booking gate `413 passed`, worker/delivery gate `102 passed`, post-self-review gate `38 passed`; compile/Compose/diff/PII/scope gates clean, live-вызовов не было.
    - [x] Review fix 7: bounded connector/polite grammar распознаёт `и` / `и пожалуйста` / `а` / `а также`, а явные родительные generic-формы `услуги/процедуры` остаются complement при отдельной цели времени/мастера; coordinated service target сохраняет приоритет. Exact RED `12 failed / 34 passed`, exact GREEN `46 passed`, полная detector-матрица `74 passed`, affected workflow/PostgreSQL/Telegram gate `133 passed`, fresh aggregate booking gate `484 passed`; live-вызовов не было.
    - [x] Review fix 6: coordinated single-action targets через `и` отделены от bounded complement `для услуги`; exact RED `6 failed`, GREEN `6 passed`, накопленная detector-матрица `62 passed`, affected workflow/PostgreSQL/Telegram gate `121 passed`, fresh aggregate booking gate `472 passed`.
    - [x] Re-review fix 5: явная noun-aware дата/время/мастер теперь имеет приоритет над generic «услуга/процедура» с любой стороны action, не подавляя selected-service positives. Exact RED `6 failed / 22 passed`, exact GREEN `28 passed`; self-review multi-action RED→GREEN `1 failed / 7 passed → 8 passed`; финальный detector matrix `56 passed`, affected unit/PostgreSQL/Telegram E2E `114 passed`, свежий aggregate booking на final source `466 passed`; live-вызовов не было.
    - [x] Re-review fix 4: time-target detector ограничен noun-aware формами `время/времени/временем`, поэтому наречие «временно» больше не скрывает preceding generic или selected service в active reschedule/cancel. Exact RED `4 failed`, exact GREEN `4 passed`, final affected `106 passed`, fresh aggregate booking `459 passed`; compile/Compose/diff/PII/scope gates clean, live-вызовов не было.
    - [x] Re-review fix 3: preceding fallback теперь отдаёт приоритет явной дате/времени/мастеру над generic «услуга/процедура» в reverse-order фразах active reschedule/cancel, сохраняя прежние positives и forward negatives. Exact RED `6 failed / 14 passed`, exact GREEN `20 passed`, workflow unit `43 passed`, affected workflow/change E2E `100 passed`, aggregate booking `453 passed`; compile/Compose/diff/PII/scope gates clean, live-вызовов не было.
    - [x] Review-fix: persisted change-state связан с protected snapshot на mutation boundary, bounded Russian recognizer покрывает reschedule/cancel без захвата date/staff сообщений, exact status требует confirmed с обеих сторон. Отдельные RED: `6 failed`, `6 failed / 4 passed`, `1 failed`, cancel-flow `1 failed`; точечные GREEN: `6 + 10 + 1 + 1 passed`, final booking gate `432 passed`; live-вызовов не было.
    - [x] Re-review fix 2: partial-service recognizer связан с первой целью после action и не эскалирует date/time/staff запросы из-за упоминания услуги; invalid-type `old_scheduled_end_at` fail-closed отклоняется до slot query/provider mutation. Exact RED `10 failed`, exact GREEN `10 passed`, self-review RED `1 failed / 6 passed`, final focused `41 passed`, directly affected `94 passed`, aggregate booking `438 passed`; compile/Compose/diff/PII/scope gates clean, live-вызовов не было.
  - [x] Task 9: реализованы атомарная booking escalation, owner-bound durable human mode, нейтральный клиентский и reason-only staff outbox, а также owner/manager-only admin list/reply/resolve с CSRF, durable actor, аудитом в той же транзакции и fail-closed изоляцией владельца. Docker TDD: initial RED `4 failed` + missing route, startup/actor/upsert/terminal-transition RED по `1 failed`; focused final `68 passed`, exact-final broader booking/worker/delivery/admin/messaging gate `481 passed in 403.12s`. Compileall/Compose/diff/line/PII/scope gates clean; новая migration не понадобилась, live Telegram/LLM/YCLIENTS вызовов не было.
  - [x] Task 10: добавлен отдельный GET-only `BookingLookupPort` и атомарный `BookingReconciler` для unresolved `booking_outcome_unknown`: закрытие допускается только при одном exact owner/key/slot/service multiset/staff/start/end/status match, а zero/multiple/malformed/error/mismatch сохраняют escalation и human mode. Kind-specific create/reschedule/cancel, mock replay/race и PostgreSQL concurrency/replay/rollback покрыты; exact-final Docker booking gate `520 passed`, affected worker/delivery/admin gate `104 passed`. Live Telegram/LLM/YCLIENTS и reconciliation mutations не выполнялись.
    - [x] Independent-review fix: reconciliation использует scenario UUID только для create, а reschedule/cancel — validated original `state.booking_key`; malformed/sentinel key отклоняется до lookup. Real bounded GET учитывает valid wrong-owner exact matches и fail-closed останавливается на exact-key записи без безопасной owner binding/snapshot, включая whitespace-only owner. Confirmed replay требует атомарно записанный exact `booking_reconciled` marker; полные kind-specific mismatch/zero/multiple/malformed/temporary матрицы сохраняют escalation/human mode. Docker RED `12 failed / 140 passed` + marker `2 failed` + whitespace owner `1 failed`, exact-final focused GREEN `153 passed`, fresh booking gate `557 passed`, affected worker/admin/delivery/escalation `157 passed`; live/provider mutations не выполнялись.
  - [x] Task 11: локальная часть и внешний read-only YCLIENTS gate завершены. Отдельная GET-only команда проверяет exact 14-day окно, allowlist completeness/duplicate/envelope fail-closed границы и выдаёт только санитизированный JSON; real worker выполняет тот же preflight до readiness, mock/disabled network-free; Compose профиль не получает user/Telegram/LLM/DB/queue secrets. После восстановления server-owned credentials в ignored локальный `.env`, автоматического выбора одного bookable мастера и двух TDD contract-fix внешний Docker run завершился `ok=true`: `1` услуга, `1` мастер, `336` слотов. Sandbox mutations не выполнялись.
  - [x] Task 12 local safety hardening: `yclients-smoke` принимает только exact consent `I_UNDERSTAND_THIS_CREATES_TEST_BOOKINGS` и marker `sandbox` без whitespace, synthetic test name и ASCII-телефон `+7000` с полной длиной, а окно слотов ограничено `YCLIENTS_TEST_WINDOW_DAYS=1..14` и начинается в текущий момент, поэтому 1-day query непустой. Неизвестный mutation outcome прекращает дальнейшие mutations и даёт ровно одну GET-only reconciliation; definite post-create cleanup ограничен одной exact-key/external-id cancel с последующей reconciliation. Protected cancel до DELETE сверяет exact external ID/customer/key. Fresh Docker affected gate: `167 passed in 59.30s`.
    - [x] Live-permission safety fix: перед первым POST runner выполняет bounded GET reconciliation по новому UUID и требует строго `matches=0`, `active_matches=0`. Ошибка авторизации/транспорта/формы ответа или неожиданное совпадение останавливают lifecycle до mutation. Fresh Docker TDD: RED `16 failed / 29 passed`, GREEN `45 passed`; обнаруженный provider `403` теперь не может оставить запись без доступного bounded cleanup.
    - [x] Единственная разрешённая external attempt завершилась fail-closed до mutation: services/staff/slots `1/1/336`, затем records preflight получил definite provider failure (`HTTP 403` по отдельной GET-only диагностике); create/get/reschedule/cancel `not_started`, manual review не требуется, созданных записей нет.
    - [x] Independent-review fix: canonical Compose/migration contract и host regression теперь знают exact `yclients-readonly` env allowlist отдельно от mutation smoke; explicit staff availability принимает requested allowlisted staff только при ровно одном bookable match до time fan-out; GET захардкожен на transport boundary без generic method parameter; test-plan использует формулировку `local non-provider sentinel` и точное финальное evidence. Docker RED `3 failed / 118 passed` + hardcoded GET `1 failed`, focused GREEN `133 passed`, exact-final affected GREEN `216 passed`; внешний YCLIENTS GET по-прежнему **NOT RUN**.
    - [x] Second review fix: explicit staff availability считает raw occurrences requested ID до `bookable`-фильтрации, требует ровно один элемент и actual boolean `true`; duplicate `true+false`/missing/non-bool и single missing/false/non-bool fail-closed до `/book_times`. Exact Docker RED `3 failed / 5 passed`, focused GREEN `8 passed`, affected GREEN `222 passed`; внешний YCLIENTS GET по-прежнему **NOT RUN**.
    - [x] External config preparation: server-owned `YCLIENTS_PARTNER_TOKEN`, company и test service безопасно перенесены в ignored локальный `.env` без вывода значений; `User Token` не копировался. Один bookable staff с доступными слотами выбран GET-only discovery, заполнены exact service/staff allowlists и `sandbox` label. Секреты не добавлялись в Git/чат, mutation-запросы не выполнялись.
    - [x] Staging credentials presence audit: ограниченная SSH-проверка `/opt/moroz-staging/.env` без вывода значений подтвердила mode `600`, заполненные `YCLIENTS_PARTNER_TOKEN`, `YCLIENTS_USER_TOKEN`, `YCLIENTS_COMPANY_ID` и `YCLIENTS_TEST_SERVICE_ID`. Новые `YCLIENTS_SERVICE_ALLOWLIST`, `YCLIENTS_STAFF_ALLOWLIST` и `YCLIENTS_ENVIRONMENT_LABEL` отсутствуют; локальный перенос секретов и внешний YCLIENTS GET не выполнялись.
    - [x] Contract fix: подтверждённый live GET shape `/api/v1/book_services/{company_id}` использует integer `seance_length`, а не `duration`; parser и catalog/read-only fixtures приведены к этому ключу, сохранив strict positive whole-minute seconds и fail-closed malformed behavior. Docker TDD RED `1 failed / 22 deselected`, focused GREEN `1 passed / 22 deselected`, affected catalog/read-only GREEN `43 passed in 0.40s`; внешний YCLIENTS GET, Telegram/LLM и sandbox mutations не запускались.
    - [x] Contract fix 2: подтверждённый allowlisted service item того же live GET содержит `seance_length: null`, а staff item duration не содержит. `CatalogService.duration_minutes` теперь `int | None`; catalog parser принимает только `null` либо прежнее строгое positive целое число секунд, кратное минуте, и fail-closed отклоняет float/string/bool/invalid values. Workflow не сохраняет и не обещает catalog duration при выборе услуги; после выбора слота сохраняется только factual `slot.duration_minutes`. Docker TDD RED `2 failed / 1 passed`, focused GREEN `33 passed`, final affected catalog/read-only/workflow/availability GREEN `178 passed in 58.92s`; внешний YCLIENTS GET, Telegram/LLM, sandbox mutations, `.env`, `tmp/` и server не затрагивались.
    - [x] Independent-review fix: absence ключа `seance_length` больше не считается допустимым `null` — service item fail-closed отклоняется до построения catalog model; явный JSON `null` и прежняя strict positive whole-minute integer проверка сохранены. Docker TDD RED `1 failed / 27 passed`, focused catalog GREEN `28 passed`, affected catalog/read-only/workflow/availability GREEN `179 passed in 59.11s`; внешний YCLIENTS GET, Telegram/LLM, sandbox mutations, `.env`, `tmp/` и server не затрагивались.
    - [x] External GET evidence: штатный `yclients-readonly` profile после fresh build завершился exit `0` и санитизированным `ok=true`, environment `sandbox`, horizon `14`, service/staff counts `1/1`, availability total `336`. Перед ним локальный fake contract gate прошёл `42 passed`; независимые reviews обоих live-shape fix дали `0 Critical / 0 Important / 0 Minor`. POST/PUT/PATCH/DELETE, User Token и private fields отсутствовали.
  - [ ] Task 12 external lifecycle completion: обновить/перевыдать `YCLIENTS_USER_TOKEN` с правом чтения и изменения записей в test-филиале, повторить GET-only records preflight и получить новое отдельное разрешение пользователя на sandbox mutation attempt. Текущую попытку не повторять вслепую.
    - [ ] Пользователь передал и явно попросил записать новый token; ignored `.env` обновлён без вывода значения, однако две bounded GET-only проверки `/records` снова получили HTTP `403`. Token непустой и отличается от Partner Token. Следующий шаг находится в YCLIENTS: восстановить лицензию test-филиала, проверить подключение приложения и права просмотра/изменения записей, затем перевыпустить уже не опубликованный в чате User Token.
  - [x] Task 13 local post-booking gate: новый Telegram mock/PostgreSQL E2E доказал reminder jobs только после confirmed owned booking, атомарную замену при reschedule, invalidation при cancel, отсутствие jobs для foreign/outcome-unknown, replay-дедупликацию и один owner-bound outbound при повторной scheduler delivery. Exact gate `4 passed in 13.68s`, full notification regression `49 passed in 39.65s`. Production-код уже соответствовал контракту и не переписывался; scheduler остаётся выключенным в staging до успешного external lifecycle и отдельного разрешения.
  - [x] Task 14 full local production-candidate verification: static/Compose `0/0`, focused booking+notifications `645 passed in 486.03s`, fresh full Docker suite `1347 passed in 709.54s`; предыдущие 12 documentation-only failures устранены корректным read-only `/docs` mount и canonical `../.env`, exact rerun `12 passed`. Worker/scheduler build и compileall прошли `0/0`, Alembic head — `0010_telegram_booking_flow`. Индекс node IDs и санитизированных DB assertions записан в тест-план. Внешние mutations не выполнялись; live production и staging scheduler остаются заблокированы external YCLIENTS records `403`.
  - [ ] Подготовить новый пустой YCLIENTS-аккаунт как изолированный sandbox: один тестовый филиал, услуга, сотрудник и расписание; затем GET-only readiness и только по новому отдельному разрешению один bounded lifecycle с cleanup/reconciliation. Дизайн пользователем утверждён и зафиксирован в `docs/superpowers/specs/2026-08-04-new-yclients-sandbox-account-design.md`; реальные клиенты и ПД исключены.
    - [x] Подробный TDD/operations implementation plan подготовлен: отдельный mutation-free records preflight, browser-настройка минимального каталога, secret-safe `.env`, два внешних GET-only gate и жёсткий permission stop перед lifecycle.
  - [x] Task 9 review-fix: `failed`/`confirmed`/`escalated` сохраняются без side effects, неизвестная persisted phase fail-closed отклоняется, а обе публичные repository/session границы до acquire/transaction принимают только пять production booking reason codes. Exact Docker RED `9 failed / 6 passed`, GREEN `15 passed`, focused escalation/admin/worker/delivery gate `117 passed`, fresh broader gate `496 passed in 399.78s`; compileall/Compose/diff/PII/scope gates clean, live-вызовов не было.
- [x] Ускорение буфера применено на staging-сервере как минимальный hotfix поверх текущего dirty detached checkout: перед правкой сохранён backup `/opt/moroz-staging/backups/buffer-hotfix-20260731T112406Z`, пересобраны и перезапущены `moroz-staging-bot-1` и `moroz-staging-worker-1`. Серверная проверка подтвердила `BUFFER_SECONDS = 2`, наличие `send_chat_action`, `bot/worker healthy`, `/healthz 200`, свежий log scan без ошибок.
- [x] Ускорен Telegram message buffer: quiet-window сокращён с 5 до 2 секунд, а webhook для нового consented text отправляет Telegram `typing`, чтобы пользователь видел быстрый отклик до LLM-ответа. Добавлены regression-тесты на двухсекундный flush и ранний typing; связанный Docker gate прошёл `29 passed`.
- [x] Перенесён клиентский consent-flow по образцу Володи/Lucky Hair Studio: до обработки текста бот показывает запрос согласий с кликабельной политикой и inline-меню `☐/☑ Согласен с политикой`, `☐/☑ Согласен на рассылку`, `Готово`; без обязательной галки политики продолжить нельзя, после галки сохраняется processing consent. Staging обновлён на tag `consent-20260731-1300`, privacy-gate regression прошёл `20 passed`; 2026-07-31 после ручной проверки исправлен env-override текстов, новая кнопка отправлена в Telegram.
- [x] Создан краткий `План ручного тестирования.md` для проверки staging-бота в Telegram со сверкой диалогов, статистики и ошибок в админке; реальные YCLIENTS-операции, scheduler, уведомления и дополнительные каналы исключены из scope.
- [x] Создан локальный скилл `manual-qa-bot-testing` для повторяемого ручного QA staging-бота: риск-ориентированный прогон Telegram/Webhook/Admin, отчёт `Отчет по тестированию бота.md`, финальная сверка логов/паузы и reuse baseline первого прогона без лишнего повтора уже закрытых OK-сценариев.
- [x] Устранена путаница серверных контуров: старый prototype checkout `/opt/moroz-i-solntse-bot` заархивирован в `/root/moroz-server-archive/` и удалён из активного `/opt`; единственный серверный Compose project — `moroz-staging`, локальный `.env` теперь указывает на staging-бота и `/opt/moroz-staging`.
- [x] Архитектура полностью перенесена в раздел 16 файла `ТЗ и архитектура.md` как ASCII-схема с блоками, связями, развилками и последовательностями.
- [x] Ступень 1: рабочий Telegram LLM-прототип.
- [x] Системный промпт v1 подготовлен, применён и протестирован в Telegram.
- [x] Системный промпт v1.1 локально обновлён: уточнены стиль ответов, защита от раскрытия инструкций, сценарии записи и правила эскалации человеку.
- [x] Системный промпт v1.2 локально обновлён: усилены алгоритм ответа, антигаллюцинации по записи/слотам, чувствительные медицинские сценарии и передача администратору.
- [x] Создан черновой файл базы знаний `project/data/services_prices.md` с текущими ценами из промпта и списком данных, которые нужно запросить у клиента.
- [x] Первый серверный деплой выполнен.
- [x] Ступень 2: FastAPI-админка добавлена в проект.
- [x] Админка задеплоена на сервер и доступна на `http://109.71.246.167:8080/login`.
- [x] Добавлены разделы админки: диалоги, статистика, редактор промпта, управление ботом, логи.
- [x] Добавлены таблицы `token_usage` и `prompt_versions`.
- [x] Добавлен hot-reload промпта через Redis.
- [x] Тумблер паузы бота подключён через Redis-флаг `bot:paused`.
- [x] Ступень 3: добавлена инфраструктура эвалов в админке (`/eval/`), таблицы eval_cases/eval_runs/eval_results, eval-runner, SSE-прогресс и локальный скилл `evals`.
- [x] Добавлена отдельная площадка `/review/evals` для клиентского ревью eval-кейсов: статусы, комментарии, предложенные эталонные ответы и новые кейсы сохраняются отдельно от боевого набора.
- [x] Интегрирован клиентский документ с комментариями по боту от 2026-07-06: обновлены правила общения, цены на загар/депозит, различия коллариума и коллагенария, блоки по водородотерапии и криомассажу, добавлены новые eval-кейсы.
- [x] Собран единый целевой документ `ТЗ и архитектура.md` со встроенной полной ASCII-схемой по методике уточняющих вопросов из урока.
- [x] Разобраны урок 9 и авторская схема Lucky Hair Studio; в целевое ТЗ и архитектуру добавлены privacy gate, scripts-first проверки, compact истории, ограниченный output-retry, безопасное восстановление ПД, событийный журнал, post-visit feedback и защита реактивации от дублей.
- [x] По двум раундам уточнений закреплены правила идентификации клиента, конфликтов YCLIENTS, human mode, ролей, согласий, деградации компонентов, идемпотентности, нагрузки, поддержки, бэкапов и production-приёмки.
- [x] Закреплена эксплуатационная модель: Влад — технический владелец; разработка проходит локальный, тестовый и клиентский production-контуры; определены правила релизов, очереди, rate limit, бюджетных порогов, логов, инцидентов и проверок восстановления.
- [x] Интегрированы новые клиентские файлы `CHATBOT_KNOWLEDGE_BASE_NO_FINAL_QUESTIONS.md` и `CHATBOT_QUICK_ANSWERS.md`: промпт обновлен до v1.5, база цен дополнена программами/сертификатами, eval-набор расширен до 53 кейсов.
- [x] Ответы собственницы по продажам и post-visit учтены в ТЗ: апселл ведёт к связанной программе/абонементу, а оценка процедуры и сервиса со ссылкой на Яндекс Карты запрашивается один раз за всю историю клиента.
- [x] Зафиксирован полный график сервисных уведомлений: сразу после записи, за день, утром в день визита, за час и при статусе YCLIENTS `не пришел` в момент начала — клиенту и сотруднику.
- [x] Согласован полный production-дизайн первого Telegram-релиза: архитектура, поток сообщения, сценарии, хранилища, админка, безопасность, тестирование, эксплуатация и launch gates; создана отдельная письменная spec.
- [ ] Создать самостоятельную интерактивную HTML-карту завершённой фазы Foundation для технического владельца; письменная spec одобрена, implementation plan подготовлен.
- [x] Создана подробная статичная HTML-схема всей реализованной Production Telegram V1 в стиле последней Foundation workflow-карты: четыре читаемых сверху вниз полотна показывают каждый runtime-узел, путь сообщения, YCLIENTS booking, scheduler/уведомления, админку/эксплуатацию, общий фундамент и честную post-launch границу без кликабельных блоков. Постоянный Docker contract прошёл `5 passed`; wide 1440 px и narrow 390 px browser QA подтвердили отсутствие наложений и горизонтального переполнения, preview log содержал только GET самого HTML, task cleanup `0/0/0/0`.
- [x] Углублена HTML-архитектура по обратной связи технического владельца: RabbitMQ, PostgreSQL и Redis показаны внутри общей системной границы как самостоятельные подсистемы. RabbitMQ раскрыт до `tasks exchange` → `tasks queue` → consumer contract → retry → DLX/DLQ; PostgreSQL — до групп реальных таблиц; Redis — до buffer/context/runtime-control/alert keys. Docker contract прошёл `6 passed`, wide 1440 px и narrow 390 px browser QA подтвердили читаемость без наложений.
- [x] Создана отдельная статичная HTML-доска пути одного Telegram-сообщения в стиле референса технического владельца: ingress и PostgreSQL-дедупликация, Redis buffer и recovery fallback, transactional outbox, RabbitMQ, внутренности worker, повторная доставка, retry/DLQ и ordered delivery ответа. Docker contract прошёл `5 passed`; wide 1440 px и narrow 390 px browser QA подтвердили читаемость без наложений и горизонтального переполнения.
- [ ] Выпустить Production Telegram V1 по восьми фазам: локальная реализация и readiness audit фаз 1–8 завершены, но production launch заблокирован неподписанными реальными gates; код Phase 7–8 находится в локальной ветке `codex/phase7-8-readiness`, без merge/push.
  - [x] Foundation Task 1: добавлены общий Python-пакет и профильный Docker Compose test service.
  - [x] Foundation Task 2 / baseline: Alembic создаёт исходную схему только отдельной миграцией; destructive downgrade запрещён fail-closed.
  - [x] Foundation Task 2 / cutover: существующая схема проходит полный catalog audit до stamp, включая повторную проверку уже stamped базы.
  - [x] Foundation Task 3: общий pool/observability и review fixes готовы — lifecycle/reconnect сериализованы, bot/admin sizing восстановлен, concurrency и real PostgreSQL tests прошли.
  - [x] Foundation Task 4: RabbitMQ, fail-closed worker и scheduler готовы — manual ack, publisher confirms, три retry, DLQ на 30 дней, supervision consumer и graceful shutdown подтверждены независимым review.
  - [x] Foundation Task 5: regression gate и review fixes завершены — 55 Docker-тестов, canonical `--env-file`, минимальные profile env allowlists с обратимо совместимым fallback по PostgreSQL parts при пустом `DATABASE_URL`, shared immutable non-root migration image, точная cleanup error precedence, bot smoke без polling, health/log/image/scans и isolated cleanup подтверждены.
  - [x] Foundation Task 6: downgrade/full-catalog audit, rendered least-privilege env allowlists, PostgreSQL DSN encoding, actual Rabbit callback supervision, bounded broker/inflight cancel, readiness-before-I/O, backoff/health, закрытие Redis clients, безопасные global runtime/eval logs и owned background tasks прошли safe Docker gate; Foundation eval CLI импортируется без будущего guardrails-модуля, fail-closed завершает data/init errors и отличает отсутствующую optional feature от любого import-time `Exception` сломанного установленного guardrails, не поглощая `BaseException`.
  - [x] Фаза Foundation завершена: Tasks 1–6, lifecycle/privacy fix-loop, независимый whole-branch review без замечаний и свежий no-cache completion gate `122 passed` подтверждены; следующий checkpoint — Reliable Telegram Pipeline.
  - [x] Reliable Telegram Pipeline Task 1: durable inbox, отдельные task outbox и Telegram outbound, PostgreSQL-идемпотентность; отдельный `update_id`, реальный rollback/downgrade и task review подтверждены полным Docker gate `128 passed`.
  - [x] Reliable Telegram Pipeline Task 2: privacy gate, versioned consent и Telegram webhook; durable idempotent consent/non-text replies, unknown-delivery stop и восстановление inline keyboard из PostgreSQL подтверждены review и Docker gate `136 passed`.
  - [x] Reliable Telegram Pipeline Task 3: пятисекундный Redis-буфер, fallback без потери и durable enqueue; concurrent flush/relay, DB-before-delete, narrow Redis fallback и broker-confirm state прошли review и Docker gate `146 passed`.
  - [x] Reliable Telegram Pipeline Task 4: полный durable Telegram path через текущий LLM, DB-backed order/idempotency, bounded Redis/PostgreSQL recovery, private-chat-only V1, prompt reload и terminal unknown-delivery; три review/fix-loop завершены Ready to merge: Yes, no-cache Docker gate `183 passed`.
  - [x] Reliable Telegram Pipeline Task 5: implementation checkpoint и whole-branch fix-loop пройдены — добавлены обязательный webhook secret, строгий конкурентный per-chat delivery order, Redis-loss startup/recovery, точная классификация Telegram ошибок, durable `/start`/paused/overlength replies, минимальный Rabbit payload и единый shutdown deadline; final review fixes закрыли pre-send release и multi-worker reconciliation race PostgreSQL singleton-lock'ом. Полный Docker-suite дал `195 passed`, worker no-cache image собран non-root, без Redis стал healthy, второй worker fail-closed отклонён, graceful stop занял 4.18s; повторный whole-branch review дал `0/0/0`, controller no-cache verification — `195 passed`.
  - [x] Фаза Reliable Telegram Pipeline завершена: Tasks 1–5, два whole-branch review/fix-loop и независимый controller completion gate подтверждены; следующий checkpoint — Staging с отдельным test token, HTTPS ingress и `setWebhook`.
  - [x] Staging design: минимальная изоляция/HTTPS/live-smoke/recovery/runbook согласованы и записаны в `docs/superpowers/specs/2026-07-18-staging-design.md`; committed spec одобрена пользователем.
  - [x] Staging implementation: Tasks 1–5, `Local/fake ready` и Live Task 6 Steps 1–11 завершены. Исходные live smoke/recovery/safe-log gates, три review fix-loop и первый full Docker gate `250 passed` дополнены distinct YCLIENTS app release: immutable previous сохранён, candidate развёрнут, официальный app-only rollback и возврат на candidate подтверждены без DB downgrade.
  - [x] Staging Task 6 Step 1: deploy skill применён; локальный read-only gate подтвердил actual contract (`project/llm/Dockerfile`, `project/migrate/Dockerfile`, base/staging Compose), ожидаемое отсутствие корневого Dockerfile и остановил работу до SSH из-за отсутствующих access/live inputs.
  - [x] **Staging VPS access:** реквизиты текущего VPS найдены в локальном ignored `.env` прототипа и не выводились; read-only SSH подтверждён через одноразовый изолированный Docker/`expect` wrapper. Общие и prototype-контейнеры не изменялись.
  - [x] **Staging reviewed source:** по явному разрешению пользователя ветка `codex/staging-phase3` опубликована в существующий Git remote и получила upstream; `.env` не отслеживается. PR и merge в `main` не создавались.
  - [x] **Staging VPS inventory:** Docker и Compose доступны, около 9.6 GiB свободно, порты 80/443 свободны, `/opt/moroz-staging` отсутствует; зафиксирован первый изолированный deploy. Четыре работающих prototype-контейнера не изменялись.
  - [x] **Staging SSH recovery:** через независимую серийную консоль Timeweb остановлены только staging bot/worker; SSH banner и безопасный control channel восстановились, прототип не перезапускался.
  - [x] **Staging VPS RAM:** kernel console подтвердила прежний OOM на 961 MiB RAM без swap; пользователь увеличил текущий VPS до 2 CPU / 4 GiB RAM / 50 GiB NVMe. После перезагрузки staging stores, bot и worker healthy без restart, available около 2.8 GiB; четыре prototype-контейнера также работают и healthy.
  - [x] **Staging live gate:** отдельные staging test bot/token и LLM API key подтверждены; TLS `404/403/403`, webhook, consented canary, duplicate replay, повторяемый synthetic `1/1/1`, worker/Redis recovery, image metadata и live-log scans прошли. Первый отличающийся YCLIENTS release закрыл последний gate настоящим app-only `candidate → previous → candidate`; schema осталась forward-only `0006`, destructive DB downgrade не выполнялся.
  - [x] **Static assets staging admin исправлены:** старый UI-каркас сохранён, Caddy больше не снимает обязательный `/admin`, regression и live smoke подтверждают CSS `200 text/css` и logo `200 image/png`, browser render применяет stylesheet без horizontal overflow.
  - [ ] **Актуализация staging для ручной приёмки:** preflight и резервная копия завершены; локальная admin/HTTPS реализация прошла TDD, Ruff, `107` affected и `919` full Docker tests, compile/config/Caddy gates. Осталось опубликовать `main`, пройти isolated backward-compatibility gate, развернуть commit, применить миграции и выполнить только technical smoke.
  - [x] **Staging rollback на первом следующем app release:** immutable previous и distinct YCLIENTS candidate сохранены; официальный `candidate → previous → candidate` выполнен только для bot/worker без DB downgrade. Bot/worker IDs различаются true/true; previous и restored candidate дали fresh smoke `1/1/1` и log scan `0/0/0/0`, schema осталась `0006`, stores/Caddy IDs/StartedAt неизменны, финальный app — candidate.
    - [x] **YCLIENTS staging release execution:** previous/candidate archives inspectable; verified bundle доставлен без push, allowlisted config `7/7`. Candidate rollout: image scan `0/0/0/0`, app `2/2`, HTTPS `404/403/403`, Telegram `1/1/1`, GET-only readiness `1/1/312`, field match `1`. Distinct rollback rehearsal полностью прошла; self-review, independent review/fix-loop и fresh completion verification закрыты.
      - [x] Fresh completion gate: no-cache полный Docker suite `472 passed`, skips `0`; отдельный task-prefixed migrate достиг `0006_yclients_booking_key (head)`, corrected compile gate прошёл, оба scoped cleanup — `0/0/0/0`. Финальный remote read-only check подтвердил exact candidate IDs, app `2/2`, staging `6/6`, schema `0006`, HTTPS `404/403/403`, log scan `0/0/0/0` и доступность обоих архивов.
- [x] **YCLIENTS Booking:** вариант A и PostgreSQL-owned redesign полностью реализованы и подтверждены. Local/fake финальный gate: `0006_yclients_booking_key (head)`, `472 passed` без skips, review `0 Critical / 0 Important / 0 Minor`. В выделенном test-филиале создан exact record field `moroz_booking_key`, выданы минимальные view/edit права, app переподключено; read-only preflight подтвердил один exact code, тип «Текст», `user_can_edit=true`, `show_in_ui=false`. Pre-design synthetic cleanup прошёл `1 → DELETE 204 attempts=1 → active 0`. Финальный consented no-cache lifecycle smoke прошёл с первой попытки: services/staff/slots `1/1/312`, create/get/reschedule/get/cancel confirmed, `matches=1`, `active_matches=0`, `manual_review_required=false`, unknown outcome отсутствует; Docker cleanup `0/0/0/0`. User Token не переносился в Git/чат, blind mutation retry не добавлен.
  - [x] Task 1: добавлены immutable booking contract и in-memory mock YCLIENTS с фильтрацией слотов, локальной идемпотентностью и unit-проверками в Docker.
  - [x] Task 2: добавлены additive revision `0005_booking_state` и durable `BookingRepository` с атомарными checkpoints/events.
  - [x] Task 3: добавлена локальная create-booking state machine с confirmation gate, PostgreSQL-сериализацией concurrent flow между workers, повторной проверкой и create-race слота, durable lost-slot alternatives, terminal repeat и fail-closed recovery из `executing`.
  - [x] Task 4: добавлены локальные перенос/отмена, ownership и 3h gates, durable fail-closed fallback, stable terminal repeats и fresh alternatives при mutation-time потере слота.
  - [x] Task 5: local checkpoint: `0005_booking_state (head)`, два независимых confirmed-create repeat запуска (`1 passed`, `1 passed`), canonical full suite `308 passed`, compile/config/static gates подтверждены.
  - [x] Whole-phase review fix-loop: закрыты external-id serialization, current snapshot ownership/status/time validation, state-only create terminal и unresolved-outcome guard; финальный review — `0 Critical / 0 Important / 0 Minor`.
  - [x] Official contract checkpoint: подтверждены `https://api.yclients.com`, partner/user auth, v2 envelope, лимиты `5 req/s` и `200 req/min`, availability endpoints и protected create/get/update/delete record endpoints. `Idempotency-Key` в контракте отсутствует; mutation retry остаётся fail-closed.
  - [x] Real adapter design: записан одобренный contract mapping, restart-safe opaque slot ID, минимальный consented customer payload, protected CRUD, dual-window rate limit и no-retry/fail-closed policy.
  - [x] Real adapter Task 1: create domain расширен обязательными consented name/phone и optional comment; contact/consent gate срабатывает до checkpoint и любого port call, Docker booking suite — `61 passed`.
  - [x] Real adapter Task 2: stdlib HTTP config/auth, no-retry transport и process-local rolling limiter `5/1s` + `200/60s`; fake HTTP/static Compose Docker gate — `29 passed`, `YCLIENTS_*` получает только worker. Controller fix-loop усилил конкурентную проверку до двухфазного сценария с шестью waiters; финальный review — `0 Critical / 0 Important / 0 Minor`.
  - [x] Real adapter Task 5 — исторический pre-0006 checkpoint: добавлен explicit `yclients-smoke` profile и bounded redacted flow services/staff/slots → create/get/reschedule/get/cancel/final-state/duplicate-count; outcome-unknown прекращает mutations, definite post-create failure допускает один cleanup. Тогдашний whole-review fix-loop закрыл redirect auth leak, повтор cancel и strict deleted/contact payloads; focused gate — `121 passed`, pre-ownership suite — `428 passed`, review — `0/0/0`.
  - [x] Real adapter Task 3: official `book_dates → book_staff → book_times`, authenticated restart-safe deterministic slot/owner markers, pre-create `book_check` и protected create/get реализованы без cache/retries; final focused GREEN — `51 passed`, fresh combined gate — `112 passed`; child и controller re-review — `0 Critical / 0 Important / 0 Minor`.
  - [x] Real adapter Task 4: protected reschedule/cancel используют exact record CRUD, Partner-only pre-reschedule `book_check`, один PUT/DELETE без retry и durable PostgreSQL `booking_outcome_unknown`; review fix-loop запретил перенос cancelled record и исправил 5xx precedence; Docker focused — `65 passed`, booking/migration regression — `140 + 20 passed`; independent re-review — `0 Critical / 0 Important / 0 Minor`, Ready yes.
  - [x] Real adapter local/fake HTTP gate: fake HTTP contract, durable outcome-unknown E2E, bounded smoke tooling, fresh no-cache verification и independent review завершены; live API не вызывался.
  - [x] Ownership redesign Task 1: additive `0006_yclients_booking_key` добавляет `bookings.booking_key UUID NOT NULL UNIQUE`, детерминированно backfill-ит legacy строки из `id`; targeted Docker evidence без downgrade — `1 passed`, exact cleanup `0/0/0/0`.
  - [x] Ownership redesign Task 2: trusted UUID ownership проведён через domain/service/fake/repository; create использует `scenario.id`, change — locked local owner/key, immutable conditional upsert fail-closed откатывает оба независимых конфликта. Docker booking regression — `75 passed`, focused rollback evidence — `2 passed`, exact cleanup обоих namespaces `0/0/0/0`.
  - [x] Pre-0006 sandbox diagnosis: query/datetime gaps и stale rights закрыты; прежние `3` synthetic records удалены `3/3`, instrumented attempt доказала замену submitted ownership `api_id`, последняя pre-design active запись позднее удалена отдельным exact cleanup `1 → 0`. Диагноз закрыт реализованным `moroz_booking_key` design; blind retry/patch запрещены.
  - [x] Final YCLIENTS sandbox lifecycle: no-cache isolated run прочитал `1` service, `1` staff и `312` slots; один trusted UUID прошёл create/get/reschedule/get/cancel, reconciliation подтвердил `matches=1`, `active_matches=0`, `success=true`, `manual_review_required=false`; task resources очищены `0/0/0/0`.
  - [x] **LLM Security:** единый in-process security pipeline завершён: PII masking до внешних LLM/judge, scripts-first guard/router, prompt-leak/jailbreak protection, primary/reserve policy, bounded output validation и count-only security eval gate.
    - Финальный независимый review — `0 Critical / 0 Important / 0 Minor`, Ready yes. Fresh task-specific Docker completion gate на exact source `ee64135`: `767 passed / 0 failed / skips 0`, `0006_yclients_booking_key (head)`, config/build/compile exits `0`, SDK AST `2 approved / 0 bad`, env allowlist `4 passed`, static secret-shaped literals `0`, migration и Phase 6–8 runtime diff `0`, cleanup `0/0/0/0`. Provider/staging/production mutations не выполнялись.
  - [ ] **LAUNCH BLOCKER:** создать отдельные стойкие production credentials для Redis и RabbitMQ вне Git.
  - [ ] **LAUNCH BLOCKER:** получить и проверить production credentials внешних интеграций по мере подключения, включая YCLIENTS.

### Ближайшие задачи: стабилизировать текущий тестовый контур
- [x] Прочитать текущий `project/llm/prompts/system.md` и провести первичную доработку промпта перед eval-прогоном.
- [x] Усилить промпт по качеству ответов, безопасности, стилю и сценариям записи/передачи администратору.
- [x] Зафиксировать стартовый synthetic baseline эталонных пользовательских вопросов для базового бота.
- [x] Разделить стартовый eval-набор по сценариям: контакты, запись, цены, объяснение услуг, медицинские границы, подготовка, сертификаты, жалобы, неизвестные услуги, подбор направления, prompt-safety.
- [x] Сделать первый прогон эвалов через админский eval-runner и зафиксировать результаты.
- [x] Добавить в админке удобный способ видеть результаты eval-прогонов и причины verdict через `/eval/`.
- [x] Подготовить короткий отчет по первому eval-прогону.
- [x] Сделать отдельную клиентскую страницу для правок и комментариев по eval-кейсам без запуска LLM и расхода токенов.
- [x] Упростить клиентскую форму новых eval-кейсов: убрать категорию из интерфейса и добавить удаление черновых предложений.
- [x] Интегрировать свежие клиентские комментарии из Google Doc и новых Markdown-файлов в `project/llm/prompts/system.md`, `project/data/services_prices.md` и `project/llm/eval/dataset.json`.
- [ ] Прогнать обновленный набор из 53 eval-кейсов и разобрать новые FAIL/спорные ответы после интеграции клиентских документов.
- [ ] Проверить, что hot-reload промпта применяет правки без ручного рестарта бота.
- [ ] Проверить права записи серверной админки в `/app/prompts/system.md`: при попытке сохранить промпт v1.2 через админку получен `write_failed`.
- [ ] Уточнить у клиента остаточные нестандартные условия: временные акции, недоступные услуги, расписание специалистов, спорные противопоказания и правила возвратов/жалоб.

### Ближайшие задачи: подготовка к целевой архитектуре из ТЗ
- [x] Объединить актуальное ТЗ и полную ASCII-схему в единый главный документ `ТЗ и архитектура.md`, удалить отдельный Markdown-дубль архитектуры и обновить ссылки.
- [ ] Исправить порядок privacy gate для аудио: до согласия не отправлять голосовое сообщение в Yandex SpeechKit.
- [ ] Дополнить ТЗ матрицей этапов релиза, таблицей источников правды и явными state machine для записи, оплаты, эскалации и удаления ПД.
- [x] После подтверждения пользователя внести в ТЗ рекомендованные решения по открытым вопросам и уточнить процессы post-visit feedback и реактивации со схемы «Отдельные процессы».
- [x] Провести полную сверку ТЗ с прямым клиентским DOCX, двумя базами знаний от 2026-07-07, вводным созвоном, обучением администраторов, старым ботом и снимками сайта/YCLIENTS; сохранить карту актуальности и конфликтов в `project/data/source_documents/client_materials_audit_2026-07-14.md`.
- [ ] Получить от собственницы финальные анкеты безопасности по процедурам, паспорта оборудования и точный опрос самооценки состояния с правилами зеленой/желтой/красной зон.
- [ ] Проверить API и права текущего аккаунта YCLIENTS на тестовом контуре: несколько услуг, перенос/отмена, сертификаты, абонементы и депозитные счета.
- [ ] Перенести нужные необработанные оригиналы клиентских документов из игнорируемых `tmp/` и `project/tmp/` в постоянный защищенный архив, не публикуя ПД и внутренние материалы в открытом Git.
- [x] Разбить первый production-релиз на восемь проверяемых фаз: Foundation, Reliable Telegram Pipeline, Staging, YCLIENTS, LLM Security, Scheduler & Notifications, Production Admin, Operations & Release. ЮKassa, голос, маркетинг и новые каналы остаются следующими релизами.
- [x] Подготовить технический backlog по конкретным файлам, контрактам, миграциям, TDD-шагам, Docker-командам и review checkpoints в корневом `План реализации.md` и связанных фазовых планах.
- [x] Выделить первый production-релиз целевой архитектуры: Telegram, YCLIENTS, защищённый LLM-контур, scheduler/worker, операционная админка и production-эксплуатация; голос, ЮKassa, маркетинг и новые каналы отложены.
- [ ] Проверить встроенную ASCII-схему в `ТЗ и архитектура.md` и при необходимости подготовить отдельную клиентскую упрощённую схему.

### Ближайшие задачи: ступень 4 — Буфер
- [ ] Добавить склейку быстрых сообщений пользователя в один LLM-запрос.
- [ ] Настроить задержку буфера 5 секунд и лимиты, чтобы бот не отвечал на каждое короткое догоняющее сообщение отдельно.
- [ ] Проверить сценарии: одно сообщение, серия быстрых уточнений, пауза пользователя, ошибка LLM во время буферизации.

### Целевая архитектура: каналы и сообщения
- [ ] Спроектировать слой адаптеров для 4 каналов: Telegram, WhatsApp, Instagram, ВКонтакте.
- [ ] Привести входящие события каналов к единому формату `IncomingMessage`.
- [ ] Добавить обработку голосовых/аудио через Yandex SpeechKit.
- [ ] Настроить поведение для неподдерживаемых типов сообщений: фото, видео, кружки, файлы.
- [ ] Решить срок временного хранения оригинальных аудиофайлов для отладки.

### Целевая архитектура: роутер и сценарии
- [ ] Добавить роутер намерений: FAQ, подбор услуги, запись, перенос, отмена, оплата, сертификат, курс, депозит, жалоба, эскалация, рассылки.
- [ ] Поддержать мультинтенты: один запрос может запускать несколько сценариев.
- [ ] Описать приоритеты сценариев: безопасность/эскалация → запись/перенос → оплата/коммерция → FAQ → маркетинг.
- [ ] Добавить deterministic workflow для записи, переноса и отмены.
- [ ] Добавить сценарий нескольких услуг за один визит.

### Целевая архитектура: YCLIENTS
- [x] Whole-phase aggregate review-fix: закрыты `1 Critical / 2 Important / 2 Minor` и два последующих review-gap через external-id serialization, current snapshot validation, immutable terminal state и unresolved-outcome aggregate guard; Docker booking/migrations — `77 passed`, canonical — `308 passed`, финальный независимый review — `0 Critical / 0 Important / 0 Minor`; отдельный post-commit controller gate повторно подтвердил `308 passed`, compile/config и `0005_booking_state (head)` с cleanup `0/0/0`.
- [x] Task 4 YCLIENTS: реализованы перенос/отмена, ownership и 3h gates, durable fail-closed fallback, stable terminal repeats и fresh alternatives при mutation-time потере слота; Docker booking/migration gate — `53 passed`.
- [x] Ownership hardening Task 3: real YCLIENTS adapter переведён с `api_id` на exact `custom_fields.moroz_booking_key`; trusted ownership, GET/PUT/DELETE preflight, foreign custom fields и one-shot mutation semantics подтверждены focused `104` и full booking `205` Docker-тестами.
- [x] Ownership hardening Task 4: local/fake sandbox smoke использует один trusted UUID для create/get/reschedule/get/cancel, exact-key reconciliation и safe summary без provider ID; локальная реализация завершена.
- [x] Ownership hardening Task 4 review fix: invalid create-result не запускает cleanup mutation; missing `custom_fields` остаётся unrelated, а present malformed field или exact-key record без explicit boolean `deleted` fail-closed. Docker focused smoke + safe logging — `38 passed`.
- [x] Ownership hardening Task 5: финальный broad review fix-loop закрыл post-cancel manual-review gap, сузил empty-env exception до exact ownership plan и убрал ownership-проверку из generic HTTP serializer. Свежий no-cache Docker gate подтвердил `0006_yclients_booking_key (head)`, `472 passed` без skips и cleanup `0/0/0/0`; актуальный внешний gate зафиксирован один раз в разделе YCLIENTS Booking выше.
- [ ] Получить и проверить доступы/API-возможности YCLIENTS.
- [ ] Реализовать получение услуг, мастеров, свободных дат и слотов.
- [ ] Реализовать создание записи после подтверждения клиентом.
- [ ] Реализовать повторную проверку слота перед созданием записи.
- [ ] Реализовать перенос записи через поиск активных записей по телефону.
- [ ] Реализовать отмену записи с подтверждением и правилом отмены.
- [ ] Сделать fallback при сбое YCLIENTS: продолжить FAQ, остановить операции записи, собрать минимальную заявку после согласия и передать администратору без обещания слота.
- [ ] Синхронизировать локальные копии записей с YCLIENTS для истории, аналитики и follow-up.
- [ ] Уточнить возможности YCLIENTS по курсам, депозитам, сертификатам и нескольким услугам в одном визите.

### Целевая архитектура: ЮKassa и коммерческие сценарии
- [ ] Создать утверждаемую матрицу «процедура → программа/абонемент» с актуальными составом и ценой; LLM не должна придумывать связи и обещать гарантированный эффект.
- [ ] Спроектировать платежный модуль ЮKassa: создание платежной ссылки, webhook, статусы, ошибки.
- [ ] Добавить оплату после создания записи как опциональный следующий шаг.
- [ ] Добавить правила обязательной предоплаты для отдельных услуг/товаров, если клиент подтвердит.
- [ ] Реализовать сценарии покупки курса, пополнения депозита и подарочного сертификата.
- [ ] Фиксировать покупки в локальной БД и уведомлять администратора.
- [ ] Синхронизировать коммерческие покупки с YCLIENTS, если API позволит.
- [ ] Уточнить формат выдачи сертификата: PDF/картинка автоматически или ручное оформление.

### Целевая архитектура: админка владельца
- [ ] Перевести админку из внутреннего тестового инструмента в клиентский центр управления владельца.
- [ ] Добавить/уточнить разделы: диалоги, карточка клиента, эскалации, записи, цены и услуги, база знаний, платежи, курсы/депозиты/сертификаты, follow-up, рассылки, статистика, здоровье системы, логи, аудит.
- [ ] Сделать редактирование цен и услуг через админку.
- [ ] Спроектировать хранилище базы знаний: Postgres + версии + Markdown/JSON-снапшоты.
- [ ] Добавить версионирование цен, услуг, базы знаний, промптов, сценариев и шаблонов.
- [ ] Добавить аудит действий собственника и администратора, включая просмотры ПД и операции с записями.
- [ ] Реализовать роли `Собственник` и `Администратор` по принципу минимальных прав, индивидуальные учётные записи и обязательную TOTP-2FA.

### Целевая архитектура: scheduler, worker и рассылки
- [x] Добавить Foundation skeleton RabbitMQ, worker и долгоживущего scheduler с heartbeat, manual ack, тремя retry и DLQ на 30 дней.
- [ ] Спроектировать отдельный scheduler для напоминаний, follow-up, реактивации и фоновых задач.
- [x] Зафиксировать RabbitMQ + Workers: ручной ack, три повтора с backoff `1/5/30` секунд, DLQ на 30 дней и стартовая параллельность 4 задачи на worker.
- [x] Реализовать DB-backed idempotency и строгий порядок внутри одного диалога в фазе Reliable Telegram Pipeline.
- [x] Реализовать устойчивые исходящие сообщения со статусами `sending/sent`, внешним message ID и `idempotency_key`; неопределённый результат отправки не повторять вслепую.
- [ ] Сохранять критическое состояние и checkpoints сценариев в PostgreSQL; Redis использовать только для временного контекста, буфера и блокировок.
- [ ] Добавить retry/fallback для YCLIENTS, ЮKassa, LLM, Yandex SpeechKit и отправки сообщений.
- [ ] Реализовать ручные рассылки из админки: черновик, предпросмотр, тест, подтверждение, запуск.
- [ ] Добавить лимиты рассылок: дневной лимит, скорость, защита от дублей, стоп-кнопка.
- [x] Уточнить сроки напоминаний и follow-up: сразу после записи, за день, в 09:00, за час, no-show в момент начала; post-visit — один раз за всю историю клиента через 2 часа после первого подходящего визита.
- [ ] Реализовать idempotency-ключи напоминаний и follow-up, отмену задач после переноса/отмены, объединение совпадающих сообщений и признак `feedback_requested_at` в профиле клиента.
- [ ] Перед запуском post-visit получить и проверить постоянную ссылку центра на Яндекс Карты.

### Целевая архитектура: безопасность LLM и ПД
- [ ] Добавить security pipeline вокруг каждого LLM-вызова: локальная validation/input security → маскирование ПД → при необходимости облачный guardrail → LLM → output security → validator.
- [ ] Определить guard-модель и инструменты маскирования ПД.
- [ ] Маскировать телефон, ФИО, email, адреса, ID каналов, платежные и медицинские данные перед внешней LLM.
- [ ] Добавить output validation: запрет утечек промпта, ПД, медицинских обещаний, выдуманных цен/слотов/услуг.
- [ ] Разделить согласия: обработка ПД, рекламные рассылки, сервисные уведомления.
- [ ] Уточнить сроки хранения ПД и правила автоанонимизации.
- [ ] Реализовать privacy gate до долгого хранения и внешних вызовов: без согласия сохранять только минимальные технические данные.
- [ ] Выполнять deterministic scripts-first проверки до guard-модели; LLM использовать только для неоднозначных случаев.
- [ ] Восстанавливать замаскированные ПД только после output security/validator и только из текущей сессии.
- [ ] Считать недоверенными и изолировать от инструкций модели имена, комментарии, данные YCLIENTS, базу знаний и другие внешние поля.

### Целевая архитектура: эвалы и качество
- [ ] Расширить eval-набор под целевую архитектуру: роутер, guardrails, запись, оплата, эскалация, fallback.
- [ ] Для каждого тест-кейса описать ожидаемое поведение: что бот должен сказать, чего не должен говорить, какой следующий шаг предложить.
- [ ] Добавить критерии оценки: корректность фактов, безопасность, отсутствие выдуманных цен, краткость, CTA, корректная эскалация.
- [ ] Подготовить отдельные проверки для роутера, основной LLM, output validator и guardrails.

### Целевая архитектура: мониторинг и надежность
- [ ] Добавить экран здоровья системы в админке: каналы, LLM, YCLIENTS, ЮKassa, Yandex SpeechKit, Redis, Postgres, очередь, workers, scheduler, последние ошибки.
- [ ] Настроить технические алерты в отдельный Telegram-бот и админку.
- [x] Реализовать безопасный public `/healthz` и минимальные реальные owner-only counters PostgreSQL inbox/outbox/worker/scheduler/tokens/escalations, Redis availability и RabbitMQ queue/DLQ.
- [ ] Подключить внешний uptime-monitor и собрать реальное alert evidence; расширить observability метриками контейнеров, интеграций, задержек и расходов, если они потребуются сверх согласованной минимальной launch-границы.
- [ ] Добавить fallback основной LLM: резервная LLM или безопасная заглушка + эскалация.
- [ ] Заложить ежедневные бэкапы Postgres и проверку восстановления.
- [ ] Добавить KPI: диалоги, записи, конверсия в запись, эскалации, стоимость LLM, оплаты, ошибки, среднее время ответа, fallback.
- [ ] Ввести `correlation_id`, единый результат сценария (`status/message/next_action/events/cost/error_code`) и событийный журнал прохождения каждого запроса.
- [x] Ввести Alembic baseline, fail-closed cutover audit и отдельный immutable migration stage.
- [ ] Довести последующие Alembic-релизы до процесса `expand–migrate–contract` в соответствующих feature-фазах.
- [ ] Защитить webhook подписью/секретным токеном, дедупликацией и проверкой возраста; несопоставленные события отправлять на техническую и при необходимости бизнес-проверку.
- [ ] Настроить уникальные пароли PostgreSQL/Redis/RabbitMQ, non-root SSH с ограниченным sudo и хранение production-секретов вне Git.
- [ ] После случайного появления значений `.env` в техническом выводе задачи от 2026-07-14 заменить все действующие серверные и production-секреты, проверить доступы и сохранить новые значения только в защищённом хранилище.
- [ ] **LAUNCH BLOCKER:** до production ротировать текущий PostgreSQL credential после локального pytest repr exposure от 2026-07-15; новое значение не выводить в задачи/логи и хранить только в защищённом `.env`/менеджере паролей.
- [ ] Ограничить цикл output validation одной повторной генерацией, затем fallback/эскалация.

### Открытые вопросы перед production
- [x] Задать отдельные 15-20 вопросов перед production по деплою, безопасности, юридике, бэкапам и эксплуатации.
- [ ] Уточнить домены, HTTPS, секреты, доступы, firewall, SSH, webhook security и план отката.
- [ ] Уточнить юридику ПД, рекламных рассылок, сервисных уведомлений и сроков хранения.
- [ ] Уточнить SLA и план действий при сбоях YCLIENTS, ЮKassa, каналов и LLM.
- [ ] Подготовить финальный production checklist приемки.

### Лестница ступеней
- [x] 1. Прототип
- [x] 2. Админка
- [x] 3. Эвалы
- [ ] 4. Буфер
- [ ] 5. Безопасность LLM (guardrails)
- [ ] 6. Надёжность LLM (резерв + retry + алерты)
- [x] 7. Первый деплой на сервер
- [ ] 8. Production-ready

### Дополнительно выполнено
- [x] Из учебной доски `Блок 2 урок 8. Продакшн.excalidraw` извлечены все 42 текстовых блока в чистый TXT-файл `tmp/Базовая информация по production.txt`; изображения и JSON-разметка исключены.
- [x] Старый локальный архив AI OS перенесён из корневой `ignore/` в единую папку расходников `tmp/`; лишняя папка `ignore/` удалена.
- [x] Прежний план размещения Excalidraw в `project/data/architecture/` отменён: исходник перенесён в корневую `tmp/`, актуальная схема хранится в `ТЗ и архитектура.md`.
- [x] Шесть скиллов `DietrichGebert/ponytail` установлены локально в `.agents/skills`; lifecycle-хуки оставлены выключенными до отдельного аудита и доверия.
- [x] Набор из 14 скиллов `obra/superpowers` установлен локально в `.agents/skills` только для этого проекта.
- [x] Context7 перенесён из глобальной конфигурации Codex в проектную `.codex/config.toml`.
- [x] Архитектура сведена в единую схему, перенесена в `ТЗ и архитектура.md`, исходный Excalidraw убран в `tmp/`; устаревший дубль `- handdrawn` удалён.

- [x] Phase 6 Scheduler & Notifications checkpoint: миграция `0007_scheduler_notifications`, устойчивые scheduler jobs, reminder/no-show handlers, feedback-once и human mode подтверждены локальными Docker gates; review-fix замкнул scheduler/worker runtime, booking schedule transaction, retry/DLQ terminal state и staff fail-closed. Финальный focused gate `54 passed`, full pytest `793 passed in 388.38s`, Alembic `0007_scheduler_notifications (head)`, scheduler/worker build и compileall exit `0`, независимый review `0 Critical / 0 Important / 0 Minor`, cleanup `0/0/0/0`; staging/production/provider mutations не выполнялись.
- [x] **YCLIENTS lifecycle `0008`:** read-only ingestion для `completed`, `no_show` и неизвестных YCLIENTS statuses через точечные scheduler jobs без polling-сервиса завершён; migration head `0008_yclients_lifecycle`, focused gate `204 passed`, свежий full Docker suite `843 passed in 421.56s`, финальный review `0 Critical / 0 Important / 0 Minor`, Ready to merge Yes, exact cleanup `0/0/0/0`. Одобренная spec — `docs/superpowers/specs/2026-07-27-yclients-lifecycle-design.md`; staging/production/provider/YCLIENTS/Telegram actions не выполнялись.
  - [x] Task 3: lifecycle scheduler handler и optional worker runtime wiring подключены; focused notification/worker/migration-profile gate `61 passed`, lifecycle regressions `11 + 6 passed`.
- [x] **Phase 7 Production Admin `0009` — local-ready security/ops layer:** существующая FastAPI/Jinja2 админка усилена DB users/sessions, TOTP-ready login, CSRF, RBAC, audit, role-aware UI и безопасным публичным prefix `/admin`; это не полная идеальная админка из ТЗ.
  - [x] Task 1: добавлены стандартно-библиотечные admin security helpers для PBKDF2, TOTP и CSRF, создана миграция `0009_production_admin` поверх `0008_yclients_lifecycle`; Docker TDD gate `8 passed`, Alembic upgrade/current подтвердил `0009_production_admin (head)`.
  - [x] Task 2: login переведён на `AuthenticatedUser`, TOTP-поле и DB-ready `user_repository` с bootstrap fallback только при отсутствии DB users; Docker RED `5 failed`, GREEN auth gate `5 passed`, составной admin/migration/profile gate `31 passed`.
  - [x] Task 3: добавлены минимальные `rbac.py` и `audit_repository.py`; `/bot-control/toggle` теперь требует CSRF до Redis и пишет audit event после успешного переключения. Docker RED подтвердил отсутствие `rbac`, focused GREEN `4 passed`, общий admin Phase 7 gate `35 passed`.
  - [x] Review-fix wave 1: bootstrap fallback теперь fail-closed на `admin/admin` и дефолтном `ADMIN_SESSION_SECRET`; CSRF и audit добавлены для prompt/eval/review POST, audit просмотра чата включён; migration/integration tests обновлены под head `0009`; focused gates `29 passed`, `47 passed`, `19 passed`.
  - [x] Review-fix wave 2: `admin_sessions` переведены на server-side validation/revocation в Postgres; устаревший, удалённый или отсутствующий `sid` больше не принимается только по подписанной cookie, logout удаляет DB-сессию, `last_seen_at` обновляется при валидном запросе. TDD RED `6 failed / 6 passed`, focused GREEN `12 passed`, расширенный admin gate `73 passed`, полный Docker suite `867 passed in 736.81s`.
  - [x] Review-fix wave 3: включён фактический RBAC enforcement для owner-only действий — глобальный bot toggle и боевые prompt save/rollback; `admin` останавливается до Redis/файла/БД, route-level expectations закреплены тестами. TDD RED `3 failed / 4 passed`, focused GREEN `7 passed`, расширенный admin gate `76 passed`, полный Docker suite `870 passed in 475.85s`.
  - [x] Phase 7 финальный review/closure: закрыты независимые замечания по JSONB audit persistence, fail-closed bootstrap при недоступной БД, owner-only GET routes и role-aware UI. TDD RED `6 failed / 16 passed`, focused GREEN `22 passed`, расширенный admin gate `82 passed`, полный Docker suite `881 passed in 613.18s`.
- [x] **Phase 8 Operations & Release — local-ready, launch blocked:** локальные contracts, artifacts и Docker gates завершены; реальный production acceptance не проводился.
  - [x] Task 1: добавлены production Compose overlay, Caddy TLS ingress, `.env.example` и fail-closed `ops/validate_env.py`; `admin` в rendered Compose остаётся только на `127.0.0.1:${ADMIN_PORT:-8080}:8080`, публичный ingress идёт через Caddy `80/443`. TDD RED: отсутствие `!override` поймано regression test; GREEN `5 passed`, docs/ops gate `11 passed`, Compose config quiet exit `0`.
  - [x] Task 2: добавлены минимальные `moroz.common.metrics`, `moroz.common.alerts` и owner-only `/metrics` в FastAPI admin; alert routing использует Redis cooldown key `alert:{code}:{subject}`, технический и business-critical routing, PII redaction email/phone и allowlisted metric labels. TDD RED: отсутствующий `moroz.common.alerts`; GREEN `4 passed`, admin/ops gate `36 passed`.
  - [x] Task 3: добавлены shell-контракты backup/verify/restore для PostgreSQL (`pg_dump --format=custom`, OpenSSL AES-256-CBC/PBKDF2, sha256, restore только в отдельную DB), runbook и production overlay mounts `./ops:/ops:ro` + `pgbackups:/backups/postgres`. TDD RED `3 failed` на отсутствующих scripts; GREEN/docs gate `14 passed`, Compose config quiet exit `0`. Реальный server restore drill остаётся launch gate после выдачи production/staging доступов и ключей.
  - [x] Task 4: добавлены локальные artifacts для smoke/load/failure gates — `ops/smoke.ps1`, `ops/load/k6.js`, `ops/failure-gates.md`; покрыты health/privacy/FAQ/mock booking/admin login, 30 inbound/min и 20 active chats, Redis/RabbitMQ/YCLIENTS/primary LLM outage expectations. TDD RED `3 failed`, GREEN `3 passed`, общий ops-focused gate `21 passed`. Реальный staging smoke/load/failure прогон остаётся launch gate.
  - [x] Task 5: добавлены `deploy-runbook.md`, `rollback-runbook.md`, `incident-runbook.md`, `launch-checklist.md`; зафиксированы exact Compose commands, запрет destructive downgrade без backup evidence, разделение technical/business incident actions и blocking launch evidence. TDD RED `4 failed`, GREEN `4 passed`, общий ops-focused gate `25 passed`.
  - [x] Review-fix: закрыты Critical/Important локальные blocker’ы Operations — runnable `ops-check` profile для env validation, backup env в Postgres, `exec -e RESTORE_TARGET_DB`, fail-closed placeholder/domain validation, webhook secret headers в smoke/load, sanitized alert key subject и `ADMIN_COOKIE_SECURE`. RED `8 failed / 11 passed`, GREEN `19 passed`, ops-check placeholder fail-closed подтверждён, valid env `production env validation passed`, расширенный ops/admin gate `60 passed`.
  - [x] Task 6 local acceptance: полный локальный Docker suite `898 passed in 433.14s`, migration gate ранее подтвердил `0009_production_admin (head)`, production Compose render `config --quiet` exit `0`, placeholder `.env.example` fail-closed, valid process-only ops-check `production env validation passed`; все `moroz_*` test namespaces очищаются после acceptance.
  - [x] P0-cleanup readiness audit: работа закреплена в `codex/phase7-8-readiness`, `/admin` сохраняет public prefix за Caddy, `ADMIN_COOKIE_SECURE=true` проверяется fail-closed, rollback использует exact Compose image overrides с pre-build сохранением previous refs, документация разделяет local-ready и production launch. Initial full gate `903 passed`; independent review fix TDD `4 failed → 17 passed`, свежий финальный Docker suite `905 passed in 395.80s`, последний runbook RED/GREEN `1 failed → 10 passed`, production `config --quiet`, runbook `sh -n` и ops-check exit `0`, `git diff --check` clean, три task cleanup `0/0/0/0`; финальный re-review `0 Critical / 0 Important`, Ready yes.
- [ ] **LAUNCH BLOCKERS:** реальные production credentials/доступы и ротация секретов; внешний uptime-monitor и alert evidence для `/healthz`; staging smoke/load/failure; restore drill на изолированной серверной БД; настоящие alert recipients; security/eval evidence; TLS/domain и операционная подпись launch checklist.
- [x] Уточнено состояние production-инфраструктуры: VPS ещё не куплен. По официальным актуальным тарифам подготовлен shortlist российских площадок под стартовую конфигурацию `4 vCPU / 8 ГБ RAM / 80 ГБ NVMe`: Timeweb Cloud, Selectel и Рег.ру; рабочая рекомендация — Timeweb Cloud как наиболее сбалансированный по цене и простоте эксплуатации вариант.
- [ ] После решения пользователя зарегистрировать production-аккаунт на клиентку, выбрать площадку/регион, заказать VPS с Ubuntu 24.04 LTS и только затем создавать отдельные SSH-доступы и production-секреты.
  - [x] **Production Admin launch/backlog boundary согласована:** до первого launch обязательны безопасный health endpoint и реальные системные счётчики; UI операций с записями, управления базой знаний/версиями и рабочих эскалаций перенесён в post-launch backlog.
  - [x] **Health/counters local implementation:** public exact `/healthz` проверяет bounded PostgreSQL readiness и не раскрывает диагностику; owner-only `/admin/metrics` читает durable bot/worker/scheduler/token/escalation counters, Redis availability и live RabbitMQ `tasks`/`tasks.dlq`. Docker TDD focused gate `141 passed`, local runtime smoke вернул health/metrics HTTP 200 и availability `1/1/1`; review fixes закрепили polling/webhook health contract, bounded cleanup, credential-free RabbitMQ Prometheus probe и честные retained LLM gauges. Свежий полный suite `917 passed in 488.11s`, финальный review `0 Critical / 0 Important / 0 Minor`, Ready yes; реализация обошлась без новых сервисов, таблиц и project dependencies. Production evidence остаётся launch blocker.
- [x] **Консолидация старой админки в staging:** существующая FastAPI/Jinja2-панель закреплена продуктовой основой; в staging перенесены 53 eval-кейса, 3 прогона и 90 результатов, legacy- и production-промпты добавлены в историю без замены активного защищённого промпта. Диалоги и production-таблицы не переносились; rollback backup сохранён и проверен. Новые business-вкладки остаются отдельной открытой задачей расширения этой же панели.
- [x] **Incremental manual QA staging-бота через skill:** после buffer/webhook hotfix выполнен повторный прогон `manual-qa-bot-testing` без полного дублирования первого baseline; Telegram Web подтвердил цену/доступность и склейку двух быстрых сообщений, synthetic webhook подтвердил non-text fallback, admin UI показал рост `60→64` сообщений и `30→32` LLM-вызовов, свежие server logs без критичных ошибок. Отчёт: `tmp/manual-test-20260731-1433/Отчет по тестированию бота.md`.
- [x] **Targeted security/guardrails QA staging-бота:** через `manual-qa-bot-testing` проверены prompt/canary leak, instruction override, medical-risk, PII на фейковых данных, fake price, clean `стоп`, rate limit и восстановление после него; критичных ошибок нет, staging containers healthy, `bot:paused` пустой, fresh error scan чистый. Нюанс: provider-side PII masking требует отдельного focused unit/integration gate при запущенном Docker. Отчёт: `tmp/manual-test-20260801-guardrails/Отчет по тестированию бота.md`.
- [x] **Provider-side PII masking gate закрыт:** focused Docker gate прошёл `243 passed`; `CapturingGateway.requests` подтвердил маскирование фейковых имени, телефона и email в текущем вводе и истории до формирования `LLMRequest`, а отдельный provider-boundary e2e gate прошёл `6 passed` для phone/email/name/address/payment/medical. Security tests также подтвердили отклонение неизвестных placeholder и восстановление только из текущей `PiiSession`. Исправлены Windows-разделители в task-команде, передаваемой в Linux test container; production-код не потребовал изменений.
- [x] **Human-only manual QA checklist:** создан файл `Ручное тестирование человеком.md` только с тем, что человек проверяет руками и глазами: качество ответов, тон, услуги/цены, запись, медицинские границы, prompt-safety, видимое обращение с фейковыми ПД, буферизацию, нестандартные сообщения и pause UX; технические Docker/SQL/log/webhook checks исключены.
- [x] **Targeted recheck после guardrails/PII fixes:** повторно проверены staging Telegram-бот, provider-side PII masking и базовый smoke через `manual-qa-bot-testing`; Docker gates `243 passed` и `6 passed`, Telegram Web подтвердил цену, буферизацию, запрет фейковой записи, medical-risk escalation, prompt-safety, видимое поведение с фейковыми ПД и нормальный ответ после safety-сценариев; staging containers healthy, `bot:paused` пустой, свежий error scan пустой. Отчет: `tmp/manual-test-20260801-1240-recheck/Отчет по тестированию бота.md`.
- [x] **Приветствие `/start`:** общий текст заменён на согласованное представление онлайн-ассистента с перечнем возможностей и пустыми строками между тремя смысловыми блоками; точный Compose render и runtime-значение проверены через Docker, корректный полный gate прошёл `932 passed, 1 deselected`.
- [ ] **Documentation Compose contract debt:** отдельно исправить прежний план `docs/superpowers/plans/2026-08-01-provider-side-pii-masking-gate.md`, в котором осталась команда `docker compose` без обязательного `--env-file ../.env`; проблема обнаружена полным gate при настройке `/start` и не относится к приветствию.
