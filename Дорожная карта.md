# Дорожная карта — Moroz i Solntse Bot

> **Единственный источник текущего статуса проекта.** Цель продукта описана в `ТЗ и архитектура.md`, состав Telegram Production V1 — в `План реализации.md`, подробная история до 2026-08-20 сохранена в `docs/archive/roadmap-history-through-2026-08-20.md`.

## Где мы сейчас

- Продуктовый этап: Telegram Production V1 развёрнут и технически проверен на staging; production launch ещё не выполнялся.
- Проверенный staging-коммит: `f8fa30d3609a725da403520de03c2de0ba9da468`.
- Staging image tag: `rc-f8fa30d3609a725da403520de03c2de0ba9da468`.
- Staging: bot, worker, scheduler, admin, PostgreSQL, Redis, RabbitMQ и Caddy работают; schema — `0024_reactivation_v2 (head)`, реактивация зафиксирована в `dry_run` без активной версии и journeys.
- Последняя техническая runtime-сверка: 2026-08-31; exact image IDs, 8/8 healthy services, schema, HTTPS/admin, webhook, scheduler smoke и safe logs подтверждены. До cutover сохранены защищённые rollback bundle и дамп PostgreSQL; прежние bot/worker отдельно подтвердили backward compatibility с текущей additive schema.
- `main` до runtime-кандидата включительно отправлен в GitHub обычным non-force push; сервер получил exact commit через `git fetch origin main` и развёрнут detached на проверенном SHA. Более поздние документные/content-only коммиты не подмешиваются в уже проверенный runtime-образ. Production и YCLIENTS credentials не затрагивались.
- Полная многоканальная целевая система ещё не завершена: VK, Instagram, WhatsApp, cross-channel profile и кампании относятся к следующим релизам.

## Ключевые завершённые этапы

- Собран первый Telegram LLM-прототип с PostgreSQL, Redis и Docker.
- Добавлены админка, системный промпт, база знаний и инфраструктура эвалов.
- Сформированы целевые `ТЗ и архитектура.md` и план первого Telegram production-релиза.
- Проведён аудит референса бота Володи/Lucky Hair Studio; применимые решения перенесены в проект, остальные явно отложены или исключены.
- Реализовано ядро Telegram Production V1: webhook, consent/privacy gate, durable inbox/outbox, worker, scheduler, админка и read-only YCLIENTS projection.
- Закрыты release blockers удаления данных: YCLIENTS suppression, deletion ↔ projection race и privacy fence перед Telegram outbound.
- Release candidate объединён, прошёл полный Docker gate, развёрнут на staging и проверен сценарием rollback → restore.
- Проектные документы разделены по ролям; подробная история сохранена в архиве и changelog.
- Проведён свежий аудит всех 68 пунктов исторического `checklist.md` относительно текущего HEAD: 32 подтверждены, 24 реализованы частично или иначе, 12 не реализованы либо не имеют достаточного evidence. Полный отчёт: `docs/audits/Аудит production-checklist 2026-08-28.md`; отдельно выявлен local config mismatch `CONTEXT_MESSAGES_LIMIT=20 < COMPACT_THRESHOLD=30`, файлы конфигурации не изменялись.

## Активная работа

- **Спроектирована полная запись внутри Telegram через YCLIENTS:** владелец подтвердил, что бот создаёт запись без внешней ссылки и позволяет переносить/отменять только записи, созданные тем же Telegram-пользователем через бота. Выбран нативный детерминированный flow с inline-кнопками, Telegram contact button, PostgreSQL-state и существующими `BookingService`/`YclientsAdapter`; Mini App и LLM tool calling исключены. Целевой Docker baseline — `275 passed`. Design: `docs/superpowers/specs/2026-09-04-telegram-yclients-self-booking-design.md`.
- **Полная Telegram-запись локально реализована и проверена:** создание, `Мои записи`, перенос и отмена только собственных записей проходят через существующий `YclientsAdapter`; добавлены durable callback/contact ingress, один активный flow, stale-callback/duplicate/ownership/privacy/retention gates, `Отменить действие`, снятие contact-клавиатуры, fail-closed для отсутствующего или старше 24 часов каталога. Exact branch review закрыт; focused gate — `435 passed`, финальный полный Docker suite — `2197 passed in 1720.74s`, migration current/heads — `0025_telegram_booking_flow`, compile/Compose/diff-check — exit `0`. До production остаются customer-owned read-only acceptance, отдельно подтверждённый mutation smoke, ручная Telegram-приёмка и rollout/rollback. План: `docs/superpowers/plans/2026-09-04-telegram-yclients-self-booking.md`.
- **Развёрнуто на staging:** панель действий Evaluations стала полноширинной и симметричной: заголовок вынесен отдельно, недоступность валидатора показана спокойным статусом без checkbox, три действия имеют равную ширину и высоту, на мобильном складываются в столбец. Exact runtime `19c4f0e8`; 8/8 сервисов healthy, schema `0022_admin_statistics`, HTTPS/admin/webhook и публичный CSS проверены. Production не затрагивался.
- **Завершено локально в `codex/admin-evaluations-navigation`:** навигация админ-раздела Evaluations сведена к одной ссылке в sidebar и внутренним подкладкам наборов `answer`, `validator`, `router_v2`, `security` и `compact`. Данные, API, legacy URLs и eval runner не изменены; «Стирание ПД» не добавлено, так как такого набора сейчас нет. Docker admin E2E: `108 passed`.
- **Текущая задача:** ручной Baseline 2 после упрощения Security, Router v2 и Compact завершён: закрыты 26/26 сценариев, включая photo/voice/sticker и long-input. Long-input дал корректный отказ по лимиту; Telegram разделил 4250 символов на два update, поэтому обработка короткого хвоста отмечена как client-side нюанс, а не дефект. Найденные GPT-5 provider/reasoning и Security false-positive дефекты исправлены test-first и развёрнуты на exact `b50f512`; final Docker suite — `1687 passed`, staging 8/8 healthy, safe-log counters — нули. Реальные платные LLM-эвалы новых prompt не запускались и требуют отдельного явного разрешения. Production и YCLIENTS credentials не затрагивались.
- Design последовательности: `docs/superpowers/specs/2026-08-28-pre-yclients-release-completion-design.md`.
- Implementation plan: `docs/superpowers/plans/2026-08-28-pre-yclients-release-completion.md`; Tasks 1–4 и code/staging часть Task 6 завершены, технические recovery/rollback шаги Task 7 зелёные, targeted Telegram recheck остаётся открытым.
- Этап подключения YCLIENTS возобновлён: на совместном созвоне с заказчицей создаётся её бесплатное непубличное production-приложение, затем credentials безопасно передаются только в server-only staging `.env`; сначала выполняется read-only acceptance, а mutation smoke требует отдельного разрешения.
- Управление контентом: системный промпт, база знаний и статические бизнес-правила меняются только локально, версионируются Git и попадают на сервер через commit-pinned Docker-деплой. Админка не является источником изменений этого контента; отдельное DB-версионирование знаний не создаётся.
- Доступ к staging-админке: владелец подтвердил успешный вход индивидуального технического аккаунта `Влад` с паролем и TOTP. Отдельный аккаунт заказчицы откладывается до безопасной связи с ней.
- Решение по владению: текущее бесплатное непубличное приложение остаётся dev/test; production-приложение создаётся и контролируется заказчиком в его действующем YCLIENTS.
- Спецификация подключения: `docs/superpowers/specs/2026-08-20-customer-owned-yclients-app-design.md`.
- План реализации подключения приложения заказчика: `docs/superpowers/plans/2026-08-20-customer-owned-yclients-app-onboarding.md`.
- Правило возобновления этапа: только после отдельного решения владельца и безопасного доступа заказчицы к кабинету YCLIENTS выполнять подключение последовательно по указанному плану; логин, пароль, токены и TOTP не записывать в чат, Git или проектные документы.

## Доказано

- Полный Docker release gate: `1259 passed in 594.78s`.
- Независимый review нашёл deletion ↔ projection race; после TDD-fix повторный review не оставил Critical/Important blockers.
- Staging smoke: четыре app runtime healthy и совпадают с candidate image IDs; scheduler synthetic job завершился штатным `skipped`; HTTPS, webhook и safe log scan прошли, log counters — `0/0/0/0`.
- Backward compatibility: предыдущие bot/worker healthy на additive schema `0012`; откат образов и восстановление candidate подтверждены без DB downgrade.
- YCLIENTS диагностика: user permissions endpoint — `200`; records-list endpoint — `403` во всех безопасных read-only вариантах. В live-настройках dev/test-приложения обнаружено, что право «Просматривать список записей» не выбрано; это более конкретная вероятная причина `403`, чем завершившийся тестовый тариф.
- Project governance: обычный Docker test-profile видит только allowlisted документы; governance contract `4 passed`, совместный governance/architecture gate — `12 passed`, Compose config — exit `0`.

## Блокеры

- YCLIENTS production acceptance отложена: приложение заказчика ещё не создано и не подключено, а текущий dev/test-контур не имеет выбранного права «Просматривать список записей» и возвращает `403` на records-list.
- Production launch не выполняется до возобновления и завершения YCLIENTS acceptance, создания индивидуального аккаунта заказчицы с TOTP и подтверждённого входа через безопасный канал.
- Финальный pre-YCLIENTS human acceptance не закрыт, пока не повторены Telegram-сценарии `5/23/26/27` в подключённой авторизованной браузерной сессии.

## Что нужно от владельца

- Compact provider-разрешение уже использовано ровно один раз, gate закрыт; новых платных прогонов не требуется.
- Обновление работающего staging до точного commit-pinned кандидата разрешено 2026-08-28; новых решений для технического rollout не требуется.
- Авторизованные Telegram Web и staging-админка уже использованы для Baseline 2; новых действий по доступу сейчас не требуется.
- До возобновления YCLIENTS-этапа и связи с заказчицей новых действий по доступам YCLIENTS не требуется.
- При возобновлении согласовать безопасную совместную сессию создания production-приложения YCLIENTS и индивидуального TOTP-аккаунта заказчицы; логин, пароль, токены и TOTP не передавать в Git или чат.
- Отдельно разрешить одну синтетическую create → get → reschedule → get → cancel проверку только после успешного read-only gate; реальные записи клиентов не изменять.

## Дальше: Now / Next / Later

### Now

- [x] **Провести отраслевой анализ расчёта окупаемости бота:** текущая формула признана только верхней расчётной оценкой высвобождённой операторской ёмкости, а не ROI. Выявлено, что `COUNT(DISTINCT chat_id)` не выделяет отдельные сессии, молчание клиента не подтверждает решение, а периодные границы могут пропустить позднее вмешательство сотрудника. Рекомендована outcome-based модель: session resolution/FCR и quality guardrails → AHT по типу обращения → fully loaded productive-hour value → отдельно capacity value, cash savings, полные operating costs, net benefit, ROI и payback. До появления подтверждённых outcomes текущие `5 минут / 500 ₽` допустимы только как явно подписанный сценарий, не как факт; код, staging и production не менялись.
- [x] **Упорядочить боковую навигацию админки по рабочему сценарию:** разделы идут как «Диалоги → Статистика → Записи → Эскалации → Реактивация → Evaluations / Эвалы → Промпт → Управление → Логи». Существующие ограничения видимости по ролям сохранены. Docker admin regression — `41 passed`; изменение развёрнуто на staging в exact `0ecf770cdc20`. Production не затрагивался.
- [x] **Показать все системные промпты LLM-контура в админке:** раздел «Промпт» получил пять полноширинных вкладок — основной ответ, Router, LLM Security, Validation и Context. Основной промпт сохраняет существующие редактирование, версии, rollback и hot reload; четыре code-backed промпта показываются read-only с назначением и источником, без ложной кнопки сохранения. Изменение развёрнуто на staging в exact `0ecf770cdc20`; image и HTTPS-контракты подтверждены. Production не затрагивался.
- [x] **Расширить локальный preview и карточку LLM-моделей:** карточка на странице диалогов занимает две правые колонки и показывает фактические пары основной/резервной модели для Answer, Router, Security, Validation и Context; отсутствие runtime fallback у Router/Context отображается явно, Validator показывает включённость. Для быстрых визуальных итераций сохранён ignored Compose override в `tmp/`; сама карточка развёрнута на staging в exact `0ecf770cdc20`. Production не затрагивался.
- [x] **Развернуть Reactivation V2 на staging в безопасном `dry_run`:** exact `f8fa30d3609a725da403520de03c2de0ba9da468` объединён с актуальным `origin/main`, запушен обычным non-force push и развёрнут immutable tag `rc-f8fa30d…`. Migration линеаризована как `0023_reactivation_draft → 0024_reactivation_v2`; до cutover сохранены rollback bundle, image manifests и PostgreSQL dump, isolated compatibility gate подтвердил прежние bot/worker на новой schema. Независимая post-verification: 8/8 healthy, exact image contracts, HTTPS/admin, зарегистрированный owner-only `/marketing/`, webhook, compatibility cleanup и safe logs `0/0/0/0`; БД подтверждает `mode=dry_run`, `active_version_id IS NULL`, journeys `0`, поэтому клиентские отправки не активированы. Реальные Telegram-сообщения, YCLIENTS/LLM-вызовы и production не выполнялись. Перед production остаются обязательны legal reference, минимум 14 дней dry-run наблюдения, owner activation, первая ограниченная партия и emergency-stop rehearsal. Ручные рекламные рассылки остаются отдельной будущей задачей. Design: `docs/superpowers/specs/2026-08-31-admin-reactivation-v2-design.md`; plan: `docs/superpowers/plans/2026-08-31-reactivation-v2.md`.
- [x] **Упростить UX Reactivation V2 без изменения бизнес-логики:** выстроена последовательность «настройки → аудитория → тест → юридическое подтверждение → запуск», технические статусы переведены, emergency stop показывается только в `active`, добавлены Telegram-preview, компактная воронка, блок «Требует внимания», полезные пустые состояния и свёрнутые consent/events/legacy. Browser QA пройдена на desktop `1440 px` и mobile `390 px` для empty/ready/paused/errors и reminder on/off; horizontal overflow отсутствует. Docker gate: `47 passed`, compileall, Compose config и diff-check — exit `0`. После локального merge и non-force push exact `eb328d264770b59d1140b2639ca12a9d89290e14` развёрнут на staging как immutable tag: 8/8 healthy, schema `0024_reactivation_v2`, live HTML/CSS, HTTPS, webhook, compatibility cleanup, rollback artifacts и safe logs подтверждены; Reactivation сохранена в безопасном `paused:none`, journeys `0`. Backend, API, БД, eligibility, consent, scheduler и доставка не менялись; production и реальные Telegram/YCLIENTS/LLM-вызовы не затрагивались. Plan: `docs/superpowers/plans/2026-09-02-reactivation-v2-ui.md`.
- [x] **Упростить подтверждение запуска Reactivation:** обязательный `legal reference`, юридический чекбокс и ввод слова `АКТИВИРОВАТЬ` убраны из рабочего запуска. Новый owner-only путь реализован атомарно: свежий ненулевой расчёт аудитории → доставленный тест → итог «Сейчас подходят N клиентов. Сообщение будет отправлено только тем, кто дал согласие на рассылку» → кнопка «Запустить» с коротким подтверждением. При `eligible=0` запуск блокируется; consent/eligibility/pre-send fences и audit сохранены, `legal_*` оставлены deprecated в БД без миграции. TDD подтверждён RED→GREEN; полный Docker-набор Reactivation дал `275 passed`, compileall/Compose/diff-check — exit `0`. Локальный browser QA пройден на desktop `1440 px` и mobile `390 px`, включая zero-state и отсутствие horizontal overflow. Ветка опубликована как PR `#4`; exact runtime commit `fc4ad5b` развёрнут на staging immutable tag `rc-fc4ad5b…`: 8/8 healthy, schema `0024`, HTTPS/webhook/log scan и live UI contract зелёные, Reactivation оставлена в `paused:none` с `eligible=0` и disabled «Запустить». Production и реальные отправки не затрагивались. Design: `docs/superpowers/specs/2026-09-03-simplify-reactivation-approval-design.md`; plan: `docs/superpowers/plans/2026-09-03-simplify-reactivation-approval.md`.
- [ ] **Провести controlled staging-demo Reactivation V2 на двух тестовых Telegram-аккаунтах:** владелец подтвердил контроль обоих аккаунтов. Consent-фаза дала `2/2` processing/marketing proof и provider ACK без дублей. Admin-фаза пройдена до прежнего legal gate: два draft со штатным текстом, исходный preview `1 → 0 no_verified_identity`, synthetic verified preview `1 → 1`, ровно один version 2 test-send доставлен `@vladdautov` с provider ACK и подтверждён владельцем визуально в Telegram; первая попытка честно осталась `cancelled` без provider call из-за неполного временного restart. Три activation POST по прежнему контракту вернули `409`, mode сохранился `paused`, active version отсутствует, journeys/steps `0`. Synthetic projection и временный `BUSINESS_ALERT_CHAT_ID` восстановлены по server-only snapshot; финальный preview снова `eligible=0`, 8/8 healthy, bot unpaused, webhook и логи чистые. Подтверждены UX gaps: `not_configured` не показывает уведомление и timestamps выводятся сырым UTC. Упрощённый запуск развёрнут и проверен на staging; следующий gate — повторить controlled activation на тестовой аудитории без реальных клиентских сообщений.
- [x] **Исправить позднее включение рекламного consent на старой карточке:** controlled staging edge-case воспроизвёл fail-closed, но вводящий в заблуждение сценарий. Исправление test-first реализовано, review blocker по Redis/PostgreSQL rollback закрыт, post-review и post-merge regression дали `65 passed`; `main` запушен и exact `ab0f2dd` развёрнут на staging immutable tag `rc-ab0f2dd…`. Human retest на controlled `@vladdautov` подтвердил полный сценарий «policy only → Готово → старая карточка → ads on → Готово»: processing v1 сохранён, создан один unique `granted` event `telegram_explicit`, active marketing-v1 связан с proof/hash, ответ «Рекламные сообщения включены» доставлен один раз с Telegram provider ACK, Redis draft очищен. Финальная read-only сверка: 8/8 healthy, webhook pending `0`/last error `false`, свежие error lines `0`; Reactivation сохранена в `paused:pending:none`, journeys `0`. Production, YCLIENTS и реальные рекламные отправки не затрагивались.
- [x] **Убрать визуальное дублирование сообщений в деталях диалога:** события категории `message` больше не повторяют основной таймлайн; переписка показывается один раз как «Клиент → Бот». LLM-расход пользователя вынесен в отдельную раскрывающуюся карточку «Технические детали» с увеличенным отступом, спокойной подложкой и полной разбивкой Security/Router/Answer по клику. Docker admin regression — `23 passed`, Compose config — exit `0`. Exact `2aa1cc951aa9` запушен и развёрнут на staging как immutable tag; 8/8 сервисов, schema `0022_admin_statistics`, HTTPS/admin, live CSS, webhook, compatibility cleanup и safe logs подтверждены.
- [x] **Привести подкладки Evaluations к общему дизайну:** строка обычных ссылок заменена полноширинной сегментированной панелью с пятью равными desktop-вкладками, явным active/focus состоянием и безопасной мобильной прокруткой. URL, RBAC, шаблон и данные не менялись. TDD: RED на старом `display:flex`, GREEN `1 passed`; полный navigation regression — `10 passed`, Docker compileall и Compose config — exit `0`. Exact `79715ce919ec` запушен и развёрнут на staging; live CSS contract и общий post-deploy gate подтверждены. Design: `docs/superpowers/specs/2026-08-30-admin-eval-tabs-layout-design.md`; plan: `docs/superpowers/plans/2026-08-30-admin-eval-tabs-layout.md`.
- [x] **Обновить staging объединённой админкой:** `main` с runtime-кандидатом отправлен обычным non-force push, exact `f75a31644fa6` развёрнут как immutable tag `rc-f75a31644fa6…`. До cutover сохранены rollback bundle и дамп PostgreSQL; изолированный gate подтвердил старые bot/worker на новой schema. После cutover независимо подтверждены `0022_admin_statistics`, 8/8 healthy services, exact image IDs, HTTPS/admin, webhook, scheduler smoke, очистка compatibility-контура и нулевые safe-log counters. Production не затрагивался.
- [x] **Вернуть в текущий `main` пакет обучения администраторов по процедурам:** 11 исходных файлов по криокапсуле ICEQUEEN, водородотерапии и коллагенарию 650 нм снова лежат в `project/data/source_documents/OBUCHENIE_PO_PROCEDURAM_BEZ_RABOCHIH_REGLAMENTOV/`. Безопасные пользовательские правила уже отражены в системном промпте; неутверждённые регламенты, старые цены и медицинские выводы не стали активной базой ответов.
- [x] **Очистить и упорядочить данные проекта:** удалён ненужный Telegram Export и две иллюстрации водородотерапии; разовые отчёты эвалов перенесены из `project/llm/eval/` в `docs/archive/evaluations/`. В `services_prices.md` зафиксирован открытый вопрос о полном каталоге YCLIENTS и возможном резервном источнике цен.
- [x] **Сохранить доступный Google Docs как постоянный источник:** оригинал «Мороз и Солнце — функционал LLM-бота» сохранён в `project/data/source_documents/` в DOCX и Markdown. Требования из него пока не меняют промпт или код автоматически: их нужно сверять с актуальными ТЗ и дорожной картой.
- [ ] **Сохранить оригинал Google Docs «Основные принципы работы бота центра „Мороз и Солнце“»:** его содержание уже учтено в промпте и сводном прайсе, но постоянного оригинала в `project/data/source_documents/` нет. Публичный экспорт по переданной ссылке недоступен (`404`); нужен доступный экспорт или файл от владельца.
- [x] **Объединить пять параллельных веток админки:** Evaluations navigation, недельные Записи, LLM-аналитика сообщений, Реактивация и периодная Статистика объединены в `main` fast-forward на проверенный integration commit `4e3c6c8`. Alembic линеаризован как `0020_message_llm_analytics → 0021_admin_reactivation → 0022_admin_statistics`; runtime/UI/privacy-контракты обеих сторон сохранены. Docker evidence: focused gates `42 + 233 + 183 + 73 + 92 passed`, canonical full rerun `1757 passed`, merged-state smoke на `main` `113 passed`, targeted local admin smoke owner/admin/CSRF/RBAC/message analytics — OK. Runtime-кандидат запушен и развёрнут на staging как exact `f75a31644fa6`; технический post-deploy gate зелёный. План: `docs/superpowers/plans/2026-08-30-parallel-admin-integration.md`.
- [x] **Переделать существующую вкладку «Записи» в недельный календарь:** в ветке `codex/admin-zapisi` реализованы полный недельный вид 00:00–24:00, навигация, безопасные карточки всех каналов из единой YCLIENTS-проекции, раскладка пересечений, ручное создание и статусы «пришёл / не пришёл / отменить» через существующий scheduler/worker. Admin не получает YCLIENTS secrets; manual-записи не создают Telegram-уведомления на служебный ID, PII команды очищаются, status events идемпотентны и пригодны для будущей статистики. Существующие `owner/admin` видят все записи; роль «только свои» не добавлена, поскольку в проекте нет ограниченной роли и связи admin user → YCLIENTS staff. Docker evidence: финальный focused unit/contract/worker/e2e `195 passed`, PostgreSQL admin/no-notification `26 passed`, Compose config и compile зелёные. Изменение локальное: merge/push/deployment/YCLIENTS-вызовы не выполнялись. Design: `docs/superpowers/specs/2026-08-30-admin-bookings-calendar-design.md`; plan: `docs/superpowers/plans/2026-08-30-admin-bookings-calendar.md`.
- [x] **Добавить пометочную LLM-аналитику в деталях диалога:** для каждого обработанного сообщения явно показывать число LLM-вызовов, prompt/completion/total/cached токены, стоимость, экономию и модели/purpose; сообщения без обращения к LLM должны быть визуально различимы. Реализована точная связь `token_usage.source_message_id` с user-сообщением, три состояния старой/нулевой/фактической аналитики и группировка по purpose/model без изменения существующих агрегатов диалога. Docker feature regression: `174 passed`; privacy/retention: `12 passed`; Compose config и compileall: `exit 0`; единственная Alembic head: `0020_message_llm_analytics`. Design: `docs/superpowers/specs/2026-08-30-message-llm-analytics-design.md`; plan: `docs/superpowers/plans/2026-08-30-message-llm-analytics.md`.
- [x] **Автоматизировать безопасную аудиторию реактивации:** отдельный marketing consent сохраняется из Telegram-чекбокса «Согласен на рассылку»; сегменты взаимоисключающие (`спящие` → `постоянные` → `после визита`); создание кампании автоматически materializes точный draft-список, а confirmation не добавляет новых клиентов и повторно исключает revoked consent/`human_mode`/удалённые данные. UI показывает ID/username, количество и дату визитов, consent и статус; shortcut create+queue удалён. Scheduler, LLM и реальные отправки не подключены. При изолированном вливании в актуальный `origin/main` stale migration перенумерована и линеаризована как `0022_admin_statistics → 0023_reactivation_draft`. Docker gates: focused `86 passed`, широкий admin/privacy без одного доказанного baseline-файла `358 passed`, единственный Alembic head `0023_reactivation_draft`; compileall и Compose config — exit `0`. Exact `0949f34a3b5c` запушен и развёрнут на staging как immutable tag; 8/8 сервисов healthy, schema `0023_reactivation_draft`, HTTPS/admin/webhook, новый UI-контракт, compatibility cleanup и safe logs подтверждены. Production не затрагивался. Design: `docs/superpowers/specs/2026-08-30-admin-reactivation-design.md`; plan: `docs/superpowers/plans/2026-08-30-reactivation-safe-audience.md`.
- [x] **Добавить вкладку «Реактивация»:** реализованы owner-only настройки, отдельный versioned marketing consent с отзывом, детерминированные сегменты после визита / спящие / постоянные (от двух completed-визитов), черновики, внутренняя очередь и журналы без worker/LLM/реальных отправок. Processing consent не переносится; очередь исключает клиентов без активного marketing consent и с активным `human_mode`, а customer deletion удаляет consent и delivery-идентификаторы. Docker evidence: focused data/privacy/migration gates прошли; full suite дал `1700 passed, 6 failed in 1081.17s`, после чего все 6 failures (1 команда в новом plan и 5 отсутствующих visual mounts) закрыты отдельным gate `6 passed`. Compose config и Docker compileall прошли. Design: `docs/superpowers/specs/2026-08-30-admin-reactivation-design.md`; plan: `docs/superpowers/plans/2026-08-30-admin-reactivation.md`.
- [x] **Добавить owner-only вкладку «Статистика» за период:** реализованы московский период, достоверные пользователи/сообщения/sent auto replies, LLM usage, эскалации, known-only стоимость и экономия кэша в USD, а также сохраняемые минуты и ставка ₽/час. Автоматизированные диалоги, часы и экономия на операторе явно помечены расчётной оценкой; отсутствующие Security-события и неизвестные тарифы дают «Нет данных» с причиной. Проверки Docker: targeted gate `35 passed`, migration cycle `31 passed`, admin regression `283 passed`; migration branch-head `0020_admin_statistics`. Перед интеграцией обязательно синхронизировать и перенумеровать migration относительно параллельной `codex/message-llm-analytics`; общий `base.html` не менялся, но подключение router в `admin/app.py` может потребовать ручного совмещения. Design: `docs/superpowers/specs/2026-08-30-admin-statistics-design.md`; plan: `docs/superpowers/plans/2026-08-30-admin-statistics.md`.
- [ ] **Завершить Baseline 2 по `TESTING_PLAN.md`:** provider/reasoning/Security fixes развёрнуты на staging; все 26 ручных сценариев закрыты, из них 25 без дефекта и long-input с client-side нюансом Telegram; final full Docker regression — `1687 passed`. Ручная часть Baseline 2 завершена. Для полного модельного acceptance остались только реальные платные LLM-эвалы Security, Router v2, Compact и main answer; нужно отдельное явное разрешение.
- [x] **Упростить LLM Router по философии Lucky Hair Studio `5398f90`:** Router возвращает ровно один из семи маршрутов по strict `route + confidence`; перенос/отмена объединены в `booking_management`, жалобы/handoff — в metadata-only `escalation`, безопасный fallback — `consultation/0.0`. Сохранены masked общий budget `2000`, максимум 6 context messages, параллельный Security gate с использованием результата только после OK и cancel/drain при блокировке. Добавлены immutable suite `router_v2` (24 cases, 16 critical) и data-only migration head `0019_router_v2`; старые dataset/migration `0014` и history не менялись, админка читает оба формата. Docker evidence: core `54 passed`, post-review focused `277 passed`, migrations `31 passed`, final full `1684 passed in 996.23s`; review `0 Critical / 0 Important / 0 Minor`. Платный Router Evaluation нового prompt не запускался. Design: `docs/superpowers/specs/2026-08-29-simple-router-design.md`; plan: `docs/superpowers/plans/2026-08-29-simple-router.md`.
- [x] **Перевести локальный модельный профиль на актуальную связку:** основная LLM, Router, Security и Compact — `gpt-5.6-luna`; резерв answer/security — snapshot `gpt-5.4-nano-2026-03-17`; eval-судья — `gpt-5-mini` с отдельным потолком `JUDGE_MAX_TOKENS=250`. Модели разрешены в OpenAI project; локальный admin пересоздан с этими настройками. Платные provider/eval-вызовы не выполнялись.
- [x] **Упростить Compact истории по философии Lucky Hair Studio `5398f90`:** сохранены окно `40`, порог `30/31`, exact tail `10`, отдельная current replica, masked-only вызов перед answer LLM, tail fallback и safe alert/logs. Strict JSON, `CompactSummary`, пять обязательных категорий и отдельные `COMPACT_MODEL/API_KEY/BASE_URL` удалены; summary стала коротким проверенным текстом под недоверенным заголовком, Compact использует Router client/model с лимитом `400`. PostgreSQL/Redis summary не хранят, Compact Evaluation и админка сохранены. Migration `0017` и dataset не менялись, новая migration не создана. Docker evidence: focused `132 passed`, migrations/dataset `41 passed`, visual mounts `19 passed`, канонический full suite `1647 passed in 904.96s`.
  - Реальный provider-прогон после смены prompt не выполнялся: прежний `40/40` подтверждал JSON prompt, а новое платное разрешение отсутствует. При необходимости повторного quality acceptance нужен отдельный явный допуск владельца.
- [x] **Упростить security pipeline по философии Lucky Hair Studio `5398f90`:** local guardrails и PII masking сохранены; каждый local-pass current masked input получает exact `OK/BLOCK` через dedicated cheap Security provider с answer reserve; Router стартует параллельно, но его verdict не используется до allow, а при BLOCK task cancel+drain-ится. Double failure повторяет референсный fail-open со статическим `security_down` alert и safe logs. Semantic output-validator управляется только `OUTPUT_VALIDATOR_ENABLED=false`, обязательные локальные output checks не отключаются. Добавлена migration head `0018_simple_security`. Design: `docs/superpowers/specs/2026-08-29-simple-security-pipeline-design.md`; plan: `docs/superpowers/plans/2026-08-29-simple-security-pipeline.md`. Docker evidence: focused `569 passed`, migrations `30 passed`, post-full relevant regression `10 passed`; полный project-context suite дал `1629 passed / 24 failed`, из которых `19` — несвязанные visual tests из-за отсутствия root `/docs` в test image, а все `5` относящихся к патчу failures исправлены и перепроверены.
- [x] **Закрыть Compact Context + Compact Evaluation:** после отдельного разрешения выполнить один платный прогон `40` immutable кейсов на `gpt-4.1-mini`, подтвердить `100%` critical, не менее `95%` total, `0` errors и независимо сверить SQL-агрегаты.
  - Preflight 2026-08-28: Docker governance `4 passed`, единственная migration head `0017_llm_compact`, dataset `40/28 critical`, provider/judge presence checks — `set/set`, owned cleanup — `0/0/0`; внешних provider-вызовов не было.
  - Real-provider acceptance 2026-08-28: run ID `1`, `40/40` (`100%`), critical `28/28`, `0` errors, status `finished`, model `gpt-4.1-mini`; независимая SQL-сверка — `1|finished|gpt-4.1-mini|40|40|0|40|28|0|0`.
- [x] **После зелёного Compact gate выполнить свежий полный Docker gate, migration cycle до единственной head `0017_llm_compact`, статические/privacy-проверки и итоговый review на точном candidate commit.** Baseline focused `326 passed`; review/fix covering `337 passed`; финальный no-cache full `1649 passed in 998.77s`. Migration `0012→0017` сохранила legacy rollback row (`count=1`), single head `0017_llm_compact`; config/compile/diff/secret/PII gates зелёные, re-review `0 Critical / 0 Important / 0 Minor`, owned cleanup во всех namespaces `0/0/0`. Проверенный runtime source commit — `d8037f8755377a6027084c1ca91240c69cc4216d`.
- [x] Развернуть разрешённый точный commit-pinned кандидат `d18f67e40e0751ef04f0455e00bd778ffa57365c` на staging без YCLIENTS-мутаций и подтвердить healthchecks, schema и image IDs. Schema `0017_llm_compact`, apps `4/4` healthy, HTTPS/webhook/safe logs зелёные.
- [x] Развернуть fix-кандидат `17a78921b401912070a0f49f8c20548b9cd723c2` на staging без push/YCLIENTS/production; exact images, schema, HTTPS, webhook и grounding flag подтверждены.
- [x] Отправить актуальный `main` в GitHub и развернуть exact Simple Router candidate `537101197ffb94667dee94c672e64e03009b36bc` на staging. Fresh canonical Docker gate `1684 passed`; schema `0019_router_v2`, apps `4/4`, HTTPS/webhook/scheduler/safe logs и image-only rollback→restore зелёные; YCLIENTS credentials и production не затрагивались.
- [ ] Повторить затронутые Telegram-сценарии `5/23/26/27` в подключённой браузерной сессии, сверить admin/log evidence и оставить бота снятым с паузы.
- [x] Исправить подтверждённый приёмкой catalog-grounding blocker test-first и повторить локальные и технические staging-проверки.
- [x] На чистом кандидате выполнить финальные technical smoke, worker/Redis recovery, safe-log scan и image-only `candidate → previous → candidate` rollback rehearsal.
- [x] Зафиксировать модель владения production-приложением YCLIENTS и подготовить отдельную спецификацию.
- [x] Подготовить план реализации подключения приложения заказчика: минимальные права, `moroz_booking_key`, секреты, проверка чтения, синтетический цикл записи и откат.
- [x] Подтвердить успешный вход индивидуального технического аккаунта `Влад` с TOTP на staging.
- [x] Зафиксировать Git-only управление промптом и знаниями: локальные правки, отдельный коммит, changelog, commit-pinned Docker image и откат предыдущего проверенного коммита/образа.
- [x] Удалить устаревший технический маркер `project/.step`; текущий статус определяется только этой дорожной картой.
- [x] Полностью удалить из runtime/UI модуль «Review кейсов», не затрагивая технические Evaluations и другие разделы. Неиспользуемая таблица `eval_case_reviews` временно сохраняется только как rollback-совместимое legacy storage; contract-удаление допускается после истечения окна отката предыдущего admin image.

### Next

> **Обязательный входной gate для четырёх LLM-этапов:** до design и реализации выполнить `docs/project/Контракт адаптации LLM-референса Володи.md`: изучить Lucky Hair Studio на commit `5398f90`, составить таблицу «берём / адаптируем / отклоняем» и встроить выбранные решения в наши единые security pipeline и eval-runner. Runtime-компонент и его Evaluation закрываются только одной парой.

- [x] **LLM Router + Router Evaluation:** на основе референса Lucky Hair Studio адаптировать scripts-first маршрутизацию для сценариев Moroz i Solntse: сначала детерминированные команды и явные правила, затем LLM-вердикт по allowlist intents и недавнему контексту; определить безопасный fallback, трассировку без ПД и интеграцию с текущим message pipeline. В общий `eval_cases/eval_runs/eval_results` добавить Router Evaluation с ожидаемым intent, контекстными и пограничными кейсами, раздельной статистикой и перепрогоном ошибок.
  - Design-spec согласована: `docs/superpowers/specs/2026-08-20-llm-router-and-router-evaluation-design.md`. Runtime, additive migration, immutable 20-case dataset и suite-aware Router Evaluation в общей админке реализованы; focused gate — `328 passed`, финальный post-review full Docker suite — `1363 passed`, migration head — `0014_llm_router_evaluations`, whole-branch re-review — `0 Critical / 0 Important / 0 Minor`. Явно разрешённый real-provider acceptance на OpenAI `gpt-4o-mini`: run ID `1`, `19/20` (`95%`), критические `14/14`, gate passed; единственный некритичный mismatch — `other` → `offtopic`.
  - Историческая multi-intent реализация superseded 2026-08-29 задачей Simple Router из раздела Now. Её dataset, migration и acceptance evidence сохранены без переписывания; текущие прогоны используют отдельный suite `router_v2`.
- [x] **LLM Input Security + Security Evaluation (историческая реализация, superseded 2026-08-29):** первоначальная typed/JSON/fail-closed версия закрыла исходный этап; её runtime-архитектура позднее упрощена задачей Simple Security Pipeline выше до always-on current-only `OK/BLOCK`, cheap → reserve → fail-open+alert.
  - Design-spec: `docs/superpowers/specs/2026-08-25-llm-input-security-and-security-evaluation-design.md`; implementation plan: `docs/superpowers/plans/2026-08-25-llm-input-security-and-security-evaluation.md`. Реализованы typed in-process classifier, NFKC/zero-width local rules, masked fail-closed runtime с alert callback, immutable 40-case dataset, additive migration `0015_llm_input_security` и owner-only Security Evaluation в общей админке. После review контекст classifier/Router/answer выровнен единым total-bound, Router запускается строго после `ALLOW`, collision-жалобы уходят на review, runtime/eval provider settings совпадают, downgrade сохраняет cross-suite results; pinned dataset checksum устойчив к Git LF/CRLF conversion. Финальный review: `0 Critical / 0 Important / 0 Minor`; post-merge focused gate: `515 passed in 115.59s`; финальный merged-tree full Docker suite: `1460 passed in 727.20s`. Явно разрешённый real-provider acceptance на `gpt-4.1-mini`: run ID `1`, `40/40` (`100%`), критические `36/36`, gate passed. Пара `LLM Input Security + Security Evaluation` закрыта.
- [x] **LLM Validator + Validator Evaluation:** добавить отдельную проверку готового ответа на соблюдение продуктовых правил, безопасность, технические артефакты и корректный формат; определить единственную безопасную регенерацию и fallback без молчания для клиента. Добавить Validator Evaluation в общий eval-контур с позитивными, негативными и пограничными ответами, раздельной статистикой и regression-прогоном ошибок.
  - Согласованы design-spec `docs/superpowers/specs/2026-08-25-llm-validator-and-validator-evaluation-design.md` и implementation plan `docs/superpowers/plans/2026-08-25-llm-validator-and-validator-evaluation.md`: local-first + обязательный semantic validator для каждого provider-generated answer, trusted local replies без дополнительного вызова, один повтор с повторной полной проверкой, local-safe деградация с alert при недоступности quality-layer и immutable 60-case suite в общей Evaluations.
  - Локально реализованы typed validator, deterministic artifact rules, runtime/retry/fallback/alert/usage/telemetry wiring, checksum-pinned dataset + migration `0016_llm_validator` и owner-only read-only Validator Evaluation в общей админке. Post-review focused Docker gate — `407 passed in 205.28s`; финальный full Docker suite — `1577 passed in 1136.98s`; Alembic current/heads — единственный `0016_llm_validator (head)`; whole-branch self-review после fix-loop — `0 Critical / 0 Important / 0 Minor`. Real-provider acceptance на `gpt-4.1-mini` последовательно выявила два prompt-contract дефекта (`55/60`, затем `59/60`), оба исправлены test-first; свежий расширенный Validator/runtime/admin/privacy/dataset gate после последнего fix — `289 passed in 29.05s`. Финальный явно разрешённый run ID `1` в чистой acceptance-БД: `60/60` (`100%`), critical `20/20`, `0 error`, gate passed; независимая SQL-сверка подтвердила те же агрегаты. Пара `LLM Validator + Validator Evaluation` закрыта.
- [x] **LLM Compact Context + Compact Evaluation:** сжимать длинную уже обезличенную историю в фактическую сводку и недавний хвост, не теряя договорённости, открытые вопросы и важные ограничения; определить порог, формат summary и поведение при сбое. Добавить Compact Evaluation в общий eval-контур: пороговые диалоги, сохранение фактов и контекста, конфликтующие сведения, отсутствие выдуманных фактов и безопасная деградация.
  - Согласован bounded-window подход референса без постоянного хранения summary: окно `40`, порог `>30`, хвост `10`, safe tail fallback и общий suite `compact`. Изначальный strict JSON v1 заменён 2026-08-29 проверенной текстовой сводкой; актуализация записана в design-spec `docs/superpowers/specs/2026-08-26-llm-compact-context-and-compact-evaluation-design.md`.
  - Implementation plan: `docs/superpowers/plans/2026-08-26-llm-compact-context-and-compact-evaluation.md`.
  - Локальная реализация завершена: typed `ContextCompactor`, masked runtime wiring и usage/alerting, immutable `40` synthetic кейсов (`28` critical), additive migration `0017_llm_compact`, production-backed runner и owner-only read-only UI в общей админке. Summary/transcript и raw ПД не сохраняются; при сбое используется точный хвост из `10` сообщений.
  - Whole-branch self-review исходной JSON-реализации закрыл `2 Important / 2 Minor` test-first; post-review focused gate — `223 passed in 21.81s`, migration cycle сохранил соседние suite и single head `0017_llm_compact`, финальный full Docker suite — `1642 passed in 921.94s`, статические/privacy gates и cleanup — чистые. На том этапе чекбокс оставался открытым до отдельно разрешённого платного real-provider прогона `40` immutable кейсов и независимой SQL-сверки.
  - Явно разрешённый real-provider acceptance прежнего strict JSON prompt на `gpt-4.1-mini`: run ID `1`, `40/40` (`100%`), critical `28/28`, `0` errors, gate passed; независимая SQL-сверка подтвердила те же агрегаты. Пара исходной JSON-реализации была закрыта; для нового текстового prompt платный acceptance повторно не запускался.

### Отложено до возобновления YCLIENTS и связи с заказчицей

- [ ] Довести read-only центр записей: безопасный manual refresh и более точная атрибуция источника.
- [ ] Добавить allowlisted technical trace одного YCLIENTS-запроса без токенов, ПД и raw response body.
- [ ] Начать этап подключения YCLIENTS по плану `docs/superpowers/plans/2026-08-20-customer-owned-yclients-app-onboarding.md`; совместно с заказчиком создать бесплатное непубличное production-приложение и выдать только утверждённые минимальные права.
- [ ] Создать в филиале скрытое текстовое поле `moroz_booking_key` и проверить его свойства read-only запросом.
- [ ] Через безопасный канал записать Partner Token, User Token и `company_id` только в server-only staging `.env`.
- [ ] Пройти live read-only gate: permissions, services, staff, slots, records-list, projection и штатный scheduler без вывода ПД.
- [ ] После отдельного разрешения пройти одну синтетическую create → get → reschedule → get → cancel проверку и подтвердить отсутствие активного тестового дубля.
- [ ] Выполнить полный Docker gate, повторный review, staging acceptance и rollback rehearsal на финальном кандидате.
- [ ] Создать индивидуальный аккаунт заказчицы с TOTP и подтвердить её вход через безопасный канал.
- [ ] Выполнить production rollout только после закрытия YCLIENTS и TOTP gates.

### После закрытия YCLIENTS: отзывы и реактивация

- [ ] **Завершить полный feedback-flow после первого подходящего completed-визита.**
  - Сохранить действующие правила: запрос через `2 часа`, после `21:00` перенос на `10:30` следующего дня по Москве, один запрос за всю историю клиента.
  - Добавить кнопки `1–5` и детерминированный callback до LLM; не доверять `booking_id` из callback и проверять принадлежность записи клиенту по паттерну IDOR-защиты референса Володи.
  - Хранить состояние цепочки и комментарий долговечно в Postgres, а не только во временном Redis pending; после ответа переводить запрос в финальный статус и исключать повторную отправку при рестарте.
  - Для `1–3`: запросить необязательный комментарий, создать escalation, включить `human mode`, уведомить персонал и не предлагать скидку/компенсацию автоматически. Для `4–5`: поблагодарить и один раз предложить подтверждённую владельцем ссылку на Яндекс Карты.
  - Перед постановкой и непосредственно перед отправкой проверять ответ, удаление клиента и `human mode`; после ответа или ручного вмешательства цепочку прекращать. Добавить repair/replay для terminal failure без нарушения lifetime-ограничения.
  - Пройти Docker unit/integration/E2E: настоящий Telegram update → rating/comment → Postgres → positive branch или escalation/human mode; отдельно replay, duplicate callback, чужая запись, restart/crash, quiet time и suppression.
- [ ] **Реализовать безопасную реактивацию клиентов после `90 дней` без completed-визита и без содержательного диалога.**
  - Сначала добавить отдельное долговечное рекламное согласие `ads/marketing` с version/source/granted/revoked и постоянную suppression/отписку; processing consent не считать рекламным.
  - Отбирать кандидатов детерминированно в Postgres без LLM. LLM, если будет разрешена, может формировать только текст из allowlisted фактов; eligibility, consent и лимиты она не определяет.
  - Взять из референса Володи полезные механики: `auto_enabled=false` по умолчанию, owner-only управление, dry-run/preview, атомарный claim со stale recovery, reserve-before-send, уникальные delivery keys, журнал sent/skipped/failed и остановку серии после ответа по общей identity клиента.
  - Следовать нашему ТЗ, а не срокам референса: одно основное сообщение, максимум одно напоминание через `5 дней`, после двух молчаний остановка, новая кампания не раньше чем через `90 дней`; не добавлять автоматические скидки и произвольные ручные массовые кампании.
  - Непосредственно перед каждой отправкой повторно проверять active marketing consent, отсутствие suppression/отписки, новой записи, свежего содержательного ответа, удаления клиента и `human mode`; отправлять через существующий durable outbox с идемпотентностью, retry/DLQ и аудитом.
  - Пройти Docker unit/integration/E2E: consent grant/revoke, STOP, eligibility 89/90/91 день, meaningful reply, новая запись, human mode, удаление, первое сообщение/follow-up, frequency cap, duplicate/restart/crash, stale claim recovery и dry-run без реальной отправки.
  - Референс для адаптации, а не копирования: `D:/AI_OS/30_Sources/Automation_Courses/01_Raw_Materials/2026-08-13_lucky_hair_llm_chatbot_reference/project-edu-public/project/llm/`; зафиксированные расхождения — `docs/audits/Аудит решений бота Володи 2026-08-13.md` и `docs/audits/Аудит фоновых процессов и проект ЮKassa 2026-08-28.md`.

### После закрытия YCLIENTS: отдельный этап ЮKassa

- [ ] Начинать интеграцию ЮKassa только после полного YCLIENTS acceptance; не включать оплату в текущую Task 6 и первый pre-YCLIENTS/Telegram production-релиз.
- [ ] Зафиксированные продуктовые решения: оплата опциональная; сумма — `100%` серверной стоимости выбранной услуги; платёж создаётся только для существующей записи; неоплата или отменённый платёж не отменяют запись автоматически.
- [ ] До implementation plan согласовать срок действия ссылки, поведение при переносе записи, test `shopId`/secret, публичные HTTPS `webhook`/`return_url`, допустимые данные клиента и бухгалтерский контур чеков/54-ФЗ.
- [ ] Реализовывать отдельным модулем: локальная payment-модель и история статусов, backend-only создание с `Idempotence-Key`, кнопка на `confirmation_url`, durable webhook inbox, проверка актуального статуса через API ЮKassa, защита от дублей/событий не по порядку, reconciliation, аудит, метрики и role-based просмотр в админке.
- [ ] Возвраты на первом этапе не автоматизировать: бот создаёт admin task, решение принимает владелец или финансовый сотрудник, возврат выполняется вручную; не вводить штраф за no-show, удержание или автоматический перенос денег между записями.
- [ ] Сначала пройти mock/contract/replay/crash-recovery тесты и test-shop staging acceptance; production rollout оплаты — только отдельным решением после доказательств проверки.
- Аудит и предварительный проект: `docs/audits/Аудит фоновых процессов и проект ЮKassa 2026-08-28.md`.

### Later

- Постоянный профиль клиента и cross-channel identity.
- VK → Instagram → WhatsApp adapters через общий inbox/outbox.
- Ручные маркетинговые кампании — отдельно после безопасной автоматической реактивации, consent/unsubscribe и нового решения владельца.

### Исключено

- Голосовые сообщения и STT.

## Источники и подробности

- Цель: `ТЗ и архитектура.md`.
- Telegram Production V1: `План реализации.md`.
- Управление проектом: `docs/project/Система управления проектом.md`.
- Референс Володи: `docs/audits/Аудит решений бота Володи 2026-08-13.md`.
- Обязательный LLM reference gate: `docs/project/Контракт адаптации LLM-референса Володи.md`.
- История: `changelog.md` и `docs/archive/roadmap-history-through-2026-08-20.md`.
